# Progress Tracker

Update this file after every meaningful implementation
change.

## Current Phase

- **Unit 114 complete — frontend room reordering UI (2026-07-11).** Frontend-only unit (`frontend/` boundary; no new package, no backend/schema/solver change) letting the admin reorder rooms with **move-up / move-down** buttons on `/rooms`, driving the room-column order on both the `/timetable` and `/preferences` grids. Spec at `context/feature-specs/114-frontend-room-reordering-ui.md`. Depends on Unit 113 (`position` on `RoomResponse`, `PUT /rooms/reorder`). **Instant-update model:** all three routes read room order from the **same** `['rooms']` query and both grids render the `rooms` array in cache order (verified `TimetableGrid`/`PreferenceGrid`/`slots.ts` never re-sort rooms), so a single optimistic cache write re-orders the Rooms table **and** both grids with no refetch — the persist call fires in the background and the UI never waits on it. **API** (`lib/api/rooms.ts`): added `position: number` to the `Room` interface and `reorderRooms(orderedIds: string[]): Promise<Room[]>` calling `PUT /rooms/reorder` with `{ ordered_ids }`, reusing the existing `parseRoomError` structured-error handling. **Rooms page** (`routes/rooms.tsx`): a `reorderMutation` follows the Unit 100 optimistic pattern — `onMutate` cancels in-flight `['rooms']` fetches, snapshots the cache, and immediately `setQueryData(['rooms'], reordered)` (rebuilding each room with `position = index`); `mutationFn` calls `reorderRooms` in the background; `onError` restores the snapshot and surfaces the message via `getErrorMessage` in an inline `role="alert"` banner with an `AlertTriangle` icon; **no `invalidateQueries` on settle** (the optimistic cache is the source of truth between reorders — mirrors the Unit 103 drop-the-refetch decision, so the table/grids never flicker on the round-trip). Each row's Actions cell gained ghost `ChevronUp`/`ChevronDown` icon buttons (`aria-label="Move {name} up/down"`); `moveRoom` computes the **full** swapped id list from the absolute `rooms` cache order (not the filtered subset) and fires the mutation. Up is disabled on the first room, down on the last. Reorder acts on the absolute full order, so both buttons are **disabled while any search/type filter is active** (`roomFiltersActive(filters)`), with a `title` hint on each button plus a muted "Clear filters to reorder rooms." line above the table; rows always render in `position` (cache) order. No `components/ui/*` change; `TimetableGrid.tsx`/`PreferenceGrid.tsx` untouched per spec. **Tests** (`routes/rooms.test.tsx`, +5; `test/fixtures.ts` `makeRoom` gained `position: 0`; the rooms API module mock gained `reorderRooms`): move-down on the first room reorders the rendered rows **instantly** while the persist promise is still pending and sends the swapped full id order `['room-2','room-1','room-3']` to `reorderRooms`; up disabled on the first row / down on the last; both disabled + the hint shown while a search filter is active (and `reorderRooms` never called); a rejected persist **rolls back** to the prior order and shows the error; and the optimistic write targets the shared `['rooms']` cache (asserted ids + `position` 0..n, proving the timetable/preferences grids would reflect the same order). **Verification:** frontend **406 passed across 38 files** (401 prior + 5 new); `tsc -b` clean; production build green (`946.45 kB`/`275.35 kB` gzip; only the pre-existing large-chunk warning). Excel export column order and tutorial-letter tie-break are unchanged (out of scope — Unit 113/93 own those).
- **Unit 113 complete — backend room ordering persistence (2026-07-11).** Backend-only unit (`backend/` boundary; one new Alembic migration, no new package, no solver/frontend change) giving rooms a persisted, admin-controlled left-to-right `position` so the timetable can render room columns in that order instead of alphabetically. Spec at `context/feature-specs/113-backend-room-ordering-persistence.md`. The backend is a **pure store** for room order — it persists whatever full ordered id list the client sends and returns rooms in `position` order; the frontend reorder UI and instant-update behaviour are **Unit 114** (out of scope). **Model** (`models/room.py`): new `position: int` column, `nullable=False` with a Python-side `default=0` so direct construction and legacy inserts stay valid while `create_room` overrides it with the real append value. **Migration** (`0016_room_position_ordering.py`, revises `0015`, new head): adds `position` nullable, backfills by the existing alphabetical `name` order (`row_number() OVER (ORDER BY name) - 1`, 0-based) so the visible timetable order is preserved verbatim, then sets `NOT NULL`; downgrade drops the column. Positions need not stay contiguous — deletes may leave gaps, which is fine because ordering is by ascending `position`. **Service** (`services/room.py`): `list_rooms` now orders by `Room.position, Room.name` (name tiebreak); `create_room` assigns `position = (max existing position) + 1` (appends to the end — a new room is the right-most column until moved); `delete_room` unchanged (a positional gap is acceptable); new `reorder_rooms(db, ordered_ids)` validates that `ordered_ids` is **exactly** the set of all existing room ids (no missing, unknown, or duplicate ids), raising a structured `AppError("rooms_reorder_mismatch", …, 422)` and persisting nothing on any mismatch, otherwise assigning `position = index` for each id in one atomic commit and returning the reordered list. **Schema** (`schemas/room.py`): `RoomResponse` gains `position`; new `RoomReorderRequest { ordered_ids: list[str] }`; `RoomCreate`/`RoomUpdate` deliberately do **not** accept `position` (mutated only via reorder). **Route** (`api/rooms.py`): new `PUT /rooms/reorder` behind `get_current_admin`, declared **before** `PUT /{room_id}` so `reorder` is not captured as a room id. **Tests** (`tests/test_113_room_ordering.py`, 12): create appends to the end; `list_rooms` orders by position with name tiebreak; delete leaves relative order intact; reorder persists + returns the new order; missing/unknown/duplicate id → 422 `rooms_reorder_mismatch` with nothing persisted; `RoomResponse` includes `position`; and `PUT /rooms/reorder` reorders via a minimal ASGI client and requires admin (401 unauthenticated). **Verification:** backend **607 passed** (full suite); `alembic heads` → single head `0016`.
- **UI polish pass — Delete-all in the filter row, smaller scheduled-card text, matched preference room text (2026-07-11).** Frontend-only (`frontend/` boundary; no schema/API/solver change), three reported UX tweaks. **(1) Delete all moved onto the search/filter row.** `components/filters/FilterBar.tsx` gained an optional `trailing` node rendered right-aligned (`ml-auto`) after the children/Clear-filters button. `routes/{units,lecturers,students}.tsx` each removed the separate `flex justify-end px-4 py-2 border-b` toolbar row above the table and instead pass their existing destructive **"Delete all"** button as `FilterBar`'s `trailing` prop — same button, same `openDeleteAll` handler and confirmation dialog, now inline with search + filters (still only rendered when rows exist, unchanged since `FilterBar` renders under the same `data.length > 0` guard). **(2) Smaller scheduled-session card text.** `features/timetable/ScheduledSessionCard.tsx` unit-code + type-line dropped from `text-xs` to `text-[0.65rem]`, the multi-slot student-count line to `text-[0.6rem]`. Card label format left as-is — already `UNITCODE CLASSTYPE [ORDER] (INITIALS)` (e.g. `HIS101 Lecture (SC)`, `THE202 Tutorial A (LH)`), verified correct against the prior Excel-export-matching work. **(3) Preference room header matches timetable.** `features/preferences/PreferenceGrid.tsx` room sub-header now uses the same `extended ? 'text-[0.65rem]' : 'text-[0.4rem]'` sizing as `TimetableGrid.tsx` (previously fixed at `text-[0.65rem]`), so the narrow `/preferences` grid shrinks its room labels identically to `/timetable`. **Verification:** affected Vitest suites green (`ScheduledSessionCard`, `units`, `lecturers`, `students`, `preferences` — 78 passing); no new lint errors introduced (the `preferenceCellKey` react-refresh export warning and the `units`/`students` memo warnings are pre-existing). `DragPreviewCard.tsx` left unchanged (pool-drag preview, not a scheduled block).
- **Bug-fix pass — title-case name normalization on upload + Delete-all for units/lecturers/students (2026-07-11).** Two fixes spanning both boundaries. **(1) Title case on upload.** New shared `backend/services/text.py::to_title_case(value)` (capitalizes after spaces/hyphens/apostrophes — e.g. `"o'brien"` → `"O'Brien"`, `"ancient HISTORY"` → `"Ancient History"`; non-alphabetic tokens like digits are left untouched). Wired into `services/lecturer_import.py::_classify_row` (lecturer `first_name`/`last_name` and unit `unit_name`, applied after row validation so blank-check semantics are unchanged) and `services/student_import.py::_classify_row` (student `first_name`/`last_name`). Scope is deliberately **upload/import only** (manual create/edit forms are untouched) per explicit product decision. Names are normalized before persistence (not display-only), matching the existing "update existing names from CSV" contract for both importers. **(2) Delete-all for units/lecturers/students.** New atomic bulk-delete services — `services/unit.py::delete_all_units`, `services/student.py::delete_all_students` (both simple bulk cascade-and-commit, matching the existing per-row cascade behavior), and `services/lecturer.py::delete_all_lecturers` (pre-checks every lecturer for a still-assigned `Session.lecturer_id` reference — the one genuine non-cascading FK per Unit 111 — and rejects the **whole** operation with the existing `lecturer_delete_blocked` 409 if any lecturer is blocked, so a bulk delete is all-or-nothing and never partially empties the table). New routes `DELETE /units`, `DELETE /lecturers`, `DELETE /students` (empty path, distinct from the existing `/{id}` routes), each admin-protected, 204 on success. Frontend: new `deleteAllUnits`/`deleteAllLecturers`/`deleteAllStudents` in `lib/api/{units,lecturers,students}.ts`. Each of `routes/{units,lecturers,students}.tsx` gained a **"Delete all"** destructive button in a small toolbar row directly above the table (`flex justify-end`, right-aligned above the Actions column — only rendered when rows exist) plus a confirmation `Dialog` (Design Invariant re: destructive-changes-require-confirmation) reusing the existing `deleteBlockedMessage` helper for the blocked-delete reason and the same invalidate-on-success query keys as the corresponding single-delete mutation. **Tests:** new `backend/tests/test_text_title_case.py` (10 cases incl. hyphenated/apostrophe names, mixed case, numeric passthrough), new title-case assertions in `test_104_lecturer_unit_csv_import.py` and `test_90_student_csv_import.py`, new `backend/tests/test_delete_all.py` (6 — units/students bulk delete + empty-table zero-count, lecturers bulk delete succeeds when unblocked, lecturers bulk delete rejected all-or-nothing when any lecturer teaches a session). Frontend: new "delete all" describe blocks in `units.test.tsx`/`lecturers.test.tsx`/`students.test.tsx` (button visible + removes all rows on confirm; blocked delete surfaces the reason and keeps rows) — the pre-existing single-row delete tests' `getByRole('button', { name: /Delete/ })` regex matches were tightened to the exact string `'Delete'` since the new "Delete all" button now also matches `/Delete/`. **Verification:** backend **595 passed**; frontend **401 passed across 38 files**; `tsc -b` clean; production build green (`945.09 kB`/`274.94 kB` gzip; only the pre-existing large-chunk warning).
- **Post-Unit 112 bug-fix pass — lunch-crossing block rendering, contracted room text, Excel-matching session labels (2026-07-11).** Frontend-only (`frontend/` boundary; no schema/API/solver change), three reported UX issues fixed on `/timetable`. **(1) Blocks no longer visually cover lunch.** `features/timetable/blocks.ts`'s `buildBlockAnchorData` merged a block group's consecutive slot cells into one rectangle purely by numeric slot-index adjacency, so a block spanning `s3`→`s4` (crossing the AM/PM boundary) merged into a single tall card whose absolutely-positioned height stretched over the Lunch/Mass divider row instead of stopping at it (sessions can't cross lunch per the existing blocking rule, but blocks — being cell reservations, not sessions — could and did). Added a `PM_START_INDEX` boundary check so a slot run always breaks at `s3`→`s4` even though the indices are numerically consecutive; a block spanning both AM and PM in a room now renders as two independent rectangles (one per side), so the divider row always shows through. New regression test in `blocks.test.ts`. **(2) Smaller contracted-view room text.** `TimetableGrid.tsx`'s narrow (non-extended) room sub-header size dropped from `text-[0.55rem]` to `text-[0.4rem]` (extended-layout size `text-[0.65rem]` unchanged); `ui-context.md` updated. **(3) Scheduled session cards match the Unit 93 Excel export label format.** `ScheduledSessionCard.tsx`'s type line now reads `Lecture (INITIALS)` / `Tutorial [LETTER] (INITIALS)` next to the bold unit code (e.g. `HIS101 Lecture (SC)`, `THE202 Tutorial A (LH)`), mirroring `_session_label` in `services/timetable_excel_export.py`; the separate full lecturer-name line was removed since initials now live in the type line. New `lib/lecturerInitials.ts` (`getLecturerInitials`, drops the leading title token from `lecturer_display_name` and returns first+last initials) and new `features/timetable/tutorialLetters.ts` (`computeTutorialLetters`, assigns per-unit tutorial order letters ordered by day/start-slot/room — tie-broken by a frontend-local copy of the export's fixed `ROOM_ORDER` — mirroring but computed independently from the backend's export-only `_tutorial_letters`; letters can therefore diverge from a given export when the draft has unsaved tutorial placements). Threaded `tutorialLetter` through `TimetableGrid → GridCell → ScheduledSessionCard`. New tests: `lecturerInitials.test.ts`, `tutorialLetters.test.ts`; updated `TimetableGrid.test.tsx`'s room-text-size assertion. `ui-context.md` updated with the lunch-boundary block-splitting rule and the new session-card label format. **Verification:** all touched-file Vitest suites green; the two `units.test.tsx` failures seen on a full parallel run (a modal-open timeout and a stale-checkbox assertion) are pre-existing/unrelated — that file was not touched.
- **Unit 112 complete — frontend delete-blocked error surfacing (2026-07-10).** Frontend-only unit (`frontend/` boundary; no backend/schema/solver change) surfacing the Unit 111 structured `*_delete_blocked` 409 reason to the admin instead of a generic failure or silent no-op. Spec at `context/feature-specs/112-frontend-delete-blocked-error-surfacing.md`. **Shared helper** (`lib/api/deleteErrorMessage.ts`): new `deleteBlockedMessage(err)` reads the raw `ApiRequestError.detail.error.message` (the Unit 111 envelope) directly rather than trusting the client's own `err.message` fallback text, returning the backend's exact reason when present (trimmed non-blank) and the new `GENERIC_DELETE_BLOCKED_MESSAGE` ("Couldn't delete — it's still in use.") only when no structured message exists (blank message, non-`ApiRequestError` failure, or a raw-status client fallback) — precisely the spec's "do not overwrite it with a generic string" rule. **`routes/rooms.tsx`, `lecturers.tsx`, `units.tsx`, `students.tsx`:** each top-level delete mutation's `onError` now calls `deleteBlockedMessage(err)` instead of `err.message`, and each delete-confirmation dialog's error paragraph gained an `AlertTriangle` icon + `role="alert"` (Design Invariant 8 — never colour alone), matching the icon+text idiom already established for unit-code/session-lecturer validation errors in `units.tsx`. No page changed its existing invalidate-on-success-only pattern (none of the four ever did optimistic/eager row removal — confirmed via a repo-wide `setQueryData` search — so a blocked delete already leaves the row in place structurally; this unit only fixes the message and the colour-only presentation). **Session delete UI inside the unit modal** (`units.tsx`): `removeSession` still locally strips a session from the pending edit-form draft (the same draft-then-Save pattern used elsewhere in the app — not a query-cache optimistic write), and the actual `deleteSession` call only fires inside `editMutation` at Save time. Added a **rollback reconciliation**: on `editMutation` `onError`, the handler now re-fetches `listUnitSessions(variables.id)` and merges back into `editForm.sessions` any backend session absent from the current draft — so a session removal that didn't actually persist (blocked or otherwise failed) reappears in the form rather than looking permanently gone; a session that *did* delete successfully is untouched (present in neither list, nothing to restore). The shared `UnitFormFields` error paragraph (used by both create and edit) also gained the icon treatment. **Tests:** new `lib/api/deleteErrorMessage.test.ts` (4 — structured message passthrough, generic fallback for a client-fallback message, generic fallback for a blank structured message, generic fallback for a non-`ApiRequestError`); new `routes/rooms.test.tsx` (3 — blocked delete shows the reason and keeps the row via `getByRole(..., {hidden:true})` since the open confirmation dialog `aria-hide`s the background table, generic fallback, successful delete removes the row); `lecturers.test.tsx` (+2), `students.test.tsx` (+3), and `units.test.tsx` (+4, including the two new session-restore-on-block and no-restore-on-success modal tests) each gained a `— delete-blocked error surfacing (Unit 112)` describe block. **Verification:** frontend **387 passed across 36 files**; `tsc -b` clean; production build green (`939.77 kB`/`274.11 kB` gzip; only the pre-existing large-chunk warning). No new dependency; no protected `components/ui/*` change.
- **Unit 111 complete — backend guarded deletes with dependency reasons (2026-07-10).** Backend-only unit (`backend/` boundary; no schema/migration/new-package change) replacing raw/unhandled `IntegrityError`s on delete with a structured `409` naming the blocking dependency. Spec at `context/feature-specs/111-backend-guarded-deletes-with-dependency-reasons.md`. **Model audit first:** checked every FK referencing `rooms`, `lecturers`, `units`, `students`, and `sessions` — `unit_students`, `unit_lecturers`, `timetable_block_cells.room_id`, `lecturer_preferences.{lecturer_id,room_id}`, `timetable_assignments.{session_id,room_id}`, and `session_student_allocations.{session_id,student_id}` **all** carry `ondelete="CASCADE"` (architecture invariants already define these as intended cascades — room delete unschedules assignments and frees block cells/preferences; unit delete removes child sessions per spec 25; student/session delete cascade allocations with existing rebalance). The **one** genuine non-cascading reference is `Session.lecturer_id` (`ForeignKey("lecturers.id")`, no `ondelete`, confirmed in both `models/session.py` and migration `0010`) — deleting a lecturer still assigned to teach a session would otherwise hit a raw `IntegrityError`. Per the spec's "respect existing intended cascades; only block where a real reference would otherwise fail or orphan data" rule, only **`delete_lecturer`** (`services/lecturer.py`) gets a real pre-delete dependency check: `_blocking_unit_codes` queries `Session.lecturer_id == lecturer_id` joined to `Unit.code`, and if any exist raises `AppError("lecturer_delete_blocked", ..., 409)` naming the affected unit codes (deduped, sorted, capped at 5 + "and N more" via `_format_unit_list`) before any delete is attempted. **`delete_room`, `delete_unit`, `delete_student`, `delete_session`** (`services/room.py`, `unit.py`, `student.py`, `session.py`) each keep their existing cascade/rebalance behavior unchanged and gain a `try/except IntegrityError` around the delete+commit (student/session wrap the existing flush+rebalance+commit block too), rolling back and raising the matching `{entity}_delete_blocked` 409 with a generic non-leaking message — a defensive fallback per the spec, since none of these four have a genuine blocking FK under the current schema, but the catch guards against future schema drift and never lets a raw 500/stack trace surface. All five now match the existing `delete_timetable_block` pattern (`services/timetable_block.py`), which already had this guard. **Tests** (`tests/test_111_guarded_deletes.py`, 7 new): lecturer delete blocked by a still-assigned session (single + multiple unit codes in the message) and succeeds when the lecturer is team-only (never session-assigned); room/unit/student/session delete each still succeed and perform their intended cascade (room frees the assignment; unit removes its session; student delete rebalances remaining allocations to 0; session delete frees its assignment) — run against the in-memory SQLite `db` fixture with `PRAGMA foreign_keys=ON`, so the un-cascaded `Session.lecturer_id` reference is genuinely enforced. **Verification:** backend **578 passed** (571 prior + 7 new). No frontend change (frontend display of these messages is **Unit 112**, out of scope here). `context/progress-tracker.md` updated (this entry).
- **Unit 110 — Timetable block click-toggle creation (2026-07-10).** Frontend-only (`frontend/` boundary; no schema/API/solver change). Spec at `context/feature-specs/110-timetable-block-click-toggle-creation.md`. Replaces the block-creation cell-selection mechanic: instead of a two-click anchor-then-rectangle expansion, block mode now uses **per-cell click toggling**, the same interaction pattern as the `/preferences` grid — each cell click toggles it in/out of the pending selection (neutral → selected → neutral), independently of every other cell. Selection is now an arbitrary set of individually chosen cells; it no longer has to be a rectangle and cells no longer need to be adjacent to begin selecting. **`features/timetable/blockSelection.ts`** rewritten: removed `computeRectangleSelection`, `singleCellSelection`, and the `SelectionRoom`/room-ordering dependency; added `toggleCellSelection(selected, cell, blockedCells)` (adds/removes the clicked cell; refuses already-blocked cells, leaving the selection unchanged; a click on a different day than the current selection replaces the whole selection with just that cell, preserving the single-day block-model constraint) and `selectionDay(selected)` (derives the shared day from the selection's keys, or `null` when empty). **`routes/timetable.tsx`**: removed the `blockAnchor` state entirely; `handleBlockCellSelect` now calls `toggleCellSelection` directly against the `blockSelectionKeys` set — no anchor/rectangle bookkeeping. `GridCell`/`TimetableGrid` needed no functional changes (their `onBlockCellSelect` callback was already a plain per-cell click handler with no drag/mousedown-mousemove logic; only doc comments were updated to describe the toggle semantics). `TimetableActionBar.tsx` block-mode instruction text rewritten to describe click-to-select/click-again-to-deselect instead of the old anchor-then-rectangle phrasing. The existing merged-rectangle **render** rule (contiguous same-group cells render as one card; non-contiguous produce separate rectangles, each with its own anchor) is unchanged and untouched by this unit — merging remains purely a rendering concern in `blocks.ts`/`BlockCellCard.tsx`, decoupled from how cells are selected. Naming/colour (block dialog), persistence timing (Unit 109's draft-first, Save-time reconciliation), and the unnamed/named block colour rules (Units 84/85) are all unchanged. **Tests:** `blockSelection.test.ts` rewritten for `toggleCellSelection`/`selectionDay` (select-from-empty, deselect-on-repeat-click, non-adjacent-cell addition without any rectangle, already-blocked-cell refusal, cross-day reset to a single fresh cell); `routes/timetable.test.tsx`'s Unit 86 block-mode suite updated — the two-click test renamed to reflect toggle semantics (assertions unchanged since two individual clicks happen to land contiguous cells), added a new click-again-to-deselect test and a new explicit non-contiguous/gapped 3-cell selection test (skips a room column and a slot row, still saves as 3 independent cells) using a 3-room fixture, and fixed the auto-unschedule regression test that previously relied on the old anchor-jump-covers-the-middle-cell rectangle behavior (clicking s1 then s3 no longer implicitly selects s2) by clicking s1, s2, and s3 individually. **Verification:** frontend **371 tests passing** (34 files); `tsc -b` clean; production build green (`938.77 kB`/`273.87 kB` gzip; only the pre-existing large-chunk warning). No new dependency; no protected `components/ui/*` change; `ui-context.md`/`architecture-context.md` needed no edits (neither described the old anchor/rectangle selection mechanism — only the still-accurate render-merge rule).
- **Unit 109 — Timetable blocks folded into the draft (2026-07-10).** Frontend-only (`frontend/` boundary; reuses the existing block CRUD API at save time — no backend/schema/solver change). **Contract change:** block edits are no longer immediately persisted through the block API; they now live in the **unsaved timetable draft** alongside assignment edits and persist together on the single **Save**. The dirty-draft block-editing guard is removed — the admin can edit blocks whether or not the draft is dirty. New `features/timetable/draftBlocks.ts` owns the draft-side `DraftBlock` type (existing blocks keep their server id; draft-created blocks get a synthetic `new:<uuid>` id), plus `savedBlocksToDraft`, `computeBlockDiff` (creates/updates/deletes vs saved blocks), and `computeSavedBlockFingerprint`. `draftStorage.ts` bumped to schema **v2**: the stored draft now carries `blocks` + `savedBlockFingerprint` and is validated/round-tripped on load; legacy v1 keys are swept. `buildBlockedCellMap` was loosened to a `BlockLike` param so it flattens either saved or draft blocks. In `routes/timetable.tsx`: a new `draftBlocks` state is seeded from the `['timetable-blocks']` query, `blockedCellMap` is derived from it, hydration/restore/persist now cover both layers (gated on both `savedAssignments` and `blocks` loaded), block create/edit/delete mutate the draft locally (auto-unscheduling overlapping draft assignments via the existing blocking-cleanup effect), and the three immediate-persist block mutations were removed. **Save order (stated in the route):** reconcile blocks first (delete → create → update) via `persistBlockDiff`, then persist the full assignment set — so the backend block state matches the draft blocks the assignments were validated against, and the full-replace assignment save is authoritative over any saved assignment a block-create unscheduled moments earlier. `BlockEditDialog` now takes a `DraftBlock`; block create/edit dialogs no longer show a persistence spinner. Immediate-persist messaging replaced by lightweight draft-edit notices ("Block added to / updated in / removed from the draft."). Tests: new `draftBlocks.test.ts`; `draftStorage.test.ts` rewritten for v2 + block round-trip/discard; the timetable route Unit 85/86/106 block tests reworked to the draft model and a new Unit 109 suite added (block editing available while dirty, edits deferred to Save, create-over-draft-cell auto-unschedule, draft block round-trip across remount, save-failure keeps both layers). `project-overview.md`, `architecture-context.md`, and `code-standards.md` updated. Frontend typecheck + full Vitest suite green (unrelated lecturers/students/units route timeouts are pre-existing full-run flakiness; they pass in isolation).
- **Unit 108 — Timetable grid view controls & session search/filter (2026-07-10).** Frontend-only (`frontend/` boundary; no schema/API/solver change). Three changes to the `/timetable` grid controls row (the Unit 103 extend + particular-days row): **(1) Course / lecturer / student search + filter.** New view-only search input on the **left** of the controls row (`GridViewControls.tsx` gains optional `searchQuery`/`onSearchChange`; only rendered when a handler is passed, so `/preferences` is unaffected). Matching lives in the new `features/timetable/sessionFilter.ts` (`buildStudentSearchIndex` maps allocated student ids → lowercased `first last number`; `sessionMatchesSearch` matches unit code/name, session-level lecturer (or team names when supplied), and any allocated student name/number). **On the grid**, non-matching scheduled cards are dimmed in place (opacity, never hidden/moved) via a `dimmedSessionIds` set threaded `TimetablePage → TimetableGrid → GridCell → ScheduledSessionCard`; an empty query dims nothing. **In the unscheduled pool**, non-matching sessions are hidden — the grid query is layered on top of the pool's own Unit 76 search + year filter (`UnscheduledPool` gains `studentIndex` + `externalSearch`), and the pool's own search is extended to also match students. The route loads `['students']` to build the index. Filtering is purely visual: it never mutates assignments/blocks/preferences, and validation/the solver still run on the full draft. **(2) Smaller narrow-grid room text.** Room sub-header font drops from `text-[0.65rem]` (extended) to `text-[0.55rem]` (narrow/non-extended) while keeping `truncate`; day headers/time labels unchanged. **(3) Extend view 2× narrower.** `EXTENDED_MIN_COL_PX` halved 92 → 46 in `gridView.ts`, roughly halving the extended grid width while still overflowing the container (scroll stays inside the grid box) and still triggering the `ResizeObserver`/`onMetricsChange` recompute. Vitest coverage added for the matcher, pool student-hiding, narrow room text, and the halved extended width; `ui-context.md` updated.

- **Unit 107 follow-up fixes — action-bar messaging, drag snap, warning marker, grid clipping (2026-07-08).** Frontend-only bug-fix pass (`frontend/` boundary; no schema/API/solver change) addressing five reported timetable issues. **(1) One message at a time.** `TimetableActionBar.tsx` no longer stacks the persistent clash line beneath the transient line (reverses the Unit 106 "show both" behaviour). A single `displayMsg = primaryMsg ?? clashMessage` renders: a transient message (error / solver lifecycle / notice / hint) sits **on top of** the scheduling-clash message, which is never removed — it keeps gating the solver, stays reachable via the "View details ({n})" toggle (now shown whenever `hasIssues`, even while a transient covers it), and re-appears as the message once the transient is dismissed or cleared. **(2) Drag drops under the pointer.** The drop target resolves to the cell directly under the pointer via the Unit 107 `pointerSlotCollision` (`pointerWithin` + `rectIntersection` fallback); the `DragPreviewCard` overlay is centred width-wise on the cursor with the cursor at the vertical centre of the first slot (Unit 78 modifier). **Correction (same day):** an initial attempt to *snap* the overlay onto `over.rect` (`x/y = over.rect.left/top`) was wrong — the `DragOverlay`'s on-screen top-left is `draggingNodeRect (base) + transform`, not the transform alone, so setting an absolute transform double-counted the base and threw the preview off-screen (pool drags invisible over the grid; scheduled-card moves teleported and felt un-draggable). Reverted `createPointerAlignModifier` to the delta-only form that never replaces the default `transform`, restoring visible, pointer-tracking drags for both pool and scheduled cards while keeping `pointerWithin` as the drop-under-pointer mechanism. **Second correction (preview under the cursor):** the delta form still computed its grab offset from `draggingNodeRect` (= `dragOverlay.rect`, the moving preview), so for pool cards — whose overlay is a different width and whose measured rect tracks the moving overlay — the offset drifted and the preview slid out from under the mouse. Per the dnd-kit 6.3.1 source, the `DragOverlay` is positioned relative to `initialRect = useInitialValue(activeNodeRect)` (the ORIGINAL draggable's rect, which stays put during the drag), so the modifier now measures `offsetX/offsetY` from `activeNodeRect` instead. The preview is centred width-wise on the cursor with the cursor at the first slot's vertical centre, matching where `pointerWithin` drops it. **(3) Warning marker moved.** `ScheduledSessionCard.tsx` removed the `AlertTriangle` from the top-right group (it overlapped the unschedule ✕) and re-rendered it `absolute bottom-1 right-1`; the `aria-label="Scheduling warning"` is unchanged. **(4)+(5) No stray scrollbar / no overshoot.** The bordered grid box in `TimetableGrid.tsx` gained `overflow-hidden`, so the absolutely-positioned session/block cards (which can spill a sub-pixel past the last row/column and otherwise made the horizontal-scroll wrapper show a spurious vertical scrollbar) are clipped to the table — every card is capped at the grid bounds and the vertical scrollbar no longer appears. **Tests:** rewrote the Unit 106 "keeps a clash warning visible alongside a transient notice" test as "shows a transient notice on top of a clash warning without removing the clash" (notice replaces the warning message, solver stays disabled, View-details stays available, dismissing the notice restores the warning, validation-clear removes it); existing `ScheduledSessionCard`/`TimetableGrid`/`DragPreviewCard`/action-bar suites still pass. **Verification:** frontend **334 passing** (only the unrelated `units.test.tsx` modal timeout under full parallel load, which passes in isolation — 20/20); `tsc -p tsconfig.app.json` clean; production build green (`937.07 kB`/`273.33 kB` gzip; pre-existing large-chunk warning only). No new dependency; no protected `components/ui/*` change.
- **Unit 107 complete — timetable card sizing & drop positioning (2026-07-08).** Frontend-only unit (`frontend/` boundary; no schema, API, or solver change) fixing two timetable grid geometry bugs. Spec at `context/feature-specs/107-timetable-card-sizing-and-drop-positioning.md`. **(1) Drop slot under the pointer.** `DndContext` (in `routes/timetable.tsx`) now uses an explicit `collisionDetection` — new `features/timetable/dragCollision.ts` `pointerSlotCollision`, which runs dnd-kit `pointerWithin` first and falls back to `rectIntersection` only when there are no pointer coordinates (keyboard drag). Previously the default `rectIntersection` picked the droppable with the greatest **area** overlap with the drag overlay, so a tall multi-slot card anchored the target to the card body and dropped the session into a slot offset from the cursor. `pointerWithin` resolves both the hover highlight (`activeHoverKey`→`computeHoverHighlightKeys`) and the `handleDragEnd` target to the exact cell under the mouse. The width-centred, first-slot-aligned drag preview / pointer-align modifier (Unit 78) is unchanged — only the vertical slot resolution moved. Validation, allowed-cell rules, and persistence are untouched. **(2) +1px per extra slot for multi-slot cards.** Centralised the per-slot height math in the shared slot helper: new `SLOT_HEIGHT_REM` (3.5) and `slotSpanHeight(slotSpan)` in `features/timetable/slots.ts` return `calc(n * 3.5rem + (n−1)px)` (no `+0px` for a single slot). A card spanning `n` slots now covers its rows exactly, closing the (n−1)px gap left by the 1px per-row border. `ScheduledSessionCard.tsx` uses it for `duration`; `BlockCellCard.tsx` uses it for the **vertical** span only (`slotSpan > 1`), so multi-**room** blocks get no height change (their width correction is a separate existing rule) and single-slot cards are unchanged. Removed the two duplicated `CELL_HEIGHT_REM = 3.5` constants. **Tests (new):** `slots.test.ts` (1/2/3/4-slot height strings), `ScheduledSessionCard.test.tsx` (1-slot unchanged, 2-slot `+1px`, 3-slot `+2px`), `BlockCellCard.test.tsx` (2/3 slot-row `+1px`/`+2px`; multi-room-single-row and single-cell get no explicit height), `dragCollision.test.ts` (pointer wins over greater-area overlap; keyboard falls back to rect intersection). Existing `hoverHighlight.test.ts` / `DragPreviewCard.test.ts` still pass. **Verification:** frontend suite green (timetable + units re-run 82 passed together; full run 335 tests — one unrelated `units.test.tsx` modal timeout only under full parallel load, passes in isolation and on the pre-change baseline); production build green (`937.21 kB`/`273.30 kB` gzip; only the pre-existing large-chunk warning); `tsc` clean for touched files (only the pre-existing `baseUrl` deprecation note). No new dependency; no protected `components/ui/*` change.
- **Unit 105 complete — frontend lecturer/unit upload UI (2026-07-08).** Frontend-only unit (`frontend/` boundary) adding a CSV/Excel upload workflow that drives the Unit 104 endpoint (`POST /lecturers/import-csv`, `LecturerImportResult`). The same upload action appears on **both** `/units` and `/lecturers` and does exactly the same thing — sibling of the Unit 91 student upload. Spec at `context/feature-specs/105-frontend-lecturer-unit-upload-ui.md`. **API** (`frontend/src/lib/api/lecturers.ts`): added `LecturerImportResult` interface (mirrors backend counts: `created_lecturers`, `created_units`, `added_team_memberships`, `skipped_invalid_rows`, `deduped_rows`) and `uploadLecturerCsv(file)` posting multipart `FormData` to `/lecturers/import-csv` through `apiRequest` (which omits the JSON `Content-Type` for `FormData`), letting structured backend errors propagate. **Shared component** (`frontend/src/features/lecturers/LecturerUnitUpload.tsx`): one reusable control owning the trigger button, dialog, file input (`.csv`/`.xlsx`), upload mutation, and success/error summary — rendered identically in both page headers so behaviour stays consistent by construction. Dialog states expected columns (`TITLE, LAST NAME, FIRST NAME, AVAILABILITY, UNIT CODE, UNIT NAME`), notes `AVAILABILITY` is accepted but not imported yet, disables upload until a file is selected, shows a loading state, surfaces structured errors, shows an aggregate success summary (created lecturers/units, added team memberships; skipped-invalid and deduped only when nonzero), and force-remounts the file input after success. On success invalidates `['lecturers']` and `['units']`. **Wiring:** shared trigger added alongside the existing add-unit action on `/units` and the add-lecturer action on `/lecturers`, labelled `Upload lecturers & units`. **Tests:** new `frontend/src/features/lecturers/LecturerUnitUpload.test.tsx` mirroring the Unit 91 upload coverage (trigger opens dialog, disabled until file selected, sends multipart, success summary, conditional invalid/deduped counts, structured error, invalidates both keys), plus trigger-present assertions on both pages. **Verification:** frontend **318 passed across 27 files** (309 prior + 9 new); production build green (`935.88 kB`/`272.98 kB` gzip; only the pre-existing large-chunk warning); `tsc -b` clean. No new frontend dependency.
- **Unit 104 complete — backend lecturer/unit CSV/Excel import (2026-07-08).** Backend-only unit (`backend/` boundary; no frontend, solver, schema/migration, or new-package change) adding a protected bulk-import endpoint that, per row, ensures the named lecturer exists, ensures the named unit exists, and adds the lecturer to the unit's `unit_lecturers` teaching team — returning aggregate counts only. Sibling of the Unit 90 student import and follows the same boundary rules (in-memory parse, no blob storage, whole-file structural reject vs row-level skip-and-count, one atomic commit). Spec at `context/feature-specs/104-backend-lecturer-unit-csv-import.md`. **Route** (`backend/api/lecturers.py`): new `POST /lecturers/import-csv` behind `get_current_admin`; reads the upload and delegates to the service (filename+bytes), returning `LecturerImportResult`. **Schema** (`backend/schemas/lecturer.py`): `LecturerImportResult` (`created_lecturers`, `created_units`, `added_team_memberships` (excludes pre-existing links), `skipped_invalid_rows`, `deduped_rows`). **Service** (`backend/services/lecturer_import.py`): accepts `.csv` (utf-8-sig, BOM-tolerant) and `.xlsx` (read lazily via `openpyxl` `read_only`/`data_only`, cells coerced to strings). Structural rejects reuse the Unit 90 codes/messages — `import_missing_file`, `import_invalid_file_type` (extension not `.csv`/`.xlsx`), `import_empty_file`, `import_invalid_encoding` (bad UTF-8, or an unreadable `.xlsx`), `import_invalid_header` — the header must be exactly the six normalized columns `title, last name, first name, availability, unit code, unit name` (trim→lowercase→collapse-whitespace, no extras). Per row (`AVAILABILITY` value ignored): (1) **lecturer** matched by a normalized `(first_name, last_name)` key (case-insensitive, whitespace-collapsed) or created — `title` maps the CSV title to `LecturerTitle` via tolerant `_TITLE_VARIANTS` (periods stripped; e.g. `Prof.`→PROF, `associate professor`→ASSOC_PROF, `doctor`→DR) with unrecognized/blank falling back to the documented `DEFAULT_LECTURER_TITLE = LecturerTitle.MR`; title never skips a row and only affects newly created lecturers; (2) **unit** matched by normalized `code` (`AAA999`, trimmed/uppercased, year derived via `parse_unit_year_level`, invariant 24) or created with the row's `UNIT NAME`, derived year, and an **empty student list** (no sessions, so no `session_student_allocations` rebalance); (3) **team membership** added additively (no-op when the link already exists, tracked per unit so within-file additions also dedupe). Row-level skips (counted, non-blocking): blank first/last name, blank unit name, or a unit code failing the `AAA999`/derivable-year contract. `(lecturer, unit)` pairs are deduped within the file (first appearance authoritative for created title/name; later rows never retitle/rename). All creations + team links apply in one transaction committed atomically; unexpected persistence failure rolls back and raises `import_failed` (500). Additive/non-destructive throughout (never deletes or renames existing records). **Tests** (`tests/test_104_lecturer_unit_csv_import.py`, 51): structural contract (header case/spacing, missing file, wrong extension, bad encoding, empty/whitespace, missing/wrong/extra column, BOM); `.xlsx`↔`.csv` identical results + xlsx invalid-header + numeric-cell coercion; lecturer match-by-normalized-name (no retitle), new-lecturer first-row title, 11 title-variant mappings + blank/unknown→default + title-never-skips; unit match (name preserved) + new-unit derived-year/empty-students + invalid-code skip; team link additive / no-op-when-present / added-without-replacing-team / multi-lecturer-per-unit / multi-unit-per-lecturer; dedupe pairs, availability ignored, blank-field skips, first-row-authoritative, accurate aggregate counts; no allocations created; atomic rollback via a failing flush; and route-level `.csv`/`.xlsx` multipart upload + invalid-header 422 + auth-required 401. **Verification:** backend **571 passed** (520 prior + 51 new; run `APP_ENV=ci` + dummy `DATABASE_URL=sqlite://`/`SUPABASE_URL`, `CORS_ORIGINS` left unset); app-import smoke green (`TTS3 API`, Sentry `no_dsn`). No new runtime dependency (reuses `openpyxl` from Unit 93). **Deferred (per spec follow-ups):** frontend upload UI on `/lecturers` (Unit 105); availability import; and the descriptive-docs acceptance pass reconciling `project-overview.md`/`architecture-context.md`/`code-standards.md` for this importer (invariants 18/24/26 + boundary parity with 31) — those context files are intentionally left unchanged this unit.
- **Unit 102 complete — lecturer preferences regression, docs, and acceptance pass (2026-07-07).** Verification + documentation unit for the whole lecturer-preferences batch (Units 98–101), run **after** the Unit 103 tab/grid bug fixes so it certifies the current post-103 state. **No defects found; no new feature behaviour added.** **Regression evidence:** backend **520 passed** (`APP_ENV=ci` with dummy `DATABASE_URL=sqlite://`/`SUPABASE_URL` and unset `CORS_ORIGINS`, matching the Unit 101 dotenv workaround); frontend **309 passed across 26 files**; frontend production build green (`931.78 kB`/`272.28 kB` gzip; only the pre-existing large-chunk warning); backend app-import smoke green (`TTS3 API`, Sentry disabled `no_dsn`); Alembic **`0015 (head)`** single linear head (`alembic current` needs a live DB, not checked offline). Confirmed the batch introduced no fake sessions, no hard-constraint behaviour for preferences (never blocks placement or reduces feasibility/scheduled count), no dirty-draft/save step or browser-storage draft on `/preferences`, no cross-validation of clicks against availability/blocks/sessions, and no availability/block hard-constraint regressions. **Docs reconciled** (the deferred Unit 101 doc work): `project-overview.md` — goal #10 now records the lecturer-preference soft-constraint exception, `/preferences` added to the core-user-flow navbar list as the right-most tab, a new **Lecturer Scheduling Preferences** feature section, a Solver bullet describing the secondary preference tie-breaker, an In-Scope bullet, and the Out-of-Scope "soft constraints"/"preference optimization" lines rescoped so only lecturer preferences are in scope (gaps/mornings/similarity-to-last-save still out unless separately specced). `architecture-context.md` — `solver/` boundary now notes preference-cell consumption; **new invariant 34** documents the `PreferenceSnapshot`/`preferences` snapshot field, the no-cross-validation/no-integrity-rejection loading, and the lexicographic `primary_multiplier · Σ(scheduled) + Σ(score·var)` objective (preferences only break ties); invariant 33 updated to note the immediate-persist, frontend-owned editor. `code-standards.md` — the "do not introduce soft constraints" solver line rewritten to permit **only** lecturer preferences as a strictly-secondary lexicographic term, the `features/preferences/` file-org entry refreshed for the wired-up cells/legend/`preferenceColors`/shared `gridView` controls, and explicit backend `lecturer_preference.py` module locations added under `api/`/`models/`/`schemas/`/`services/` plus the `solver/` `PreferenceSnapshot` note. `ui-context.md` preference tokens already final (Unit 100) with rendering rules updated in Unit 103 — no change needed. **Manual checklist:** new `docs/lecturer-preferences-acceptance-flow.md` (15 steps + scope guard + recorded automated evidence), following the existing `docs/*-acceptance-flow.md` pattern. **Remaining/next:** manual checklist steps are pending a human run against a live workspace; no code follow-ups.
- **Unit 103 complete — Preferences tab and grid bug fixes (2026-07-07).** Frontend-only unit (`frontend/` boundary; no backend, solver, schema, or new-package change) turning the six post-Unit-98–102 Constraints/Preferences-tab bugs from `context/TODO.md` into fixes; spec at `context/feature-specs/103-frontend-preferences-tab-and-grid-bug-fixes.md`. **(1) Lecturer selector showed the id.** `LecturerSelector` now resolves the selected lecturer from the list and renders `lecturerDisplayName` explicitly as the `SelectValue` child, so the trigger shows the name instead of the raw UUID Radix was falling back to. **(2) Frontend owns the preference grid.** `routes/preferences.tsx` drops the `onSettled` `invalidateQueries` per-click refetch — the optimistic cache write is now the source of truth between lecturer loads (backend just stores each click; no flicker/revert/lag on rapid clicks). Load-on-select and `onError` rollback are retained; server state reconciles when a lecturer's preferences (re)load. **This supersedes the Unit 100 "invalidate on settle" step** (noted at the top of the Unit 100 spec). **(3) Rounder, text-free cells + (5) legend.** `PreferenceCell` renders a rounded (`rounded-md`), inset, token-filled chip with **no in-cell `Prefer`/`Avoid` text**; the level is conveyed by a new `PreferenceLegend` (green = Prefer, red = Avoid) plus each cell's `aria-label` (now `"{day} {time}, {room name}: {level}"` using human labels, not raw slot/room ids), so status never relies on colour alone. **(4) Shared grid view controls (both grids).** New `features/timetable/gridView.ts` (`useGridViewState`, `extendedGridMinWidth`, extended-col constants) + `GridViewControls.tsx` give `/timetable` and `/preferences` a per-day visibility filter (keeps ≥1 day; view-only, never mutates saved data) and an **extend** toggle that sets a `min-width` past the container and wraps the grid in `overflow-x-auto` (page never scrolls horizontally). Room sub-header text reduced to `text-[0.65rem]` in both grids. `TimetableGrid`/`PreferenceGrid` take `visibleDays`/`extended` props; the timetable grid's existing `ResizeObserver` re-measures cells on the width change so drag/drop metrics stay correct. **(6) Nav order.** `TopNav` moves `Preferences` to the far-right end of the (still-centered) nav list, after `Rooms`. **Tests:** updated `preferences.test.tsx` (cell has no text but keeps the `Avoid` aria-label + coloured fill; new Unit 103 suite — trigger shows the name not the id, a click stays set with no per-click refetch, legend renders Prefer/Avoid, day selector hides a column without any preference API call) and `TopNav.test.tsx` (Preferences is the last link). **Verification:** strict `tsc -b` green; production build green (`931.78 kB`/`272.28 kB` gzip; only the pre-existing large-chunk warning); frontend **309 passed across 26 files**. No backend/solver/schema/package changes. **Docs:** `ui-context.md` (preference rendering rules rewritten for text-free chips + legend + frontend-owned state; new "Shared grid view controls" section) and the Unit 100 spec (superseded-note) updated. **Remaining/next:** none for this bug-fix set; Unit 102 preferences regression/acceptance pass still stands separately.
- **Unit 101 complete — solver lecturer-preference soft constraint (2026-07-07).** Backend-only unit (`backend/solver/` boundary; no frontend, schema/migration, or new-package change) feeding the persisted Unit 98 lecturer preferences into the CP-SAT solver as the **first soft constraint**. **This supersedes the earlier "no soft constraints in the v1 solver model" state** (the Unit 42 objective was `Maximize(sum(scheduled_vars))`); per the spec, full reconciliation of the descriptive context docs (`project-overview.md`, `architecture-context.md`, `code-standards.md`) is deferred to the Unit 102 acceptance pass — the historical Unit 42/57 tracker entries below are left as-is. **Snapshot** (`solver/types.py`, `solver/snapshot.py`): new frozen `PreferenceSnapshot(lecturer_id, day, slot, room_id, level)` DTO and a `preferences: list[PreferenceSnapshot]` field on `SolverInputSnapshot` (defaults empty). `build_snapshot_from_data` takes an optional `preferences` arg and carries the cells through verbatim, deterministically sorted by `(lecturer_id, day, room_id, slot)` — **no integrity validation** (preferences carry no feasibility meaning, so unlike locked assignments/blocks they are never rejected). The DB loader `build_solver_input_snapshot` loads all `LecturerPreference` rows (`p.day.value`/`p.slot.value`/`p.level.value`) with no cross-validation against availability, blocks, or sessions. **Objective** (`solver/model.py`): candidate generation and every feasibility check (capacity, lunch, off-timetable, availability, locked occupancy, blocks) are **unchanged** — preferences never gate a candidate. For each candidate a net preference `score` is computed over its occupied cells via a `(lecturer_id, day, slot, room_id) → level` lookup: `+PREFERENCE_WEIGHT` per `preferred` cell, `-PREFERENCE_WEIGHT` per `avoid` cell, `0` otherwise (one fixed uniform weight `= 1`; no per-lecturer/per-cell config). The objective is now lexicographic: `primary_multiplier * sum(scheduled) + Σ(score·var)` where `primary_multiplier = 2·PREFERENCE_WEIGHT·Σ(unscheduled durations) + 1` is strictly larger than the full `2B` secondary swing, so **scheduling one more session always beats any preference score** — preferences only break ties among equally-maximal outcomes and can never reject a feasible placement or reduce the scheduled count. **Result application** (`solver/apply.py`) is untouched: preferences add no new rejection path. Determinism preserved (`num_search_workers=1`, `random_seed=0`, deterministic build order; multiplier is a deterministic function of durations). **Tests** (`tests/test_101_solver_preference_soft_constraint.py`, 11): snapshot carries + sorts preferences with no dropping; empty default; preferred cell chosen when count unaffected; avoid shunned when an alternative exists; preferred beats avoid in the same room; avoid does **not** reject the only feasible placement; avoid never trades a scheduled session for a better score (count dominates); preferred optimized without reducing count (conflict-sharing pair, one lands on the preferred cell); multi-slot session scores every occupied cell; no-preference count parity; determinism across two runs. **Verification:** backend **520 passed** (509 prior + 11 new; run with `APP_ENV=ci` + dummy `DATABASE_URL`/`SUPABASE_URL` to skip the empty-`CORS_ORIGINS` dotenv issue noted in Unit 97). No frontend/schema/migration/package changes. **Remaining/next:** Unit 102 — reconcile the "no soft constraints" wording across the descriptive context docs and run the preferences regression/acceptance pass.

- **Unit 100 complete — frontend lecturer preference API integration (2026-07-07).** Frontend-only unit (`frontend/` boundary; no backend, solver, schema, or new-package change) wiring the Unit 99 `/preferences` grid to the Unit 98 API. **Immediate per-click persistence with optimistic cache updates — deliberately NOT a dirty-draft/explicit-save model** (confirmed with the user against the timetable draft pattern): TanStack Query's per-lecturer cache is the source of truth, each click fires one API call, and the grid updates optimistically then reconciles on invalidate. **API client** (`lib/api/lecturerPreferences.ts`): `LecturerPreferenceLevel = 'preferred' | 'avoid'`, the `LecturerPreference` cell DTO + `LecturerPreferenceCell`/`LecturerPreferenceUpsert` input types, and `listLecturerPreferences(lecturerId)` (GET `/lecturers/{id}/preferences`), `upsertLecturerPreference(cell)` (PUT `/lecturer-preferences`), `deleteLecturerPreference(cell)` (DELETE `/lecturer-preferences` with a JSON body — `apiRequest` passes body through generically) over the existing authenticated client, plus `parsePreferenceError` that keeps the backend's structured `{error:{message}}` text (already surfaced on `ApiRequestError.message`) and only falls back to a canned per-status string (404/422/409) for generic failures. **Tokens:** six new dedicated preference tokens in `index.css` and `ui-context.md` (`--preference-preferred-{bg,border,text}` green #E4F1E9/#2E7D4F/#1E5636; `--preference-avoid-{bg,border,text}` red #FBE3E1/#B4232A/#7A1B1F), separate from subject and block tokens; `features/preferences/preferenceColors.ts` `getPreferenceTokens(level)` maps a level to `{background,border,text,label}`. **Cell + grid:** `PreferenceCell` now renders a level (token fill + border + a short `Prefer`/`Avoid` text label so the level never relies on colour alone), is a keyboard-accessible `role="button"` (Enter/Space), carries `data-level` for tests, and is disabled until a lecturer is selected; `PreferenceGrid` takes `preferenceLevels` (a `day:roomId:slotId → level` map via the exported `preferenceCellKey`), an `onCellClick`, and `interactionDisabled`, threading them to both AM and PM cells. **Route** (`routes/preferences.tsx`): loads `['lecturer-preferences', lecturerId]` (enabled only once a lecturer is selected, so switching lecturers swaps the cache set with no cross-lecturer bleed), builds the level map, and a single `useMutation` handles the click cycle — neutral→`upsert(preferred)`, preferred→`upsert(avoid)`, avoid→`delete` — with `onMutate` optimistically patching the cached cell, `onError` rolling back + showing the message, and `onSettled` invalidating `['lecturer-preferences', lecturerId]`. The frontend does **no** validation of clicks against availability/blocks/sessions (matching the backend's unvalidated persistence). A concise inline error renders near the grid for a failed preference load or a failed click (`role="alert"`), and a hint prompts selecting a lecturer. **Tests:** new `lib/api/lecturerPreferences.test.ts` (5 — GET path, PUT body incl. level, DELETE body, structured-error message surfaced, `ApiRequestError` type) and expanded `routes/preferences.test.tsx` (Unit 100 suite of 6 over a small in-test fake preference store so the invalidate-on-settle refetch reflects persisted state — renders a selected lecturer's saved cells, cycles neutral→preferred→avoid→neutral persisting each click with the exact upsert/delete args, no persistence before a lecturer is selected, lecturer-switch with no bleed, and a load-error alert near the grid; plus the retained Unit 99 shell tests, updated so "preferences don't load until a lecturer is selected"). **Verification:** strict `tsc -b` green; production build green (`928.90 kB`/`271.50 kB` gzip; only the pre-existing large-chunk warning); frontend **304 passed across 26 files** (`--no-file-parallelism`). No backend/solver/schema/package changes. **Docs:** `ui-context.md` (new Lecturer Preference Colors token table + rendering rules) and `code-standards.md` (already lists `features/preferences/`) updated; `index.css` tokens added. **Remaining/next:** solver soft-constraint integration for the persisted preferences is a later, separate unit.

- **Unit 99 complete — frontend preferences page shell (2026-07-07).** Frontend-only unit (`frontend/` boundary; no backend, solver, schema, or new-package change) adding a new `/preferences` tab and page shell — a timetable-shaped grid plus a lecturer selector. **Layout and local UI state only:** no preference data loads or saves, and **no calls to the Unit 98 endpoints** yet (no preference API module is wired in this unit). **Route + navbar:** `routes/preferences.tsx` (default-exported `PreferencesPage`) registered in `App.tsx` behind the existing `ProtectedRoute` pattern at `/preferences`; `TopNav` gains a `Preferences` link placed right after `Timetable` (the two timetable-shaped pages sit together). **Grid shell** (`features/preferences/`): `PreferenceGrid.tsx` is a sessions-free variant of `TimetableGrid` — it reuses the timetable `slots.ts` `DAYS`/`AM_SLOTS`/`PM_SLOTS`/`LUNCH_LABEL` and imports the `RoomColumn` type, rendering the identical day-header / room-subheader / AM rows / `Lunch/Mass` divider / PM rows structure but with neutral `PreferenceCell.tsx` cells (mirroring the empty `GridCell` look — same `h-14`, borders, `--bg-surface`) and **no** session/unscheduled cards, drag/drop, blocks, validation, metrics, or sticky action bar. Like `TimetableGrid` it returns `null` when `rooms` is empty. **Lecturer selector** (`LecturerSelector.tsx`): a single-select shadcn `Select` over the existing `['lecturers']` query (`listLecturers`), rendered above the grid, with `lecturerDisplayName` (`${title} ${first} ${last}`), a `Select a lecturer` placeholder (`No lecturers available` + disabled when empty); the selected lecturer id is tracked in page-local `useState` and wired to nothing else this unit. **Empty state:** the page loads `['rooms']` (`listRooms`); when no rooms exist it renders the **same** empty-state pattern as `/timetable` (the `CalendarDays` block with a `Create rooms` link to `/rooms`, message adapted to "The preferences grid requires at least one room."), plus loading/error variants matching the timetable route; `components/ui/*` primitives untouched. **Tests:** updated `TopNav.test.tsx` to expect the `Preferences` link; new `routes/preferences.test.tsx` (4 — grid renders the full day/room/slot structure with only neutral `data-preference-cell` cells and no sessions; the shared empty-state shows with the `/rooms` link and no grid when no rooms; the lecturer selector lists lecturers and tracks a single selection via the base-ui `Select`; and only the rooms + lecturers list queries run, asserting no Unit 98 endpoints are hit). **Verification:** strict `tsc -b` green; production build green (`925.25 kB`/`270.51 kB` gzip; only the pre-existing large-chunk warning); frontend **296 passed across 25 files** (run with `--no-file-parallelism`). No backend/solver/schema/package changes. **Docs:** `project-overview.md` (navbar list) and `code-standards.md` (File Organization: `/preferences` route + new `features/preferences/` folder) updated. **Remaining/next:** wiring the selected lecturer to load persisted Unit 98 preferences and to a click-to-set-preference cell interaction (upsert/delete against the Unit 98 API) are separate, later units.

- **Unit 98 complete — backend lecturer preference persistence and API (2026-07-07).** Backend-only unit (`backend/` boundary; no frontend, solver, or new-package change) adding persisted room-specific lecturer scheduling **preferences** as a soft-constraint data/API contract. A preference cell is `lecturer_id + day + slot + room_id` (mirroring the room-specific shape of timetable blocks) with exactly one `level` — `preferred` or `avoid`; no row means neutral, and neutral is never stored. Preferences are distinct from availability (hard) and timetable blocks (hard), are not sessions, and this unit does **no** cross-validation against availability, blocks, or sessions — the backend trusts the frontend and persists submitted cells as-is. Solver integration is deferred to a later unit. **Model** (`models/lecturer_preference.py`): new `PreferenceLevel` enum (`preferred`/`avoid`) and `LecturerPreference` (`id`, `lecturer_id` FK cascade, `day`, `slot`, `room_id` FK cascade, `level`, timestamps) with a unique `(lecturer_id, day, slot, room_id)` and indexes on `lecturer_id` and `(day, slot, room_id)`; reuses the `AvailabilityDay`/`AvailabilitySlot` python enums bound to new PG enum types `prefday`/`prefslot` (mirroring the block model's `blockday`/`blockslot` pattern). Registered in `models/__init__.py`. **Migration** `0015_lecturer_preferences.py` (head now **`0015`**, revises `0014`): creates the `preferencelevel`/`prefday`/`prefslot` enums and the `lecturer_preferences` table with the unique constraint + two indexes; rows cascade when their lecturer or room is deleted. **Schemas** (`schemas/lecturer_preference.py`): `LecturerPreferenceUpsert` (`lecturer_id, day, slot, room_id, level`; `level` accepted as a raw `str` so an unknown value surfaces as a *structured* error rather than a generic 422), `LecturerPreferenceDelete` (the four cell keys), and `LecturerPreferenceResponse` (from-attributes; id, cell keys, level, timestamps). **Service** (`services/lecturer_preference.py`): `list_lecturer_preferences` (validates the lecturer exists, returns all that lecturer's cells ordered by `created_at`), `upsert_lecturer_preference` (validate lecturer + room exist and level ∈ {preferred, avoid}, then create the row if absent or overwrite `level` if present — keyed by the four-tuple), and `delete_lecturer_preference` (validate lecturer + room, remove the row entirely returning the cell to neutral; deleting an already-neutral cell is a no-op). Structured errors: `lecturer_not_found` (422), `room_not_found` (422), `invalid_preference_level` (422), `preference_cell_conflict` (409 defensive). **Routes** (`api/lecturer_preferences.py`, registered in `api/router.py`): thin handlers behind `get_current_admin` — `GET /lecturers/{lecturer_id}/preferences` (all cells for one lecturer), `PUT /lecturer-preferences` (upsert a single cell, returns the persisted cell), `DELETE /lecturer-preferences` (remove a single cell to neutral, `204`). **Tests** (`tests/test_98_lecturer_preferences.py`, 21): persistence + room-specificity, `preferred`/`avoid` stored, neutral never stored, upsert overwrite keeps one row, distinct cells coexist, unknown-lecturer/unknown-room/invalid-level rejection (with nothing persisted on invalid level), list scoping + unknown-lecturer rejection + empty list, delete-to-neutral + no-op delete of a neutral cell + target-only delete + unknown-lecturer/room rejection, lecturer/room cascade delete, and no-cross-validation persistence. **Verification:** backend **509 passed** (488 prior + 21 new); app import green (`TTS3 API`); Alembic head **`0015 (head)`** (single linear head); the three routes confirmed registered at the exact spec paths/methods. No frontend/solver/package changes. **Remaining/next:** solver soft-constraint integration for preferences and any frontend preference-editing UI are separate, later units.

- **Unit 97 complete — Excel export template fidelity fix (2026-07-07).** Backend-only unit (`backend/` boundary; no frontend/solver/schema/migration/package change) that makes the generated `.xlsx` visually match the real Campion template by rebuilding the export template from the correct source sheet and stopping the builder flattening the grid. **`build_template.py` rewrite:** now builds from the real pristine source `backend/assets/excel/Timetable S2 2025 DRAFT.xlsx` (no trailing space), selecting the canonical sheet **by name** `S2, 2025 Timetable ` (trailing space — not `worksheets[0]`) and dropping the other two sheets. The blank template is a verbatim blanked copy: the sparse column model is preserved exactly (only `A, G, O, R, S, T, W, Y, AE, AP` carry explicit widths; every other room column left default/unset — no global widths, no `bestFit`, no autofit), plus all row heights (row 4 = 24.6, row 5 = 33.0, slot bands ~45.95, `defaultRowHeight` 12.95), sheet-format props, page setup (landscape, `scale 35`, `fitToPage`), margins, green tab `FF00B050`, and sheet view zoom 90. **The four defects fixed:** (1) row/column sizes now match (verbatim from the source, openpyxl round-trips the sparse col model cleanly); (2) per-day empty fills fixed — the grid is entirely 2-row merged bands where only the anchor carries fill/border; (3) borders made uniform and dark; (4) per-category text position via the baked styles — time labels/headers `center`/`center` (static, preserved by the blank copy), class cells `center`/`top`/wrap, and **block cells now carry the template's event alignment** (`FORMAL HALL` `Z22` `center`/`center`) rather than the class alignment. **Named styles** (`tt_class_*`, `tt_block_*`) re-derived from the new sheet's own exemplars — subject fills are theme-based (HIS `theme9/0.6` `C6`, THE `theme5/0.6` `B6`, LIT `theme2/-0.25` `O8`, PHI `theme4/0.6` `G16`, GRE `theme6/0.6` `AK6`, LAN `theme9/0.8` `M6`), default class white `K6`, events gold `Z22`/blue `R10`/maroon `B12`, unnamed = grey empty look. **`services/timetable_excel_export.py` unchanged** — it already only applies the baked NamedStyles, preserves static content, never resets heights/widths, and paints only within its rectangles (verified). **Tests:** rewrote `test_96_excel_export_style_parity.py` to load the new source/sheet and assert page setup/tab colour/zoom/default-row-height, per-day white-vs-grey empty fills, bold-dark borders (not `c6c6c6`), verbatim empty-cell parity, theme-based class fills, and event-derived block alignment; updated the two Unit 93 block-fill assertions (gold `FFCC9900`; unnamed grey `theme0/-0.15`). **Verification:** regenerated `campion_timetable_export_template.xlsx`; backend **496 passed**; confirmed output parity vs the source (column model, row heights, tab/zoom/page setup, per-day fills, 280 grid merges, no leftover grid values, static merges equal). **Follow-up refinement (per user review of the output):** rather than preserving the template's inconsistent per-cell borders verbatim, the grid is now blanked to a **uniform** look — **every** room×slot band gets its day-block fill (Tue/Thu white, Mon/Wed/Fri grey) plus a **uniform medium box border on all four sides**, so every box reads the same. This also fixed a white gap at `T16–17`: the source's odd `T15:T16` event merge is offset from the 2-row band grid, so the earlier "only reset formerly-populated bands" logic left those cells unfilled; blanking every band uniformly resets them to Wednesday grey. `build_template.py` gained `_uniform_border` (medium box, side copied from the `C6` exemplar) and a simpler `_blank_grid` (fills chosen by day block from the `D6` grey / `K6` white exemplars); the default-class and unnamed-block styles carry the same medium box. Docs updated (`architecture-context.md` invariant 32 + repo-owned-export-template bullet; `code-standards.md` export rule). Manual "open both workbooks in Excel side-by-side" acceptance remains for a human run. (Note: `.env.development` has an empty `CORS_ORIGINS=` that breaks `Settings()` for local runs — a pre-existing env-consolidation issue, unrelated to this unit; tests were run with `APP_ENV=ci` + dummy `DATABASE_URL`/`SUPABASE_URL` so the dotenv source is skipped.)

- **Unit 97 spec authored — Excel export template fidelity fix (2026-07-07).** Spec only; no implementation yet. Wrote `context/feature-specs/97-excel-export-template-fidelity-fix.md` to fix the four export defects the user reported (row/column sizes, Tuesday/Thursday white contrast, bolder/darker/continuous borders, and per-category text position). **Key discovery:** the Units 93/96 export template + `build_template.py` were built from a **stripped `wijmo.xlsx` re-export** that had lost the real template's page setup, tab colour, zoom, fine row heights, default column model, per-day background shading, and bold theme-coloured borders — which is why Unit 96 concluded the "pristine template" was "not present in the repo." The user has now placed the **real pristine template** at `backend/assets/excel/Timetable S2 2025 DRAFT.xlsx` (no trailing space; three sheets — the canonical one is `S2, 2025 Timetable ` **with a trailing space**; landscape/scale-35/fitToPage, zoom 90, green tab `FF00B050`, defaultRowHeight 12.95, day-header row 4 = 24.6, room-header row 5 = 33.0, slot bands ~45.95). Verified the **layout mapping is already correct** (day cols B/J/R/Z/AH, room order PDS/L1.05/Bromley/L1.08/Dawson/L1.10/L1.12/JTW, slot top rows 6/8/10/14/16/18/20, Mass/Lunch rows 12–13, K29 version, A28/A29–33/D29–33 key, Z22 FORMAL HALL) — so the fix is to rebuild the blank template from the correct sheet and stop the builder flattening the grid to one grey style. Confirmed the template's per-day empty fills are theme-based (Mon/Wed/Fri `theme0/tint-0.15` grey; **Tue J–Q / Thu Z–AG `theme0/tint0.0` white**) and its borders are medium (bold) top+bottom on every row with dark theme-coloured interior verticals (current export uses thin `c6c6c6` grey), and time labels are `center`/`center`/wrap (current export uses vertical `top`). Unit 97 is backend-only (template asset, `build_template.py`, minor `timetable_excel_export.py` checks, parity tests in `test_96_excel_export_style_parity.py`); no frontend/solver/schema/package changes. Docs to update on landing: `architecture-context.md` invariant 32 and `code-standards.md` export rule (new source filename/sheet, preserved page-setup/tab-colour/row/column model, Tue/Thu white contrast, template-copied bold borders).

- **Environment configuration consolidation (2026-07-07).** Tooling/config change only; no product-code, schema, or behavioural change to any flow. **Problem:** the two stacks (dev = `rariuszlvewstzaqojbm` Supabase + `proj_bmtqxqdddzxouqfewdiy` Trigger; prod = `icfeyjdpmitozkrutjhq` Supabase + `proj_kwgyhghrnimqdhmwqjrr` Trigger) were switched by hand-editing/commenting values inside single `.env` files and inside `trigger.config.ts`, which was error-prone when running alembic and Trigger.dev. **Fix — a single `APP_ENV` selector (`development` | `production`, default `development`) plus per-environment env files.** Each boundary now has `.env.development` + `.env.production` (both gitignored; `.gitignore` Environment section broadened to `.env` + `.env.*` with `!.env.example`; only `.env.example` templates are committed). `.env.development` files were migrated **verbatim** from the previous active `.env` values so the working dev stack is unchanged; `.env.production` files are seeded templates from the old commented `# PROD:`/`# ENV:` alternates with `TODO_*` placeholders for the rest. **Wiring:** `backend/config.py` and `backend/alembic/env.py` load `.env.{APP_ENV}` (so `APP_ENV=production alembic upgrade head` targets prod — no file editing); `jobs/trigger.config.ts` selects the project ref from a `PROJECT_REFS` map by `APP_ENV` (overridable via `TRIGGER_PROJECT_REF`) instead of a hardcoded ref; `jobs/package.json` scripts set `APP_ENV` and `--env-file` via a new `cross-env` devDependency (`dev`/`deploy:dev` → development, `dev:prod`/`deploy` → production). The three `.env.example` files document the copy-to-`.env.{env}` scheme. **Verified:** backend `Settings` loads the dev DB by default and the prod DB under `APP_ENV=production`; jobs `tsc` typecheck green and prod ref resolves to `proj_kwgyhghrnimqdhmwqjrr`; all six new env files confirmed git-ignored and no secret file is tracked. **Open follow-ups for the user:** the migrated Trigger secret keys are inconsistent (backend/.env.development used a `tr_prod_`-prefixed key while jobs used a different one, and a `tr_dev_` key was unused) — confirm which key each environment's Trigger project expects; fill every `TODO_*` in the `.env.production` files before deploying; the stale plain `.env` files can be deleted once the split is confirmed working.

- **Full-codebase review and cleanup pass (2026-07-07).** Whole-repo maintenance sweep (backend, frontend, jobs) fixing bugs, dead code, and stale comments; no new features, no schema/migration changes, no behavioural change to any product flow. **Bug fixes:** (1) `services/timetable_excel_export.py` `_paint` static-content guard only checked a rectangle's *endpoints* against the static `Mass/Lunch` rows (12–13), so a malformed lunch-crossing placement spanning rows 10–15 would have painted over static template content instead of failing with `export_overlaps_static_content`; the guard now rejects any rectangle *intersecting* rows 12–13 (`top <= 13 and bottom >= 12`) — no legitimate placement is affected (AM bands end at row 11, PM bands start at row 14). (2) `main.py` middleware order contradicted its own comment: `RequestContextMiddleware` claimed to be "added last so it runs outermost" but was added *first* (Starlette wraps last-added outermost), leaving CORS outside the request-id scope; it is now genuinely added last/outermost so every response (including CORS-handled ones) carries the correlation id. (3) `tests/conftest.py` now blanks `SENTRY_DSN` before the app is imported — the suite was sending real events to Sentry when a DSN was present in `backend/.env`. **Dead code removed:** frontend `SolverStatusPanel.tsx` + its test (the solver status UI was merged into `TimetableActionBar`; nothing rendered the panel), the no-op `except Exception: raise` in `auth/deps.py`, the unused `detailsRef` in `TimetableActionBar.tsx`, unused imports (`field` in `constraints/types.py`, `ORDERED_SLOTS as CONSTRAINT_SLOTS` in `solver/snapshot.py`), the retired `lab`/`workshop` entries in `SESSION_TYPE_ORDER` (session types reduced to lecture/tutorial in Unit 60; callers already fall back via `.get(..., 99)`), and an unused variable in `_block_rectangles`. **Consistency fixes:** `models/room.py` and `models/unit.py` timestamp columns re-annotated `Mapped[datetime]` (were `Mapped[str]`, inconsistent with every other model); `services/session_allocation.py` no longer rebuilds `set(lecture_ids)`/`set(tutorial_ids)` per iteration inside the reconcile comprehensions. **Stale comments corrected:** `solver/apply.py` ("intentionally not wired to any request handler or job yet" — it has been invoked by `solver/job.py` since Unit 45), `services/timetable_block.py` ("frontend validation and solver integration arrive in later units" — Units 86/87 shipped), `api/units.py` (frontend query-invalidation planning note removed), `schemas/session.py` + `lib/api/sessions.ts` ("later units" phrasing; `unit_year_level` is *not* surfaced by the backend — consumers derive it from the unit code), `GridCell.tsx` ("placement validation against blocks is a later unit" — it lives in `lib/validation/blocking`), and `BlockEditDialog.tsx` user-facing copy still describing the removed lock icon. **Verification:** backend **475 passed** (Sentry now silent during the run); frontend strict `tsc -b` green; frontend suite green with `--no-file-parallelism`.

- **Unit 96 complete — timetable Excel export styling parity fix.** Backend-only unit (`backend/` boundary; no frontend, solver, schema/migration, or new-package change) that makes the generated `.xlsx` template-faithful instead of placement-only-correct. **Root cause found by inspecting the actual files:** the old `build_template.py` re-normalised the grid with hardcoded styles and the export service *constructed* new style objects (frontend-token subject fills like HIS `F7E5D4`, subject-coloured text, center-vertical alignment, reconstructed borders) rather than copying the template's own styles. **Canonical template clarification (decided with the user — "Preserve DRAFT exactly"):** the spec's "observed differences" describe a pristine template (landscape scale 35, zoom 90, `defaultRowHeight` 13, ~46pt class rows, tab colour) that is **not present in the repo** — both provided files (`backend/assets/excel/Timetable S2 2025 DRAFT .xlsx` = the real human source sheet, Version 4; `context/assets/campion-timetable-2026-06-21.xlsx` = the current app output) are `wijmo.xlsx` re-exports whose row heights (row 1 = 18.75, morning class rows = 25.125) and missing page-setup/zoom/tab-colour match each other, not the spec's example numbers. Per the spec's own instruction to "inspect and compare the files directly rather than relying only on this list", the repo's **DRAFT source is treated as canonical** and preserved verbatim; the pristine-template values were deliberately **not** synthesised. **`build_template.py` rewrite:** the blank template is now a *blanked copy of the real source sheet* — it preserves the source's column widths, row heights, sheet-format properties, sheet view, page margins, tab colour, and every static merged range/cell value/style untouched; clears only the dynamic s1–s7 grid bands, normalising them to the template's own grey empty-cell look (fill/font/alignment + medium-black band-top / thin-grey interior border `Side`s **copied from a real template empty cell `D6`**, fixing the old thin-top border) and re-merging each room×slot into its 2-row band; and bakes **template-derived `NamedStyle`s copied verbatim from the source's own exemplar cells** — `tt_class_{HIS,THE,LIT,PHI,GRE,LAN}` (from cells C6/B6/O8/G16/AK6/M6 → real subject fills `fcd5b5`/`e6b9b8`/`c4bd97`/`b9cde5`/`d7e4bd`/`fdeada`, Trebuchet 10 bold **black** text, center/top/wrap, medium-box border), `tt_class_default` (neutral white `K6` exemplar for unknown prefixes incl. SCI), and `tt_block_{gold,blue,pink,unnamed}` (gold `cc9900`+maroon `660033` from FORMAL HALL `Z22`, blue `0070c0`+black from Augustine `R10`, maroon `660033`+gold from Mass/Lunch `B12`, grey empty for unnamed). openpyxl persists unused named styles, so they ship inside the single self-contained template asset (no runtime dependency on the source workbook). **`services/timetable_excel_export.py` rewrite:** removed the hardcoded `SUBJECT_FILLS`/`DEFAULT_CLASS_FILL`/`BLOCK_FILLS`/`Font`/`Border`/`Side` construction; `_paint` now *applies* a baked `NamedStyle` to each cell of the rectangle then merges + writes the value; class style is chosen by unit-code prefix via `_class_style_name` (`SUBJECT_PREFIXES` → `tt_class_*`, else `tt_class_default`); blocks use `BLOCK_STYLE[colour]`; `_write_lecturer_key` no longer overrides the key font/alignment (written entries inherit the preserved template key-area style; overflow rows beyond the 5 styled rows copy row-29's style). Placement logic, mapping constants, tutorial lettering, block rectangle merging, and the no-persistence guarantee are unchanged. **Tests:** updated the two Unit 93 block-fill assertions to the template-derived values (gold `cc9900`, unnamed grey `d9d9d9`); added `tests/test_96_excel_export_style_parity.py` (35) — structural parity vs the DRAFT source (one sheet w/ template name, dimension `A1:AP40`, all 42 column widths + customWidth/bestFit verbatim, no-autofit width check, rows 1–40 heights, sheet-format props, page margins, static (non-grid) merged ranges), static cell style+value parity (day/room headers, time labels, `Mass/Lunch`, `FORMAL HALL`, evening label, notes, key heading, title style-preserved/value-changed, version style-preserved/value-changed, key-body inherits template style), dynamic class parity (parametrized per-subject full-style equality vs the template exemplar, History uses template `fcd5b5`+black not the old `F7E5D4` token, unknown subject → white default, multi-slot one merged block with the subject fill), block parity (named gold copies `Z22` event style, unnamed → grey), and placement stability. **Verification:** backend **475 passed** (440 prior + 35 new; the 2 updated block tests included); regenerated `campion_timetable_export_template.xlsx`; app-import smoke green (`TTS3 API`); generated a populated sample export and confirmed cell-by-cell that statics match the template and dynamic classes/blocks carry the template-derived colours. No frontend/solver/schema/package changes. **Docs:** `architecture-context.md` (invariant 32) and `code-standards.md` (storage-model export rule) rewritten to state the styling is template-derived/baked-`NamedStyle` and never reconstructed, autofit is forbidden, and the DRAFT source is the canonical visual contract. **Remaining/next:** if the pristine template (with the spec's landscape/zoom/13pt/46pt-row properties) is later supplied, re-run `build_template.py` against it to pick those up; the manual "open in Excel and eyeball vs template" acceptance step remains for a human run.

- **Unit 95 complete — timetable Excel export regression, docs, and acceptance pass (Units 93–95).** Verification and documentation only; **no new product features and no production code changes were required** — the Units 93–94 export batch verified clean. **Automated verification (2026-06-21):** backend **440 passed** (Unit 93 `tests/test_93_timetable_excel_export.py`, 26 export tests, included); frontend **300 passed** across 25 files (Unit 94 `lib/api/timetableExport.test.ts` + the `routes/timetable.test.tsx` "Unit 94" suite included; run with `--no-file-parallelism` for a deterministic pass, ~280 s); frontend **production build green** (strict `tsc -b` + Vite; only the pre-existing large-chunk warning, `919.69 kB`/`269.63 kB` gzip); backend app-import smoke green (`TTS3 API`). **Backend regression** confirms the spec coverage by reading `services/timetable_excel_export.py` + `api/timetable_export.py`: the `GET /timetable/export.xlsx?title=...` route is protected behind `get_current_admin`, validates a non-empty trimmed title (`invalid_export_title` 422), streams a valid single-sheet `.xlsx` from the fixed repo-owned template, preserves static template content (day/room headers, time labels, notes, the static `Mass/Lunch` row, evening rows + `FORMAL HALL`, key area), fills one-slot sessions into both rows of their band and spans multi-slot sessions vertically, paints classes with the export-scoped subject fills + neutral fallback, exports named blocks with their name/colour and unnamed blocks as blank grey cells, merges contiguous same-group block cells into rectangles (split at the lunch gap), fails clearly when a saved assignment or block uses a room outside the fixed order (`export_room_not_in_template` 422), assigns deterministic per-unit tutorial letters in timetable order, builds lecturer initials + the balanced lecturer/tutor key from exported sessions, and persists nothing (returns a `BytesIO`, no DB/object-storage write). **Frontend regression** confirms the Unit 94 UI: `Download Timetable` in the sticky `TimetableActionBar`, disabled with a human-readable reason while the draft is dirty / saving / solver running / rooms-or-saved-assignments-or-blocks loading or failed / an export already running, and deliberately enabled for partial saved timetables, unscheduled sessions, and warning-invalid saved assignments; the dialog requires a title; success streams the backend blob and triggers a download (backend `Content-Disposition` filename or dated fallback) then closes with the `Timetable downloaded.` notice; export errors stay visible in the dialog and never alter timetable state; the browser never generates the workbook (no spreadsheet library bundled). **Scope guard passes** — the batch introduced no browser-side Excel generation, generated-file storage, export/version history, custom template upload, dynamic room-column generation, all-rooms blocks, student/lecturer-facing views, timetable import, CSV-import changes, or solver-behavior changes. **Docs:** `code-standards.md` gained an explicit React-section rule that the download UI must never generate the workbook in the browser (streams the Unit 93 blob via `apiRequestBlob`/`triggerBlobDownload`, no blob storage / export history, saved-state-only disabled gating); `project-overview.md` (scope lines 272–273) and `architecture-context.md` (invariant 32 + storage-model notes) were already aligned from Units 93–94, so no further edits were required, and `ui-context.md` needed none (no new action-bar visual rules). Created `docs/timetable-excel-export-acceptance-flow.md` (24-step manual PASS/FAIL checklist + automated evidence + scope-guard record); the interactive browser checklist remains intentionally unchecked for a human run. **Remaining manual follow-ups:** run the browser checklist against a full stack (incl. opening the exported workbook in Excel and verifying a non-template-room export error), ensure `openpyxl` is installed in the backend deployment image (already in `backend/requirements.txt`), and the frontend large-chunk code-splitting warning remains an open optimization when prioritized. No further timetable-export units are queued.

- **Unit 94 complete — frontend timetable Excel download UI.** Frontend-only unit (`frontend/` boundary; no backend, solver, schema/migration, or new package change) adding a `Download Timetable` flow that exports the current **saved** timetable as an `.xlsx` by streaming the Unit 93 backend blob — the browser never generates the workbook, no blob storage or export history is added, and scheduling/saving/validation/solver behavior is untouched. **Binary API client** (`lib/api/client.ts`): added `apiRequestBlob(path, options)` — a small authenticated binary GET helper that reuses the same Supabase bearer auth and structured-error parsing as `apiRequest` (a failed request still returns a JSON envelope, parsed for a readable message) but returns `{ blob, filename }`, with a private `parseContentDispositionFilename` handling both the RFC 5987 `filename*=` and plain `filename="…"` forms; this avoids overloading the JSON-focused `apiRequest` with binary parsing. **Export module** (`lib/api/timetableExport.ts`): `exportSavedTimetableExcel({ title })` GETs `/timetable/export.xlsx?title=…` (title sent verbatim as a query param; the caller trims first) returning the `.xlsx` blob + `Content-Disposition` filename; `fallbackExportFilename(today?)` produces `campion-timetable-YYYY-MM-DD.xlsx` from the local date; `triggerBlobDownload(blob, filename)` creates an object URL, clicks a synthetic anchor, and revokes the URL after triggering the download. **Dialog** (`features/timetable/TimetableDownloadDialog.tsx`): a required title input defaulting to `Campion Timetable` with helper text "This title appears inside the downloaded Excel file.", `Download` (disabled while the trimmed title is blank or an export is running) + `Cancel` actions, an in-dialog error region, and a `Downloading…` spinner. **Action bar** (`TimetableActionBar.tsx`): new outline `Download Timetable` button placed between `Clear all` and `Generate Timetable` (shows `Downloading…` while exporting, carries the disabled-reason as its `title`), plus a dismissible one-time `Timetable downloaded.` success notice (success-coloured, priority 2.5 alongside the block/draft notices). **Route wiring** (`routes/timetable.tsx`): a `useMutation` over `exportSavedTimetableExcel` that on success triggers the download (preferring the backend filename, else the dated fallback), closes the dialog, and surfaces the success notice; on error keeps the dialog open and shows the message via `getErrorMessage`. `downloadDisabledReason`/`canDownload` block the button while the draft is dirty (`Save timetable changes before downloading.`), a save is in flight, a solver run is active, rooms/saved-assignments/blocks failed to load, saved assignments are still loading, or an export is already running — and deliberately stay enabled for partial saved timetables, unscheduled sessions, and warning-invalid saved assignments. **Tests:** new `lib/api/timetableExport.test.ts` (5 — GET with the `title` query param returning blob + parsed filename, null filename when no `Content-Disposition`, structured-error message surfaced as an `ApiRequestError`, dated fallback-filename formatting, and the object-URL/anchor-click/revoke download trigger) and a `routes/timetable.test.tsx` "Unit 94" suite (12 — button appears in the sticky bar; disabled while dirty with the exact reason; disabled while saving; disabled while the solver runs; disabled when saved assignments fail to load with reason; opens the title dialog with the default + helper text; blank title disables the dialog Download; export sends the trimmed title and triggers a blob download then closes with the success notice; dated fallback filename when `Content-Disposition` is absent; backend error keeps the dialog open and downloads nothing; unscheduled sessions keep export enabled; warning-invalid saved assignments keep export enabled), mocking `@/lib/api/timetableExport`. **Verification:** strict `tsc -b` green; frontend **300 passed** across 25 files (283 prior + 17 new; run with `--no-file-parallelism` for a deterministic pass — the full parallel run intermittently times out unrelated `units`/`lecturers`/`students` modal tests under load, which pass in isolation); production build green (`919.69 kB`/`269.63 kB` gzip; only the pre-existing large-chunk warning). New files lint clean; the only ESLint findings remain the two pre-existing `set-state-in-effect` notes in `timetable.tsx` (hydration effect) and `TimetableActionBar.tsx` (overlay-close effect). No backend, solver, schema/migration, or new package changes. **Docs:** `project-overview.md` (scope: frontend Excel download UI now in scope) and `code-standards.md` (File Organization: `apiRequestBlob` binary helper + `timetableExport.ts`) updated. **Remaining/next:** the export regression/acceptance pass (Unit 95, spec present) is a separate unit.

- **Unit 93 complete — backend timetable Excel export template and API.** Backend-only unit (`backend/` boundary; no frontend, solver, or schema/migration change) adding a protected `GET /timetable/export.xlsx?title=...` endpoint that renders the **saved** timetable into a fixed, repo-owned Campion `.xlsx` template and streams it in-memory — no generated file is persisted to the database or object storage, and no assignment/block state is mutated. **Dependency:** added `openpyxl>=3.1.5` (the one new package the spec allows). **Static template** (`backend/export_templates/campion_timetable_export_template.xlsx`): derived once from the uploaded `S2, 2025 Timetable` source sheet (`backend/assets/excel/Timetable S2 2025 DRAFT .xlsx`) by the committed `build_template.py`. The build keeps the single layout sheet, clears every app-populated class/event value from the s1–s7 grid bands and normalises them to the grey empty-cell style (re-merged into clean 2-row vertical bands), preserves all fixed/static content (day headers, room headers, time labels, the static `Mass/Lunch` row, the evening rows incl. the static `FORMAL HALL` banner per the user decision, the notes area, and the lecturer/tutor key layout), clears the old key names, and sets the version text to a static `Version 1`. **Service** (`services/timetable_excel_export.py`): `generate_timetable_export(db, title) -> BytesIO` loads the template, sets the title (`B2`) + `Version 1` (`K29`), loads saved assignments (+ unit/lecturer/room) and blocks, validates every room name against the fixed room order, generates session labels + tutorial letters + the lecturer key, paints classes and block rectangles into the mapped cells, and returns an in-memory stream. **Fixed mapping** is explicit constants (mirrored in the build script): days map left→right Monday–Friday in 8-column room blocks (`PDS, L1.05, Bromley, L1.08, Dawson, L1.10, L1.12, JTW` → column starts B/J/R/Z/AH); app slots s1–s7 map to two-row bands (top rows 6/8/10/14/16/18/20). A one-slot session fills both rows of its band; a multi-slot session merges vertically across all its bands. **Classes** render with subject styling: a small export-scoped subject→fill map keyed by unit-code prefix (HIS/PHI/THE/LIT/LAN/GRE/SCI, neutral gold fallback) — per the user's styling decision, the **one scoped exception** to the otherwise frontend-only subject-colour rule, used purely to render the Excel artifact, **not** a general backend subject parser (documented in `architecture-context.md` invariant 32 + `code-standards.md`). **Session labels** use `UNITCODE Lecture|Tutorial[ Letter] (Initials)` (e.g. `HIS101 Lecture (SC)`, `THE202 Tutorial A (LH)`); initials are first+last initial ignoring title; a scheduled session missing its lecturer fails with `export_session_missing_lecturer`. **Tutorial letters** are export-only, per unit, in timetable order (day Mon–Fri → start slot s1–s7 → fixed room order → stable session id); unscheduled tutorials consume no letters. **Blocks** render with the template block/event styling mapped from the app block colour (unnamed→grey empty, named gold/light_blue/light_pink — the user's block-colour decision), grouping contiguous same-group cells into merged rectangles (mirrors the frontend rectangle merging, split at the static lunch gap so `Mass/Lunch` is never clobbered); named blocks export their name, unnamed export blank. **Lecturer/tutor key** is regenerated from exported sessions (unique by lecturer id, `Initials: Title First Last`, sorted by initials then display name, balanced across the two key columns A/D), preserving the key-area styling. **Room matching is by name**: any saved assignment or block using a room not in the fixed order fails the export with structured `export_room_not_in_template` (422). **Route** (`api/timetable_export.py`, registered in `api/router.py`): thin handler behind `get_current_admin`, validates a non-empty trimmed `title` (structured `invalid_export_title` 422 otherwise), streams the workbook with media type `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` and a `Content-Disposition` filename slug `{title-slug}-{YYYY-MM-DD}.xlsx` (e.g. `semester-2-2026-timetable-2026-06-20.xlsx`). Structured errors never leak stack traces or filesystem paths (missing template → generic `export_template_missing` 500; unexpected overlap of static content → `export_overlaps_static_content` 409). **Tests** (`tests/test_93_timetable_excel_export.py`, 26): endpoint requires auth; title required (missing + blank); `.xlsx` content-type/disposition + a loadable single-sheet workbook; template loads with static content preserved; one-slot fills both rows; multi-slot spans all rows; parametrized fixed day/room/slot mapping + `cell_anchor` helper; unknown room fails (assignment + block); lecture label format; tutorial letters by timetable order; unscheduled tutorials consume no letters; lecturer initials + key generation/sort; missing-lecturer failure; named block name+style; unnamed block blank+style; block rectangle merging; filename slug (incl. non-alnum fallback); and the no-persistence guarantee (DB row counts unchanged, returns `BytesIO`). The test file ships a small self-contained ASGI client that handles query strings and—critically—blocks in `receive()` after delivering the request so `StreamingResponse`'s disconnect listener never truncates the streamed body. **Verification:** backend **440 passed** (414 prior + 26 new); app import green; no schema/migration change. **Docs:** `project-overview.md` (scope: backend Excel export now in scope, frontend UI deferred), `architecture-context.md` (storage-model notes + new invariant 32), and `code-standards.md` (export rules + File Organization) updated. **Remaining/next:** the frontend Excel download UI (Unit 94, spec present) and the export regression/acceptance pass (Unit 95, spec present) are separate units; `openpyxl` must be installed in any deployment image (added to `backend/requirements.txt`).

- **Unit 92 complete — student upload regression, docs, and acceptance pass (Units 89–92).** Verification and documentation only; **no new product features and no production code changes were required** — the 89–91 batch verified clean. **Automated verification (2026-06-20):** backend **414 passed**; frontend **283 passed** across 24 files; frontend **production build green** (strict `tsc -b` + Vite; only the pre-existing large-chunk warning, `915.06 kB`/`268.43 kB` gzip); backend app-import smoke green (`TTS3 API`); Alembic repository head **`0014 (head)`** with local DB `current` also at **`0014`**. **Backend regression** confirms the spec coverage: the deliberate destructive student reset migration (`0014`), the `student_number` CRUD contract, CSV structural validation, `dd/mm/yyyy` date filtering (Australia/Sydney), student-number year derivation for new students, existing-student name update without year overwrite, additive enrolment, allocation rebalance, and auth-protected structured errors — via `tests/test_89_student_number_reset_contract.py` (24) and `tests/test_90_student_csv_import.py` (31). **Frontend regression** covers the student-number CRUD UI, table/search display, the upload dialog, the multipart API client, success/error states, and query invalidation after import — via `lib/api/students.test.ts` and the `routes/students.test.tsx` Unit 91 suites. **Scope guard passes** — the batch introduced no CSV file retention, blob/object storage, unit creation from CSV, enrolment deletion/sync mode, student- or lecturer-facing views, soft constraints, timetable version history, or hidden timetable-assignment mutation beyond the normal `rebalance_unit_session_allocations` refresh (confirmed by reading `services/student_import.py`: in-memory parse only, units looked up never created, `student.units.append(...)` only — never removed). **Docs:** the descriptive context files (`project-overview.md`, `architecture-context.md`, `code-standards.md`) were already aligned to the 89–91 decisions in those units (CSV upload in scope + processed-and-discarded + additive + unknown-codes-skipped + student-number-required; `students.student_number` required-unique column, backend-owned CSV route, no blob storage, canonical `unit_students` write + hidden allocation refresh; parser/service location, student-number validation contract, CSV header/date rules, counts-over-raw-lists logging), so no context-doc changes were required this unit. Created `docs/student-upload-acceptance-flow.md` (18-step manual PASS/FAIL checklist + automated evidence + scope-guard record). The interactive browser checklist remains intentionally unchecked for a human run. **Remaining manual follow-ups:** run the browser checklist against a full stack, and deliberately apply `alembic upgrade head` (now `0014`, a destructive student reset) on the deployed database; the frontend large-chunk code-splitting warning remains an open optimization when prioritized.

- **Unit 91 complete — frontend student-number contract and CSV upload UI.** Frontend-only unit (`frontend/` boundary; no backend/solver/schema change) wiring the `/students` page to the Unit 89 `student_number` contract and the Unit 90 `POST /students/import-csv` import. **API client** (`lib/api/client.ts`): `apiRequest` now detects a `FormData` body and omits the default `Content-Type: application/json` for it, so the browser sets the multipart boundary itself (JSON requests are unchanged). **API types** (`lib/api/students.ts`): `student_number` added to `Student`, required on `StudentCreate`, optional on `StudentUpdate`; new `StudentImportResult` type mirroring the backend aggregate (`created_students`, `updated_students`, `added_enrolments`, `skipped_unknown_unit_rows`, `skipped_invalid_rows`, `skipped_past_census_rows`, `deduped_rows`); new `uploadStudentCsv(file)` posting `FormData` through the authenticated client (structural `AppError` envelopes already surface their human message on `ApiRequestError.message`, so they propagate for the caller to display). **Create/edit form** (`routes/students.tsx`): a required `Student number` input is the first field, validated as exactly 8 digits (`/^\d{8}$/`) with an inline format error shown once a non-empty invalid value is typed; create/save stays disabled until the whole form (including the number) is valid; the edit dialog initializes the number from the student; `year_level` remains the existing manual selector and is **never** derived from the number in this form. **Table + search**: a `Student number` column was added (first column, monospace/tabular) and the management search now matches first name, last name, **and** student number (`filters.ts`); existing name/year/unit/subject filters and the no-title student model are unchanged. **CSV upload action**: an `Upload student information` header button opens a dialog stating the expected headers (`Student number, first name, last name, scheduled unit code, dest census date`) and the `dd/mm/yyyy` date format, with a `.csv`-only file input; Upload is disabled until a file is chosen and shows an `Uploading…` loading state; structural backend errors render in a clear error alert; on success an aggregate summary shows created/updated students, added enrolments, skipped unknown-unit rows, and skipped invalid rows **only when nonzero** — `skipped_past_census_rows` is deliberately never shown. On success the four dependent queries `['students']`, `['units']`, `['schedulable-sessions']`, `['assignments']` are invalidated (reusing the page's existing `invalidateDependentQueries`) so enrolment counts, hidden allocation-derived session pools, and timetable validation data refresh; the native file input is force-remounted (keyed) to clear its filename. The CSV is never stored in localStorage/Zustand/blob — it is sent straight to the backend. **Tests:** `fixtures.ts` `makeStudent` gains a default `student_number`; `filters.test.ts` adds a student-number search case; new `lib/api/students.test.ts` (2 — `uploadStudentCsv` posts a `FormData` body carrying the file with no JSON `Content-Type`, and returns the parsed result) stubbing `fetch`; `routes/students.test.tsx` adds two Unit 91 suites (16 — number field in create, edit init from existing number, create disabled for invalid number + inline error + re-enabled when valid, valid create submits `student_number`, year stays manual/not derived on create, edit shows stored not derived year, table shows the number column, search matches the number; upload dialog opens + states format, upload disabled until a file is chosen, file sent through the client, success summary shows required counts but not past-census, structural error displays, dependent queries invalidate). **Verification:** strict `tsc -b` green; frontend **283 passed** across 24 files; production build green (`915.06 kB`/`268.43 kB` gzip; only the pre-existing large-chunk note). Changed files lint clean (the only ESLint findings are the two pre-existing `units` memo-dependency warnings in `students.tsx`). No backend, solver, schema/migration, or new package changes. **Docs:** `code-standards.md` (frontend student CSV upload UI rule + API-client multipart note + File Organization update) updated.

- **Unit 90 complete — backend student CSV import API.** Backend-only unit (`backend/` boundary; no frontend, solver, schema-migration, or storage change) that adds a protected `POST /students/import-csv` endpoint reading a single uploaded `.csv` (multipart form data), filtering current rows by census date, creating/updating students by the Unit 89 `student_number`, enrolling them into matching **existing** units (units are never created from CSV), and returning aggregate counts. The CSV is parsed in-memory and discarded — **no blob/object storage**. Added the one new dependency the spec allows: **`python-multipart>=0.0.18`** (installed `0.0.32`) for FastAPI file uploads. **Route** (`api/students.py`): `POST /students/import-csv` behind `get_current_admin`, accepts an optional `UploadFile` (so a missing file becomes a structured 422 rather than FastAPI's generic error), reads the bytes, and delegates all validation to the service. **Schema** (`schemas/student.py`): `StudentImportResult` (`created_students`, `updated_students`, `added_enrolments`, `skipped_unknown_unit_rows`, `skipped_invalid_rows`, `skipped_past_census_rows`, `deduped_rows`) — counts only, never student lists or raw rows. **Service** (`services/student_import.py`): `import_students_csv(db, *, filename, content, today=None)` with `today` injectable for deterministic tests (defaults to `Australia/Sydney` now via `zoneinfo`; its `.year` drives new-student year derivation). **Structural rejection (whole file, 422):** missing file (`import_missing_file`), wrong/`.csv`-less extension (`import_invalid_file_type`), non-UTF-8 (`import_invalid_encoding`, decoded with `utf-8-sig` to strip a BOM), empty/whitespace file (`import_empty_file`), and a header that — after per-cell trim → lowercase → collapse-whitespace normalization — is not **exactly** the five required columns (`Student number, first name, last name, scheduled unit code, dest census date`), covering missing, wrong, and extra columns (`import_invalid_header`). **Row classification** (pre-loads units by normalized `code` and students by `student_number` up front): per row trim all fields; reject as `skipped_invalid_rows` if `student_number` isn't exactly 8 digits, either name is blank, or `dest census date` doesn't parse as `dd/mm/yyyy`; else `skipped_past_census_rows` if census `< today` (Sydney); else `skipped_unknown_unit_rows` if the trimmed-uppercased unit code isn't a known unit; else — for a **new** student only — `skipped_invalid_rows` if the derived year is `< 1` (future cohort, e.g. `20271234` in 2026); else a candidate, deduped on `(student_number, unit_code)` into `deduped_rows`. Year derivation `current_year - first_four_digits + 1` (reject `< 1`, cap `> 3` to 3) applies to **newly created students only** — existing students are matched by number and never re-derived, so manually edited year levels are preserved; existing student first/last names ARE updated from the CSV (first-row-wins for the name). **Apply phase** (one transaction): create missing students with the derived initial year, update changed existing names, append missing `unit_students` enrolments (additive — existing enrolments are never removed), collect affected unit IDs, `rebalance_unit_session_allocations` for each in the same transaction, then `commit`; unexpected persistence failure rolls back and raises `import_failed` (500). **Tests:** new `tests/test_90_student_csv_import.py` (31 — exact/case-spacing/BOM header acceptance; missing-file/wrong-extension/bad-encoding/empty/whitespace/missing-column/wrong-column/extra-column rejection; `dd/mm/yyyy` parse + on-today inclusion; past-census ignored; six invalid-value forms skipped; future-cohort rejected; unknown-unit skipped without creating a unit; `(student_number, unit_code)` dedupe incl. case-normalized; new students created with derived/capped year (parametrized 1/2/3/cap); existing matched-by-number + name update; manually edited year preserved; unchanged name not counted as update; one student into multiple units; additive import never removes prior enrolments; affected lecture allocations rebalanced; accurate aggregate counts; and route-level multipart upload success, invalid-header 422, and auth-required 401 via a small multipart ASGI client). **Verification:** backend **414 passed** (383 prior + 31 new); app import green (`TTS3 API`); route registered at `/students/import-csv`; no Alembic/schema change. **Docs:** `project-overview.md` (Student Management — CSV import), `architecture-context.md` (invariant 31 + storage note), and `code-standards.md` (student CSV import rules) updated.

- **Unit 89 complete — backend student number reset and required unique contract.** Backend-only unit (`backend/` boundary; no frontend, solver, or new package change) that adds `student_number` as the canonical institutional identifier while keeping `students.id` as the internal primary key. **Deliberate destructive reset:** new migration **`0014_student_number_reset_and_contract` (head now `0014`, revises `0013`)** intentionally `DELETE`s all existing `students` rows before adding the new required column — existing student data is discarded on purpose so the DB starts fresh under the required contract (this is NOT a preservation/backfill migration; the destructive intent is documented in the migration module docstring and inline comments). The delete relies on the existing `ON DELETE CASCADE` FKs on `unit_students.student_id` and `session_student_allocations.student_id` to clear dependent enrolment/allocation rows; the now-empty table then takes `student_number` as `NOT NULL` (no backfill/server default needed) with a unique index `ix_students_student_number`. `students.id` PK and the `ck_student_year_level` (1–3) CHECK are left unchanged; downgrade drops only the column + index (the data reset is not reversible, documented). **Model** (`models/student.py`): `student_number: Mapped[str]` `nullable=False, unique=True, index=True`, placed after `id`. **Schemas** (`schemas/student.py`): shared `_normalize_student_number` (trim → require `^\d{8}$`, else `ValueError`→422), `STUDENT_NUMBER_PATTERN`/`STUDENT_NUMBER_ERROR`; `StudentCreate.student_number: str` required + validator; `StudentUpdate.student_number: str | None` optional-but-validated-when-supplied; `StudentResponse.student_number: str` returned. Validation rejects letters, punctuation, internal spaces, blank, and any non-8-digit length, trimming surrounding whitespace. **Service** (`services/student.py`): new `_check_student_number_unique(db, number, exclude_id=None)` → structured 409 `AppError("student_number_conflict", …)` mirroring the unit-code-conflict pattern; `create_student` rejects duplicates before persisting and sets `student_number`; `update_student` rejects duplicates excluding self when a new number is supplied. Existing enrolment auto-enrol behavior and **manual** `year_level` create/update behavior preserved; **no** year derivation from `student_number` here (that derivation is reserved for the future CSV upload feature, not added in this unit). **Tests:** new `tests/test_89_student_number_reset_contract.py` (24 — model required/NOT NULL + `id`-only PK + unique-index contract, DB-level uniqueness + NOT NULL enforcement, migration documents the intentional destructive reset, create requires number, create rejects 9 invalid forms + trims whitespace + persists, create rejects duplicate (409/code), update changes number, update rejects duplicate excluding self while allowing self-resubmit + leaves row unchanged on conflict, update rejects invalid + leaves number unchanged when omitted, response includes `student_number`, year level stays manual and is not derived from the number). Updated every existing fixture that builds a `Student`/`StudentCreate` to supply a valid unique 8-digit `student_number` (helper builders in `test_year_level_and_enrolment`, `test_unit_team_and_session_lecturer`, `test_solver_job`, `test_solver_api`, `test_apply`, `test_session_allocations`, `test_constraint_and_solver_suite` now draw from a monotonic `itertools.count(10_000_000)`; inline constructions in `test_68_allocation_solver`, `test_72_title_and_unit_code_contract`, and the year/DB-constraint cases get explicit distinct literals so each rule under test is isolated). **Verification:** backend **383 passed** (24 new); app import green (`TTS3 API`); Alembic head **`0014 (head)`**. **Docs:** added the `student_number` contract to `project-overview.md` (Student Management fields), `architecture-context.md` (Storage Model bullet + new invariant 30) and `code-standards.md` (General student rules). No frontend/solver/package changes.

- **Post-88 UI fixes — block rectangle merging, lock icon removal, and left-border removal.** Three sequential visual changes applied after Unit 88: (1) **Horizontal merging**: adjacent room-column cells of the same block group at the same slot now render as one wide card; (2) **Lock icon + left border removed**: blocks are now visually distinguished from session cards solely by the absence of a left-border accent (no lock icon); (3) **Full rectangle merging (horizontal + vertical)**: contiguous cells of the same block group that form a rectangle (consecutive rooms × consecutive slots) now render as a single card spanning both dimensions, mirroring how multi-slot sessions are rendered. Algorithm: `buildBlockAnchorData` in `blocks.ts` groups cells by (blockId, day, room_id) → finds consecutive slot runs → groups runs by (blockId, day, startSlot, slotSpan) → finds consecutive room runs → produces one anchor per rectangle with `{ roomSpan, slotSpan }`; anchor renders with `width = calc(N_rooms × 100% + (N_rooms−1)px)` and `height = calc(N_slots × 3.5rem)`. `TimetableGrid` passes `blockRoomSpan`, `blockSlotSpan`, and `suppressBlockVisual` to each `GridCell`; `BlockCellCard` applies both spans via inline style overrides on `inset-0`. `ui-context.md` rendering rules updated to describe rectangle merging and the no-lock/no-left-border visual contract. **Verification:** frontend **266 passed** (259 prior + 7 new `buildBlockAnchorData` tests including vertical and combined rectangle cases); strict `tsc -b` green; production build green (`910.26 kB`/`267.33 kB` gzip). No backend changes.

- **Timetable Blocks batch (Units 84–88) — complete.** All five units delivered and verified. The descriptive context files (`project-overview.md`, `architecture-context.md`, `ui-context.md`, `code-standards.md`) were pre-aligned to the batch. No further timetable-blocks units are queued. Standing operational items remain: run the browser checklist in `docs/timetable-blocks-acceptance-flow.md` against a full stack, deliberately apply `alembic upgrade head` (now `0013`) on the deployed database, and address the frontend bundle code-splitting warning when prioritized.

- **Unit 88 complete — timetable blocks regression, docs, and acceptance pass.** Verification and documentation only; no new product features or production code changes were required — the 84–87 batch verified clean. **Automated verification:** backend **358 passed**; frontend **259 passed** across 23 files; frontend **production build green** (strict `tsc -b` + Vite; only the pre-existing large-chunk warning, `908.65 kB`/`266.61 kB` gzip); backend app-import smoke green (`TTS3 API`); Alembic repository head **`0013 (head)`** (local DB at `0012`). **Scope guard passes** — the batch introduced no fake sessions (blocks never enter the session/solver variable models), no all-rooms block abstraction, no soft constraints, no timetable version history, no file import/export, no student/lecturer-facing views, and no hidden backend mutation on normal drag/drop (block create/update is an explicit admin action through the block CRUD API). **Docs:** all descriptive context files were pre-aligned; no context-doc changes were required. Created `docs/timetable-blocks-acceptance-flow.md` (18-step manual PASS/FAIL checklist covering block create, unnamed/named rendering, room-specificity, hover/drop rejection over blocks, dirty-draft guard, block-over-saved-assignment unscheduling, block delete, restored draft cleanup, solver avoidance of blocked cells, and partial solver result). The interactive browser checklist remains intentionally unchecked for a human run.

- **Unit 87 complete — solver and backend defensive timetable-block integration.** Backend/solver-only unit (no frontend or schema change, per spec scope) that makes timetable blocks a hard backend **and** solver constraint: stale frontend data can never save a session into a blocked cell, and the solver never generates an assignment occupying one. **Constraint mirror** (`constraints/types.py`): added `ConstraintType.TIMETABLE_SLOT_BLOCKED = "timetable_slot_blocked"` with `blocking` severity in `CONSTRAINT_SEVERITY` — a cell-feasibility constraint, **not** a conflict-graph edge (`build_conflict_graph` is unchanged; blocks are enforced as static candidate exclusion). **Solver snapshot** (`solver/types.py`): new frozen `BlockedCellSnapshot(day, slot, room_id, block_group_id, block_name)` and a `blocked_cells: list[BlockedCellSnapshot]` field on `SolverInputSnapshot` (defaulted via `field(default_factory=list)` so existing direct constructions and 4-arg `build_snapshot_from_data` calls keep working); both exported from `solver/__init__.py`. **Snapshot builder** (`solver/snapshot.py`): `build_snapshot_from_data` gained an optional `blocked_cells` arg, builds a normalized `(day, slot, room_id)` key set, sorts blocked cells deterministically by `(day_index, room_id, slot_index)`, and stores them on the snapshot; `_validate_saved_assignments` now also expands each saved assignment's occupied slots and raises `SnapshotIntegrityError` if any intersects a blocked cell (so a saved assignment overlapping a block can never be valid solver input). The DB loader `build_solver_input_snapshot` loads all `TimetableBlockCell` rows with their owning group (`selectinload(TimetableBlockCell.block_group)`) and maps them to `BlockedCellSnapshot` carrying `block_name`. **CP-SAT model** (`solver/model.py`): builds a `blocked_room_cells` set of `(day, room_id, slot_index)` from `snapshot.blocked_cells` and, during candidate generation, skips any candidate whose occupied cells intersect it — **before** creating the CP-SAT variable — composing with the existing capacity, lunch, off-timetable, lecturer-availability, and locked-room-occupancy exclusions. **Result application** (`solver/apply.py`): new `_load_blocked_cells(db)` reads live block cells into a `(day, room_id, slot_index)` set; `_validate_generated` defensively rejects any generated placement overlapping a block with structured code `assignment_overlaps_timetable_block`, rolling back before any write so saved/locked state is preserved (this guards against a block added after the snapshot was built). **Assignment save** (`services/assignment.py`): `save_assignments` loads blocked cells once and rejects any requested assignment whose duration-expanded occupied cells intersect a block via `_reject_save("assignment_overlaps_timetable_block", …)` — defensive persistence validation, not the normal frontend-owned UX path. **Job/API**: unchanged start/status behavior — a saved assignment overlapping a block now fails the existing structured `snapshot_integrity` path in `run_solver_job` (snapshot build raises `SnapshotIntegrityError`, the job returns `FAILED`/`failure_code="snapshot_integrity"`, saved state untouched). **Tests**: new `tests/test_87_solver_block_integration.py` (19 — constraint-mirror value/severity; snapshot carries/defaults/sorts blocked cells; saved single- and multi-slot overlap → `SnapshotIntegrityError`, non-overlap accepted; solver schedules around a blocked cell, creates no blocked candidate, returns partial when a block makes a session impossible; DB loader includes named blocked cells and rejects an at-rest saved assignment over a directly-inserted block; result application rejects a blocked generated placement and preserves an existing locked assignment; assignment save rejects single- and multi-slot block overlaps and allows non-overlapping/other-room saves; solver job fails safely with `snapshot_integrity` while preserving the overlapping saved assignment); updated `tests/test_constraints.py` enum-count (8→9) and blocking-set assertions to include `TIMETABLE_SLOT_BLOCKED`. **Verification:** backend **358 passed** (339 prior + 19 new); app import green (`TTS3 API`). No frontend, schema/migration, or new package changes. `architecture-context.md` (invariants 28–29) and `code-standards.md` (Backend Constraint Mirror) already documented this behaviour from the batch pre-alignment, so no context-doc changes were required.

- **Unit 86 complete — frontend block-selection mode and `timetable_slot_blocked` placement validation.** Frontend-only unit (no backend/solver change, per spec scope) that lets the admin select adjacent room-specific cells, name + colour them, and save a block, and that wires persisted blocks into the frontend-owned blocking-validation model so sessions can never be manually placed in a blocked cell. **Blocking rule** (`lib/validation/blocking.ts`): added `timetable_slot_blocked` to `BlockingIssueType`; `checkProposedPlacement`, `checkDraftForBlockingViolations`, and `getBlockingViolatorIds` all gained an optional `blockedCells?: Map<string, BlockedCell>` param. A new `findBlockedOverlap` expands an assignment's occupied cells (contiguous slots in its `day:room_id`) and returns the first reserved cell; `blockedMessage` produces `This time is blocked by Chapel.` when the block is named and `This time is blocked.` otherwise (never raw slot IDs). The rule rejects any proposed placement whose occupied cells intersect a blocked cell and flags existing draft assignments overlapping blocks. **Hover** (`features/timetable/hoverHighlight.ts`): `computeHoverHighlightKeys` now takes `blockedCells` and passes it to `checkProposedPlacement`, so an invalid hover over a blocked cell shows no highlight and no pre-drop reason. **Selection helper** (`features/timetable/blockSelection.ts`, pure/unit-tested): `SelectionCell`; `selectionKey`/`parseSelectionKey` (round-trip the `day:roomId:slot` key shape shared with the blocked-cell map and droppable IDs); `singleCellSelection` (excludes an already-blocked anchor); `computeRectangleSelection(anchor, target, rooms, blockedCells)` builds a same-day rectangle across slot rows × visible room columns (inclusive, corner-order normalized, may span the lunch divider), excludes already-blocked cells, and falls back to the target cell when the two cells are on different days. **Create dialog** (`features/timetable/BlockCreateDialog.tsx`): shows a day/time/room summary using human slot labels (`slotLabel`), an optional name field, and a colour selector that appears only when a name is entered (blank name → `name: null, colour: null`; named → selected colour defaulting to `gold`); remounted per distinct selection via a `key` so name/colour reset between blocks. **Grid** (`GridCell`/`TimetableGrid`): new `blockSelectionMode`/`isBlockSelected`/`onBlockCellSelect` (grid: `blockSelectionKeys`/`onBlockCellSelect`); in block mode a cell click toggles/extends the block selection instead of placing a session, the droppable is disabled, the session card and (unless no session is pending) the block overlay become click-through so selection works over occupied/blocked cells, and selected cells get a temporary token-based highlight (`--accent-primary-soft` + accent outline, `data-block-selected`, `pointer-events-none`). The block overlay also falls through to the cell while a session is pending placement, so a blocked click attempt surfaces its reason instead of opening the editor. **Action bar** (`TimetableActionBar`): new `Add block` button (disabled with a title reason when a solver run is active, blocks failed to load, data is loading, there are no rooms, or the draft is dirty → `Save or discard timetable changes before editing blocked slots.`); in block mode the bar shows concise instructions plus `Cancel` and `Create block` (the latter disabled until ≥1 cell is selected), replacing the normal Clear/Generate/Save controls. **Route** (`routes/timetable.tsx`): block-mode state (`blockMode`, `blockAnchor`, `blockSelectionKeys`, create dialog), a `createTimetableBlock` mutation that on success invalidates `['timetable-blocks']` + `['assignments']`, exits block mode, clears the selection, and surfaces a sticky-bar success notice including any `unscheduled_session_ids` count returned; starting block mode clears the pending session selection and disables normal drag/click placement (`handleSelectSession`/`handleCellClick`/`handleDragStart` guarded by `blockMode`). `blockedCellMap` (moved above the cleanup effect) is now threaded into both placement checks, the draft-violation summary, the hover computation, and the auto-unschedule effect — whose dependencies now include `draft` so a restored draft overlapping a block is cleaned up immediately (idempotent/terminating), satisfying "deleting a block frees cells without rescheduling" while data changes that newly overlap a block unschedule the affected session. **Tests**: new `blockSelection.test.ts` (10), `blocking.test.ts` +6 (`timetable_slot_blocked`: named/unnamed message, multi-slot span, different room/day clean, draft detection, no-blocked-cells passthrough), `hoverHighlight.test.ts` +2 (no highlight over a blocked cell, multi-slot span into a block), `routes/timetable.test.tsx` "Unit 86" suite (7 — Add block appears, disabled-while-dirty with reason, rectangle select + named create, blank-name colourless create, cancel clears selection, blocked click placement surfaces its reason and never schedules, restored draft over a block auto-cleaned); `createTimetableBlock` added to the route test's block-API mock. **Verification:** frontend **259 passed** across 23 files; **strict `tsc -b` green**; **production build green** (only the pre-existing large-chunk warning, `908.65 kB`/`266.61 kB` gzip); new files lint clean (the only ESLint findings remain the pre-existing `set-state-in-effect` notes in `timetable.tsx`'s hydration effect and `TimetableActionBar`'s overlay-close effect). No backend, solver, or new package changes.

- **Unit 85 complete — frontend timetable block API client, tokens, and passive grid rendering.** Frontend-only unit (no solver/placement-validation behavior, no backend change) that surfaces persisted blocks on `/timetable` and adds basic existing-block management. **API client** (`frontend/src/lib/api/timetableBlocks.ts`): `TimetableBlockColour = 'gold' | 'light_blue' | 'light_pink'`; `BlockCell`/`TimetableBlock` DTOs; `BlockCellInput` + `TimetableBlockCreate`/`TimetableBlockUpdate` input types; `TimetableBlockMutationResponse` (`block` + `unscheduled_session_ids`); `listTimetableBlocks` / `createTimetableBlock` / `updateTimetableBlock` / `deleteTimetableBlock` over the existing authenticated `apiRequest` client, with a `parseBlockError` helper turning 404/409/422 structured errors into readable messages (day/slot reuse the `AvailabilityDay`/`AvailabilitySlot` types from `lecturers.ts`). **Tokens**: added the eleven `--block-empty-*` / `--block-gold-*` / `--block-blue-*` / `--block-pink-*` design tokens to `index.css` (values already documented in `ui-context.md`), deliberately separate from the subject tokens. **Rendering helpers** (`frontend/src/features/timetable/blocks.ts`): `buildBlockedCellMap(blocks)` flattens block groups into a `Map` keyed `day:room_id:slot` (same key shape the grid uses); `getBlockColorTokens(colour)` maps each named colour to its `--block-*` token set and falls back to the grey `--block-empty-*` set for unnamed/missing/unknown colour. **Cell rendering**: new `BlockCellCard` renders an absolute in-cell overlay — always a `Lock` icon (never colour alone, per Design Invariant 11), the name only when present (`sr-only` "Blocked" for unnamed), token-based colours; `GridCell` gained `blockedCell`/`isBlockInteractive`/`onBlockClick` props and renders the block below the session layer (`blockedCell && !assignment`); `TimetableGrid` gained `blockedCells`/`isBlockInteractive`/`onBlockClick` and threads the per-cell lookup to both AM and PM `GridCell`s. **Route wiring** (`routes/timetable.tsx`): loads `['timetable-blocks']` via `listTimetableBlocks`, memoizes `blockedCellMap`, passes it to the grid, and surfaces a concise block-load error in the sticky `TimetableActionBar` (`blocksError`/`onRetryBlocks`, generalized the bar's retry button to an optional per-message `onRetry`). **Existing block edit/delete** (`BlockEditDialog`): clicking a block opens a dialog to edit name/colour or delete (no cell re-selection — saved cells are preserved on update); a named block requires a colour, clearing the name makes it unnamed and drops the colour; update/delete invalidate `['timetable-blocks']` and `['assignments']`; the returned `unscheduled_session_ids` count is surfaced as a one-time `blockNotice` in the action bar. Block edit/delete is guarded behind `blockEditingDisabled = isDirty || editingDisabled` (blocks mutate saved state immediately, so editing is disabled while the timetable draft is dirty or a solver run is active); the dialog seeds form state from the block prop and is remounted via a `key={editingBlock?.id}` rather than a setState-in-effect. **No placement validation added** (Unit 86): blocked cells stay droppable; blocks never enter the unscheduled pool (they are not sessions). **Tests**: new `features/timetable/blocks.test.ts` (8 — flatten incl. multi-cell + unnamed + empty, every named colour → tokens, null/undefined/unknown → grey); `test/fixtures.ts` gained `makeTimetableBlock`; `routes/timetable.test.tsx` adds a "Unit 85" suite (7 — named block shows name + lock, unnamed shows lock + sr-only label, block renders only in the grid not the pool, block-load error surfaces in the sticky bar, click opens the edit dialog and saves name/colour, delete from the dialog, and the dirty-draft guard prevents opening the dialog) and mocks the new block API module. **Verification:** frontend **235 passed** across 22 files; **strict `tsc -b` green**; **production build green** (only the pre-existing large-chunk warning, `900.52 kB`/`264.78 kB` gzip); new files lint clean (the only remaining ESLint findings are the pre-existing `set-state-in-effect` notes in `timetable.tsx`'s hydration effect and `TimetableActionBar`'s overlay-close effect). No backend, solver, or new package changes.

- **Unit 84 complete — backend timetable block persistence and protected CRUD API.** Backend-only data/API contract for room-specific timetable blocks (no frontend validation or solver integration yet — those are Units 85–87). **Data model** (`backend/models/timetable_block.py`): new `BlockColour` enum (`gold`/`light_blue`/`light_pink`); `TimetableBlockGroup` (`id`, nullable `name`, nullable `colour`, timestamps, `cells` relationship with `cascade="all, delete-orphan"` + `passive_deletes`); `TimetableBlockCell` (`id`, `block_group_id` FK→`timetable_block_groups` `ondelete=CASCADE`, `day`/`slot` enums, `room_id` FK→`rooms` `ondelete=CASCADE`, `created_at`), with a unique `(day, slot, room_id)` (`uq_block_cell_day_slot_room`) and indexes on `block_group_id`, `room_id`, and `(day, slot, room_id)`. Registered in `models/__init__.py`. **Migration `0013_timetable_blocks` (head now `0013`)**: creates the `blockcolour`/`blockday`/`blockslot` Postgres enums and both tables with the FKs, unique constraint, and three indexes; reversible downgrade drops indexes, tables, then enum types. **Schemas** (`backend/schemas/timetable_block.py`): `BlockCellInput`, `TimetableBlockCreate`/`TimetableBlockUpdate` (name, colour, cells), `BlockCellResponse`, `TimetableBlockResponse`, and `TimetableBlockMutationResponse` (`block` + `unscheduled_session_ids`). **Service** (`backend/services/timetable_block.py`): trims name (blank→`null`); rejects named-without-colour (`block_named_without_colour`) and unnamed-with-colour (`block_unnamed_with_colour`); requires ≥1 cell (`block_no_cells`); validates rooms exist (`room_not_found`); dedupes duplicate request cells; rejects cells already blocked by another group (`cell_already_blocked`, update excludes the group's own cells); on create/update expands each saved assignment's session duration into occupied cells (reuses `ORDERED_SLOTS` from `services.assignment`), deletes overlapping assignments in the same transaction, and returns their `unscheduled_session_ids`; update replaces cells (deletes orphans + flush before insert to avoid tripping the unique constraint); delete frees cells via cascade without rescheduling; missing group → `block_group_not_found` (404). **Routes** (`backend/api/timetable_blocks.py`, registered in `api/router.py`): protected `GET/POST /timetable-blocks`, `PUT/DELETE /timetable-blocks/{block_group_id}` — thin handlers behind `get_current_admin`, structured errors via `AppError`. **Tests** (`tests/test_84_timetable_blocks.py`, 25): persistence + room-specificity, name trim/blank-to-unnamed, named/unnamed colour rules, empty-cells/unknown-room/dedupe/cross-group-duplicate rejection, list, update keep-own-cells / replace / cross-group rejection / missing-group, create+update unscheduling overlapping assignments (incl. multi-slot duration expansion) and not touching non-overlapping/other-room assignments, delete frees cells without rescheduling + cascades + missing-group. **Verification:** backend **339 passed** (314 prior + 25 new); app import green (`TTS3 API`); Alembic head **`0013 (head)`**. No frontend, solver, or new package changes.

- Unit 83 complete — **More-features regression, docs, and acceptance pass (Units 72–82).** Verification and documentation only; **no new product features and no production code changes were required** — the 72–82 batch verified clean. **Automated verification:** backend **314 passed**; frontend **222 passed** across 21 files; frontend **production build green** (strict `tsc -b` + Vite; only the pre-existing large-chunk warning, `893.31 kB`/`263.19 kB` gzip); backend app-import smoke green (`TTS3 API`); Alembic repository head **`0012 (head)`**. **Backend regression** confirms Unit 72 coverage (student-title removal, final lecturer titles, unit-code structural validation + normalized uniqueness, scheduling/solver suites) via `tests/test_72_title_and_unit_code_contract.py` (43 tests) and the wider suite; confirmed **no backend subject parser/colour logic** exists (`backend/schemas/unit.py` only enforces the structural `AAA999` contract + authoritative year derivation). **Frontend regression** covers the parser (`lib/unit-code-parser.ts`), subject colours (`features/timetable/unitColors.ts`), invalid-code create/save blocking, student-title removal, lecturer title options, subject/year filters, fixed-width unscheduled pool + search, sticky action bar + overlay details, slot-label display (`lib/slot-label.ts`), drag preview/hover-range helpers (`features/timetable/hoverHighlight.ts`), local draft persistence (`features/timetable/draftStorage.ts`), empty-timetable save, draft-only Clear All, `Lunch/Mass` row, `Campion - Timetable` navbar, and the two-column unit modal. **Scope guard passes** — the batch introduced no backend subject parser/storage, soft constraints, version history, file import/export, student/lecturer-facing views, Redis/cache, blob storage, hidden backend mutations on drag/drop, or immediate backend mutation on Clear All (`clearAssignments()` is never called from the timetable route; verified by grep). **Docs:** the descriptive context files (`ui-context.md`, `architecture-context.md`, `code-standards.md`) were already aligned to the 72–82 decisions in earlier units; this unit added explicit helper/module-location notes to `code-standards.md` (parser, draft-storage, slot-label, subject-colour helpers) and created `docs/more-features-acceptance-flow.md` (22-step manual PASS/FAIL checklist + automated evidence + scope-guard record). The interactive browser checklist remains intentionally unchecked for a human run.

- Unit 82 complete — **Frontend unit modal layout polish (two-column).** Restructured the `/units` create/edit modal so unit identity, teaching team, and students sit in one column and sessions in the other (per spec; confined to `frontend/`, no backend/API/persistence change). **Layout:** `UnitFormFields` outer grid changed from `md:grid-cols-2` to `md:grid-cols-[minmax(0,1fr)_minmax(22rem,28rem)]` (stacks on narrow screens); **left column** now groups Unit Code → parser feedback (Unit 74) → Unit Name → Teaching Team → Students; **right column** is Sessions only. Both `DialogContent`s widened `sm:max-w-4xl → sm:max-w-5xl`. **Sessions (both modes):** the create **and** edit modals both keep live inline session management on the right side — Add session, `SessionBox`es, team-restricted lecturer select, Lecture/Tutorial type, 1–4h duration stepper — with a shared `No sessions yet. Add a session to schedule this unit.` empty state until a session is added. (Spec 82 was revised on user feedback: create-mode keeps creating sessions exactly as before — held in form state and persisted via the existing `createUnitSession` loop right after `createUnit` succeeds — rather than showing a no-sessions message.) **Students action row:** the single conditional `Select all Year X students` link became a two-action row on one line — `Select Year {N} Students` (disabled when the unit-code year is invalid, i.e. `derivedYear === null`, styled with `--disabled-text`) and `Clear All` (always enabled; clears `student_ids` only and sets `studentSelectionTouched`, leaving teaching team, sessions, code, and name untouched). Manual student toggles still persist after either action. Token-based styling throughout; no `components/ui/*` edits. **Tests (`routes/units.test.tsx`):** width assertion updated to `sm:max-w-5xl`; the two original session tests (lecturer-team restriction, Lecture/Tutorial + duration clamp) stay in the create dialog; new `Unit 82` describe block (10 tests — two conceptual columns; create supports adding sessions on the sessions side with empty-state → session-box transition; create save persists added sessions via `createUnitSession` after `createUnit`; edit live session management; parser feedback still visible; Clear-All clears students only/preserves team; Select-Year re-selects derived-year students; Select-Year disabled on invalid code; many students/lecturers usable; many edit sessions render). A new `openEditDialog`/`makeEditableUnit` helper drives the edit-modal cases. Units suite **19 green**; full frontend suite **222 green** across 21 files; **production build green (strict tsc)** (pre-existing large-chunk warning only). No backend, API, persistence, or package dependency changes. The two-column modal design was already documented in `ui-context.md`/`architecture-context.md` from the 72–83 batch, so no context-doc changes were required.

- Unit 81 complete — **Frontend timetable visual styling and navbar polish.** Visual-only pass, no scheduling/solver/validation/persistence logic changed. **Grid borders darkened (token-based):** `index.css` `--grid-line` `#E5DED2→#D3C9B8` and `--grid-line-strong` `#CFC4B4→#A99A80`; new `--grid-border-emphasis: #8A7B61` now styles the `TimetableGrid` outer frame (was `--grid-line-strong`). Empty/hover/selected/warning/dragging/solver-running states are unchanged and still readable. **Lunch/Mass divider:** new tokens `--grid-lunch-mass-bg: #7A1F2B`, `--grid-lunch-mass-text: #F7F0D8`, `--grid-lunch-mass-border: #C9A646` (deliberately separate from the error palette); `TimetableGrid` lunch row repainted with these tokens (`border-y`, maroon bg, gold border, gold-cream text), centre label changed `Lunch → Lunch/Mass`, time-column keeps `12:00`, both cells stay non-interactive (`select-none` + `onContextMenu` prevent-default preserved). **`/timetable` header removed:** dropped the `PageHeader` title/description (and its import) so the sticky `TimetableActionBar` is the first major element under the navbar; an `sr-only` `<h1>Timetable</h1>` preserves the page landmark. Management pages (`/rooms`, `/lecturers`, `/students`, `/units`) untouched. **Navbar brand:** `TopNav` brand `Timetable Scheduler → Campion - Timetable` (kept `font-bold`, `--font-serif`, `--accent-primary`, left placement; centred nav links unchanged). Login/signup branding intentionally left as-is (out of spec scope). **Solver button:** idle label already `Generate Timetable` and solver-blue from Unit 70; running label aligned `Solving... → Generating…`; disabled state keeps the solver accent + title reason. **Tests:** `TimetableGrid.test.tsx` lunch assertions → `Lunch/Mass`; `routes/timetable.test.tsx` lunch text + solver-button regex updated, new test asserts no visible header/description and an `sr-only` H1; new `components/layout/TopNav.test.tsx` (2 tests — brand text/font/weight/colour, centred links). Targeted suites **38 green**; **production build green (strict tsc)** (pre-existing large-chunk warning only). Token tables in `ui-context.md` realigned to the implemented darker grid values. No backend, API, or package dependency changes.

- Unit 80 complete — **Frontend timetable empty-draft save, clear, and save-state fixes.** Audited the explicit-save path against the empty-draft edge cases and confirmed the behavior was already correct from Units 70/79: `handleSave` has **no** `draft.length === 0` early return; the Save button is gated on `!isDirty` (not `draft.length`), so a dirty empty draft (after Clear All) is saveable; the save mutation sends `{ assignments: [] }` for an empty draft; the `saveAssignments` client does not reject empty arrays; Save labels are `Save Timetable` (dirty) / `Saving…` (saving) / `Saved` (clean, disabled); Clear All stays draft-only behind the confirmation dialog, never calls the backend, and persists the empty dirty draft to local storage (Unit 79) until an explicit Save; the solver stays blocked while the empty draft is dirty (`Save your timetable changes before running the solver.`) and an empty saved timetable is not a validation issue. No production code change was required — the bug-fix scope was already satisfied, so this unit **adds the two missing regression tests** the spec calls for: an empty dirty draft (Clear All) surviving a local-storage restore (`Unsaved draft restored.`, dirty, session back in the pool) and the solver remaining blocked while the empty draft is dirty after Clear All. New `routes/timetable.test.tsx` describe block "Unit 80: empty-draft save and clear" (2 tests). Timetable route suite **31 green**, **production build green (strict tsc)**. No backend, API, or package dependency changes.

- Code-review fix pass (post Unit 79) — verified review findings against current code and applied the still-valid ones. **Fixes:** `TimetableGrid.tsx` now declares all hooks before the `rooms.length === 0` early return (Rules-of-Hooks bug); `routes/timetable.tsx` blocks manual placement (`handleCellClick`/`handleDragEnd`) while the saved assignments query is in error, so an incomplete draft can never overwrite the unknown saved timetable (Save/Clear stay disabled because the draft cannot become dirty/non-empty — regression assertions added to the assignment-load-error test); `routes/students.tsx` `isFormValid` validates `year_level` is an allowed `YEAR_LEVELS` value rather than merely non-empty. **Test/doc hardening:** `test_72…py` passes an explicit `title=` to prove the unknown field is dropped; `features/{lecturers,units}/filters.test.ts` assert the invalid `ENG` prefix matches nothing directly; `routes/lecturers.test.tsx` asserts the exact subject option set and the exact 7-title count; `72-more-features.md` lecturer title list aligned to the finalized contract (`Mr, Ms, Mrs, Dr, Fr, A/Prof., Prof.`); Unit 69 tracker entry drops the stale "session type" search field. **Skipped:** ui-context "Unit / Session Card Colors" already describes the subject-prefix token model (no hash-based text remains); the Vitest-vs-Jest nitpick is moot — `code-standards.md` already documents Vitest as the test runner. Backend Unit 72 tests **43 green**, affected frontend suites **137 green**, **production build green**.

- Unit 79 complete — **Frontend local timetable draft persistence.** The current unsaved timetable draft is now persisted in browser `localStorage` so draft work survives leaving `/timetable` or refreshing the browser. New dedicated helper `frontend/src/features/timetable/draftStorage.ts` owns the storage contract: a schema-versioned `StoredTimetableDraft` (`schemaVersion: 1`, `savedAssignmentFingerprint`, `updatedAt`, `assignments: TimetableAssignment[]`) under key `tts3.timetable.draft.v1`; `saveStoredDraft` / `clearStoredDraft` (best-effort, swallow quota/SSR/private-mode failures via a guarded `getStorage`); `computeSavedAssignmentFingerprint` (deterministic, order-independent `session_id:day:start_slot:room_id` join over the saved set); and `loadStoredDraft(currentFingerprint)` returning a discriminated `LoadDraftResult` — `none` / `restored` / `discarded` with reason `invalid` (bad JSON or missing required assignment fields), `schema-mismatch` (version ≠ 1), or `fingerprint-mismatch` (saved backend state changed underneath the draft). Every unsafe stored draft is cleared on load and reported so the UI can message it. Server-owned query data is never persisted (only the unsaved draft + minimal metadata); no version history; no backend sync until explicit save. A `TODO(multi-admin)` documents including the authenticated user ID / workspace key in the payload and key if multi-admin ever lands (single-admin in v1, not implemented). **Route wiring (`routes/timetable.tsx`):** a one-time `hydratedRef` gate restores at most once after saved assignments first load — on restore the draft is set from storage, `isDirty=true`, and `draftNotice='restored'` is shown; on `discarded` the draft initializes from saved backend state and `draftNotice='discarded'`; `none` initializes from saved silently. Subsequent saved-assignment refetches (after save or solver run) always re-init from saved state and never resurrect a stored draft (notice cleared). A hydration-gated persistence effect writes the draft whenever `isDirty` and clears storage once clean (after a successful save), so an empty dirty draft (Clear All) persists until saved. Restored assignments flow through the existing blocking auto-unschedule and warning-derivation effects unchanged. **Action bar (`TimetableActionBar.tsx`):** new `draftNotice`/`onDismissDraftNotice` props render a dismissible notice at priority 2.5 (after solver states, before blocking placement) — `Unsaved draft restored.` (accent, `CheckCircle2`) or `Old unsaved draft was discarded because saved timetable data changed.` (warning, `AlertTriangle`); the dismiss button now uses an optional per-message `onDismiss` (defaults to `onDismissSolver`); no browser-storage internals are shown. **Tests:** new `draftStorage.test.ts` (15 tests — fingerprint order-independence/change/empty, save+load round trip, empty-dirty persistence, restore on match, none, invalid-JSON discard, missing-field discard, schema-mismatch discard, fingerprint-mismatch discard, clear); new `routes/timetable.test.tsx` suite (8 tests — dirty write, remount restore + notice, refresh-seed restore, save clears storage, empty-dirty Clear All persistence, fingerprint-mismatch discard + notice, restored assignment cleaned by auto-unschedule, no resurrection after refetch); existing route tests get `localStorage.clear()` in setup/teardown. Frontend draft + timetable tests **green** (`units.test.tsx` showed only an environmental 5s-timeout flake under full-suite load — passes 9/9 in isolation), **production build green (strict tsc)**. No backend, API, validation-rule, or package dependency changes.

- Unit 75 complete — **Frontend management title cleanup and unit filters.** Student title is fully absent from all student UI (already removed in Unit 73); the lecturer title selector uses the final product list (`Mr`/`Ms`/`Mrs`/`Dr`/`Fr`/`A/Prof.`/`Prof.`) and old cached titles display as plain text without crashing the selector. **Subject filter added to `/students`**: a subject `FilterSelect` derives its options from loaded units via `parseUnitCode`; units with invalid/unknown subject prefixes never appear as options; a student matches if any enrolled unit parses to the selected subject. **Subject and year filters added to `/lecturers`**: a subject `FilterSelect` (derived from loaded units) and a year `FilterSelect` (fixed Year 1–3 list) both work against the existing `taughtUnitsByLecturer` derived map; a lecturer matches if any taught unit has the selected subject/year; units with invalid codes are ignored in both. **Subject filter added to `/units`**: a subject `FilterSelect` derived from loaded units filters the unit list using `parseUnitCode`; the existing year and lecturer filters are unchanged. All three filter functions updated: `StudentFilters`, `LecturerFilters`, `UnitFilters` each gained a `subject: string` field; `LecturerFilters` also gained `year: string`; `EMPTY_*_FILTERS` and `*FiltersActive` predicates updated; the pure `filter*` functions use `parseUnitCode` to match by prefix and year. **Tests**: `features/students/filters.test.ts` (11 tests, +5 for subject filter); `features/lecturers/filters.test.ts` (10 tests, +5 for subject and year filters); `features/units/filters.test.ts` (9 tests, +3 for subject filter); `routes/students.test.tsx` (+3: no-title-field, no-title-column, subject-options-from-valid-codes-only); `routes/lecturers.test.tsx` (+3: final-title-list, subject-options-from-valid-codes, year-filter-present). Frontend **147 tests green**, **production build green**. No backend, API, or package dependency changes.

- Unit 74 complete — **Frontend unit-code parser display, subject-based colours, and unit validation UX.** The unit create/edit forms now run `parseUnitCode` as the user types; the code field input is normalised to uppercase in real time. A valid code renders a compact success line (`History · Orange · Year 1`); a non-empty invalid code renders `Invalid unit code. Use three letters and three numbers, e.g. HIS101, with a supported subject prefix and year 1–3.`; empty shows nothing. The create/save button is disabled whenever `parseUnitCode(code).valid` is false, requiring a known subject prefix (HIS/PHI/THE/LIT/LAN/GRE/SCI) and year 1–3 in addition to the structural contract. Generic hash-based unit colouring has been fully replaced: `unitColors.ts` now exports `getSubjectTokens(unitCode)` (returning `UnitColorTokens = SubjectTokens` from the parser, or a stone fallback for invalid/legacy codes) and drops the old `getUnitColor` hash function. All card and group components (`ScheduledSessionCard`, `UnscheduledSessionCard`, `UnscheduledSessionCardPreview`, `UnitGroup`) accept `colorTokens: UnitColorTokens` instead of `colorVariant: UnitColorVariant`; the fixed lookup tables are removed. Consumers (`GridCell`, `UnscheduledPool`, `timetable.tsx` drag-preview) call `getSubjectTokens(unitCode)` instead of `getUnitColor(unitId)`. Subject colour tokens (`--subject-*-bg/border/text`) declared in Unit 73 now drive all card backgrounds, accents, and text; the `--unit-stone-*` tokens remain for the fallback only. Invalid legacy codes do not crash. New `unitColors.test.ts` (7 tests); `units.test.tsx` updated (1 revised + 5 new tests). Frontend **128 tests green**, **production build green**. No backend, API, or package dependency changes.

- Unit 72 complete - **more-features backend: title and unit-code contract cleanup.** Backend persistence/API contracts updated for the new title rules and defensive unit-code structural validation; this is a backend-only contract/migration unit (no subject parsing, labels, or colours — those stay frontend-only). **Student titles removed completely:** the `StudentTitle` enum and the `students.title` column are gone (not hidden/retained); `title` dropped from `StudentCreate`/`StudentUpdate`/`StudentResponse`, the embedded `StudentSummary` on `UnitResponse`, and `services/student.create_student`; no placeholder/empty title is returned. **Lecturer titles migrated** to the final product list (`Mr`, `Ms`, `Mrs`, `Dr`, `Fr`, `A/Prof.`, `Prof.`): the `LecturerTitle` enum now exposes exactly those values (member names `MR/MS/MRS/DR/FR/ASSOC_PROF/PROF`), punctuation retained only for `A/Prof.` and `Prof.`. Display sourcing (`services/assignment.py`, `services/session.py`) is unchanged — `lecturer.title.value` now renders without the trailing period for the remapped titles. **Unit-code structural validation:** new `_normalize_unit_code` helper in `schemas/unit.py` trims, uppercases, and enforces `^[A-Z]{3}\d{3}$` before persistence, then confirms the year level is derivable; both `UnitCreate.code` and `UnitUpdate.code` validators use it and return the normalized uppercase value, so the service's existing uniqueness check and `parse_unit_year_level` operate on the normalized code. The clear validation error is `Unit code must be three letters followed by three numbers, e.g. HIS101.` Service behavior is consistent: `his101`→`HIS101`, `phi201`→`PHI201`, duplicate checks use the normalized value, updating without changing the code never self-conflicts, and invalid-code updates fail at schema construction before any DB mutation. **Migration `0012_title_cleanup_and_unit_code_contract` (head now `0012`):** drops `students.title` and the `studenttitle` type; rebuilds the `lecturertitle` Postgres enum (rename-aside → recreate → recast via explicit `CASE` mapping `Dr.→Dr`, `Mr.→Mr`, `Ms.→Ms`, `Prof.`/`A/Prof.` unchanged → drop-old), adding `Mrs`/`Fr`. Unit-code structure is enforced in schemas/service (no DB constraint, no migration). Existing test fixtures updated to drop student `title`; three `PHI3A` unit codes (no longer valid `AAA999`) replaced with `PHI301`/`PHI201`. New `tests/test_72_title_and_unit_code_contract.py` (43 tests — student schema/response title removal, lecturer final-value enum + create/update acceptance + mapping/punctuation assertions, structural rejection of too-short/too-long/digits-first/letters-only/punctuation/blank codes, lower-case + whitespace normalization, normalized uniqueness, no self-conflict, invalid-update-before-mutation). Backend **314 tests green** (43 new); app import smoke green (`TTS3 API`); Alembic head **`0012 (head)`**. Breaking API change; frontend alignment is Unit 73. No new runtime dependencies.
- Unit 71 complete - **post-v1 regression, docs, and acceptance pass.** The full Units 58-70 adjustment batch is verified and documented. Two genuine regressions were fixed: `StudentUpdate.year_level` now enforces the documented 1-3 range (it still accepted 4-5), and saved `AssignmentResponse` DTOs now include the session-level `lecturer_id`, so lecturer overlap/availability warnings are correctly recomputed after refresh. Backend regressions now explicitly cover update-year validation, both sides of the shared `unit_students` relationship, assignment lecturer identity, and the database-backed solver snapshot consuming session lecturers plus `session_student_allocations`. A new Units page regression suite covers the wider modal, derived/invalid year feedback, default/search/year-filter student selection, teaching-team-restricted session lecturers, Lecture/Tutorial-only controls, and the 1-4 hour duration stepper. `project-overview.md`, `architecture-context.md`, `code-standards.md`, and the post-v1 build plan now reflect derived years, teaching teams, session lecturers, hidden allocations, retired unit-overlap warnings, draft-only Clear All, frontend-only filters, and hour-labelled integer durations. Created `docs/post-v1-adjustments-acceptance-flow.md` with the required 22-step manual PASS/FAIL checklist and recorded automated evidence. Verification: backend **271 passed** (one non-blocking pytest cache warning), frontend **100 passed** across 14 files, frontend production build **green** (existing large-chunk warning only), backend import smoke **green** (`TTS3 API`), and Alembic repository/current revisions both **`0011 (head)`**. The interactive browser checklist remains intentionally unchecked for a human run; no v2 features or new dependencies were introduced.
- Unit 70 complete - **post-v1 frontend: timetable action polish.** `TimetableActionBar` is now a token-styled sticky surface (`sticky top-4 z-30`) with responsive wrapping and an accessible live status region. Validation details keep a fixed-height scroll area while open so changing issue messages do not continually reflow the timetable. A `Clear all` action is disabled when the draft is empty, saving, or solver editing lock is active; it opens an existing-primitive confirmation dialog whose copy explains that only the current frontend draft is cleared until Save. Confirming sets the route draft to `[]`, marks it dirty, clears pending session selection and the current placement error, and immediately returns sessions to the unscheduled pool; cancel preserves the draft. The route does **not** import or call the backend `clearAssignments()` DELETE client. Clicking Save after a clear persists `{ assignments: [] }` through the existing explicit-save endpoint. The clean save button now reads `Saved`; dirty remains `Save Timetable`; loading remains `Saving...`. The idle solver button now reads `Generate Timetable`, keeps all existing gating and visible/title disabled reasons, and uses solver-blue tokens in enabled and disabled states; active runs retain the spinner and `Solving...` label. Route tests cover sticky/clean labels, dialog open/cancel, confirmed draft-only clear, return to the pool, no backend clear call, explicit empty save, solver disabled reason/title/styling, and Clear all's solver lock. Frontend **96 tests green** (3 new), **production build green**, `TimetableActionBar.tsx` and the updated timetable test pass ESLint; linting the route still reports only its pre-existing React `set-state-in-effect` finding. No backend, API, validation-rule, protected UI primitive, or package dependency changes.
- Unit 69 complete - **post-v1 frontend: unscheduled pool redesign.** The pool derives only from currently unscheduled sessions and now supports local search across unit code, unit name, and lecturer display name plus an All years/Year 1-3 filter. A pure pool view model includes `unit_year_level` bucket metadata (using the existing unit-code derivation as a frontend-only fallback because this unit does not change backend APIs), excludes empty buckets, sorts units by year/code/name, and sorts sessions by type then lecturer display name. Visible units render as compact token-styled boxes that wrap across the page; each header shows code/name once, an optional year badge, and remaining count, while cards stack vertically and show only session type, duration in hours, lecturer, and allocation-derived student count. Draggable IDs, selected state, click scheduling, validation behavior, and backend mutation boundaries are unchanged; `DragOverlay` reuses the card presentation. The timetable route supplies the total schedulable count so the pool distinguishes no schedulable sessions (existing `/units` action), all sessions scheduled (success state), and filters hiding every unscheduled session (clear-filter action). Filtering a selected session out also clears that pending selection so only visible pool sessions remain selectable. Tests cover grouping/sorting, all search fields, year filtering, all three empty/completion states, metadata de-duplication, clear filters, click selection, and hidden-selection clearing. Frontend **93 tests green**, **production build green**, and **Unit 69 files pass ESLint**; full-project lint still reports only pre-existing Fast Refresh/protected-UI and timetable effect findings outside this unit. No backend, validation-rule, or package dependency changes.
- Unit 68 complete — **post-v1 backend: solver allocation and session-lecturer integration.** Constraint mirror, snapshot builder, CP-SAT model, and result application service now all derive session student membership from `session_student_allocations` (not `unit.students`) and use `session.lecturer_id` as the authoritative lecturer for every conflict, availability, and capacity decision. **Constraint graph** (`constraints/graph.py`): `build_conflict_graph` no longer calls `derive_unit_session_overlap_conflicts`; unit/session overlap is retired as an active constraint edge — per-session student overlap (from allocations) subsumes it. `ConstraintType.UNIT_SESSION_OVERLAP` is retained in the enum for backward compatibility with the `compile_assignment_violations` diagnostic and pre-v2 test fixtures but is marked deprecated. The `derive_unit_session_overlap_conflicts` and `check_unit_session_overlap` helpers remain available for diagnostic/test use. **Snapshot builder** (`solver/snapshot.py`): `build_solver_input_snapshot` was rewritten to load sessions via `select(Session).options(selectinload(Session.unit))` filtered to schedulable (non-null `lecturer_id`), then fetch all `SessionStudentAllocation` rows for those sessions in one query; `SessionSnapshot.student_ids` is now the allocated-group frozenset, not all unit students. The `_validate_saved_assignments` integrity check automatically uses allocation-derived `student_count` for capacity validation. **Result application** (`solver/apply.py`): `_validate_generated` capacity check now queries `SessionStudentAllocation` counts for the generated session IDs instead of loading `session.unit.students`; the `Unit` import and `selectinload(Session.unit).selectinload(Unit.students)` eagerly-loaded chain were removed. **Solver model** (`solver/model.py`): unchanged — still merges all three conflict pair types and uses `session.student_count` for room capacity filtering; model correctness already flows from the updated snapshot inputs. Existing tests updated: `test_constraint_and_solver_suite.py` (`test_conflict_graph_edges_cover_each_category`, `test_snapshot_builder_compiles_real_format_data`, and `test_solver_unit_session_overlap_forces_partial_result` renamed to `test_solver_same_unit_no_conflict_post_v1` with inverted assertion — two same-unit sessions with no shared students and different lecturers now both schedule); `test_constraints.py` (`test_conflict_graph_conflicts_between` updated to assert `UNIT_SESSION_OVERLAP not in types`); `test_snapshot.py` (`test_unit_session_conflict_pairs_same_unit` updated to assert empty); three capacity-checking tests in `test_apply.py`, `test_solver_job.py`, and `test_solver_api.py` each received a `rebalance_unit_session_allocations` call so allocation rows exist before the assertion. New `tests/test_68_allocation_solver.py` (8 tests — same-unit different-lecturers no lecturer-conflict, same lecturer generates conflict, disjoint tutorial students no student-conflict, lecture+tutorial shared students conflict, room capacity uses allocation size not unit enrollment, disjoint tutorials with different lecturers can overlap simultaneously in separate rooms, solver respects session-level lecturer availability, locked assignment rejected by allocation-derived capacity). Backend **266 tests green** (8 new). No frontend or new package changes (per spec scope).
- Unit 67 complete — **post-v1 frontend: timetable validation now judged against session-level lecturers and hidden session-student allocations.** Manual scheduling keeps its frontend-owned, explicit-save, blocking-vs-warning workflow (warning-invalid saves still allowed), but conflicts and capacity are now measured against the **actual students allocated to each session** rather than inferred unit membership — confined to `frontend/src/lib/validation` and `frontend/src/features/timetable`, no backend/solver change (per spec scope). **Model** (`features/timetable/assignment.ts`): `TimetableAssignment` made `allocated_student_ids: string[]` **required** (both the schedulable-session and saved-assignment DTOs supply it) and added optional `unit_year_level?: YearLevel`; `lecturer_id?` stays **optional** because the saved `AssignmentResponse` does not carry a session-level lecturer id (only `lecturer_display_name`), so loaded assignments have no id until re-sourced from the pool. **Conversion** (`routes/timetable.tsx` `toTimetableAssignment`): copies `allocated_student_ids` from the assignment response (no `lecturer_id` available there). **Placement** (both `handleCellClick` and `handleDragEnd`): build the draft assignment from the `SchedulableSession`'s session-level `lecturer_id`, `allocated_student_ids`, and `unit_year_level` — students are never inferred from unit membership on the frontend. **Blocking** (`lib/validation/blocking.ts`): unchanged in rules — room capacity already used `assignment.student_count`, and the auto-unschedule effect already refreshes counts from the schedulable pool's allocation-derived `student_count`; double-booking / capacity / crossing-lunch / off-timetable all preserved. **Warnings** (`lib/validation/warning.ts`, rewritten): lecturer overlap now compares session-level **`lecturer_id`** (guarded truthy so two sessions on units that merely share a teaching team do **not** conflict unless the same lecturer teaches both, and undefined ids never match); lecturer availability looks the lecturer up by **`lecturer_id`** (sessions without an id are skipped); **student overlap** is the non-empty intersection of the two sessions' `allocated_student_ids` (so a lecture + tutorial of the same unit conflict only through shared students, and two tutorials of the same unit with disjoint groups can overlap); the independent **`unit_session_overlap`** warning type was **removed** (shared allocated students subsume it). Warning objects stay structured/human-readable and the student-overlap message says sessions "share students … at the same time" **without revealing hidden tutorial allocation groups** (`affected_student_ids` carries the intersection internally but is never displayed). **Loaded warning-invalid assignments**: warnings are recomputed live from draft data on every render (`checkDraftForWarnings(draft, lecturers)`), warning-invalid placements stay visible in the draft, and solver gating still blocks while any blocking **or** warning issue exists (unchanged `solverDisabledReason`). Tests: `lib/validation/warning.test.ts` rewritten for the allocation model (11 tests — clean draft, lecturer overlap by id, no-overlap for same-team-different-lecturer, lecture+tutorial student intersection with non-revealing message, disjoint tutorials clean, removed unit/session overlap, different-day clean, availability by id, free-slot clean, missing-lecturer-id skipped, `getWarningSessionIds`); `test/fixtures.ts` `makeAssignment` adds `allocated_student_ids: []`; the `routes/timetable.test.tsx` solver-gating warning case now triggers via shared `allocated_student_ids` (the saved DTO's real mechanism) instead of a shared lecturer display name. No new package dependencies, no `any`. Frontend **strict tsc green**, **production build green**, **83 frontend tests green** (79 prior + 4 net new).
- Unit 66 complete — **post-v1 frontend: client-side search and filters on the management pages (`/rooms`, `/students`, `/lecturers`, `/units`).** Adds usability-only filtering over the currently loaded TanStack Query data with **no backend search endpoints, no query params, and no filter persistence** (per spec scope). Three small reusable primitives in `frontend/src/components/filters/`: `SearchInput` (icon + accessible `aria-label`ed search field), `FilterSelect` (labelled shadcn `Select` wrapper over `{value,label}` options), and `FilterBar` (flex layout that renders a `Clear filters` action only when any filter is active). Pure, testable filter functions live per domain: `features/rooms/filters.ts` (search by name only + room-type filter), `features/students/filters.ts` (search title/first/last + year + enrolled-unit via `student.units`), `features/lecturers/filters.ts` (search title/first/last + taught-unit via the derived `taughtUnitsByLecturer` map), and `features/units/filters.ts` (search code/name + derived `year_level` + teaching-lecturer via `unit.lecturers`); each exports an `EMPTY_*_FILTERS`, a `*FiltersActive` predicate, and a pure `filter*` function. Each route renders a `FilterBar` above its table only once data has loaded and is non-empty (so loading/error and true-empty states are never hidden), filters the loaded list via `useMemo`, and distinguishes **`No records yet`** (true empty data) from **`No records match the current filters`** (empty match). Create/edit/delete behavior is unchanged; no Zustand (filters are local route state); no new package dependencies, no `any`. Tests: pure filter-function unit tests per domain with real-shaped fixtures covering every filter type (`features/{rooms,students,lecturers,units}/filters.test.ts` — 23 new: rooms 6, students 6, lecturers 5, units 6). Frontend **strict tsc green**, **production build green**, **79 frontend tests green** (56 prior + 23 new).
- Unit 65 complete — **post-v1 frontend: lecturer teaching visibility on `/lecturers`.** The lecturer page now shows which units each lecturer teaches **read-only** — the teaching relationship stays owned by the Units page (no backend/API change, no lecturer-side teaching edits, per spec scope). Because the backend `LecturerResponse` carries no taught-unit data (confirmed Unit 62), visibility is **derived on the frontend** from the units list: `/lecturers` now runs `useQuery(['units'], listUnits)` alongside `['lecturers']` and builds a `taughtUnitsByLecturer: Map<lecturerId, Unit[]>` by walking each `unit.lecturers`. Reading the same `['units']` cache means a teaching-team change made on `/units` (which already invalidates `['units']`) refetches here automatically — satisfying the cross-page query-invalidation requirement with no extra wiring. **Lecturer table** gained a "Units taught" column (colSpan updated 4→5) rendered by a new `TaughtUnitsCell`: `No units assigned` (muted) for none; up to `MAX_VISIBLE_TAUGHT_UNITS` (3) token-styled `UnitChip`s showing the unit **code** (sorted by code) with the full `CODE — Name` on the native `title` tooltip; any remainder collapses into a single `+N more` chip whose `title` lists every overflow unit's `CODE — Name`. The cell also shows `…` while units load and `Unavailable` if that query errors (lecturer rows still render). **Edit modal**: `LecturerFormFields` takes an optional `taughtUnits?: Unit[]` and, when provided (edit mode only), renders a **read-only** "Units taught" summary (full `CODE — Name` chips, or `No units assigned`) plus the helper text **"Teaching assignments are managed from Units."** No editable unit fields were added; the lecturer create/edit mutation shape is unchanged and submits **no** unit IDs (`updateLecturer` still sends only `title`/`first_name`/`last_name`). Units page was already spec-compliant from Unit 63 (Teaching team column + `lecturer_ids`-only editing in its modal) and was left untouched. New `routes/lecturers.test.tsx` (4 tests — taught-unit codes + empty state, `+N more` collapse, read-only summary with helper text and **no** edit controls/checkboxes in the modal, and that saving an edit sends no unit IDs); reuses `makeLecturer`/`makeUnit` fixtures. No new package dependencies, no `any`. Frontend **strict tsc green**, **production build green**, **56 frontend tests green** (52 prior + 4 new).
- Unit 64 complete — **post-v1 frontend: student enrolment management on `/students`.** The admin can now view and edit a student's enrolled units directly from `/students`, using the exact same unit-student relationship the `/units` page writes to (no second enrolment model, no Zustand enrolment state, no backend schema change per spec scope). **Student table** gained an "Enrolled units" column rendering `student.unit_count` as `0 units` / `1 unit` / `N units` (muted when zero); row actions and `colSpan` updated to 6. **Create/edit modal** (`routes/students.tsx`) now embeds a unit selector: `StudentFormState` adds `unit_ids: string[]` + `unitSelectionTouched: boolean`; the year-level selector offers Year 1–3 only (was 1–5); on **create**, while the selection is untouched, choosing/changing the year level default-selects all units whose derived `year_level` matches (any manual checkbox toggle or the "Select all Year X units" action sets `unitSelectionTouched` so the choice is never silently overwritten); **edit** loads `student.units` as the initial selection and treats it as already chosen. The selector mirrors the `/units` student picker: search by unit code/name, an All-years/Year 1–3 filter, a scrollable checkbox list, a live "N selected" count, and a derived "Select all Year X units" secondary action — tutorial allocations / session-level data are never shown. **Persistence**: because there is no student-side enrolment endpoint, a new module helper `reconcileEnrolment(studentId, desiredUnitIds, currentUnitIds, allUnits)` diffs desired vs current enrolment and writes the changes through the units endpoint (`updateUnit(unitId, { student_ids })`, partial update — leaves teaching team/sessions untouched), computing each affected unit's new `student_ids` from the cached `unit.students` ± this student; failures propagate to the mutation (no partial hiding). Create reconciles against the backend's auto-enrolled `created.units`; edit against `student.units`. Both create and edit (and delete) mutations invalidate `['students']`, `['units']`, `['schedulable-sessions']`, and `['assignments']` so the `/units` page and timetable-dependent data stay in sync after refetch (and vice-versa). New `makeStudent`/`makeUnit` test fixtures; new `routes/students.test.tsx` (3 tests — enrolled-unit count display, create defaults to matching-year units via the year selector, edit add/remove persisted to the units endpoint). No new package dependencies, no `any`. Frontend **strict tsc green**, **production build green**, **52 frontend tests green** (49 prior + 3 new).
- Unit 63 complete — **post-v1 frontend: `/units` page redesign for the new scheduling model.** The `/units` create/edit flow now manages derived year levels, multi-lecturer teaching teams, year-aware student selection, and per-session lecturers — confined to `frontend/` with no backend/API/solver change (per spec scope). New pure helper `frontend/src/features/units/yearLevel.ts` (`parseUnitYearLevel`) mirrors the backend `services/year_level.py` parser: first digit of the trimmed code must be 1/2/3 → `{ ok: true, year }`, otherwise `{ ok: false, error }`; UX-only, backend stays authoritative (6 unit tests in `yearLevel.test.ts`). `routes/units.tsx` rewritten: the create/edit dialog is widened to `sm:max-w-4xl` with a two-column layout (left = identity + teaching team; right = students + sessions) that stacks on narrow screens via `md:grid-cols-2`, all token-styled, no `components/ui/*` edits. **Year level**: displayed read-only as "Year N" derived from the code (never an editable field); an invalid/empty code shows an inline `AlertTriangle` error and blocks create/save through `isFormValid` (which now requires a valid parsed year). **Student selector**: form state keeps `student_ids: string[]` plus a `studentSelectionTouched` flag; a search input + year-level filter (`All years`/`Year 1–3`) live in the selector; on create, while untouched, the default selection auto-tracks the derived year (changing the code re-selects all matching-year students); any manual toggle sets `studentSelectionTouched` so later code edits never clobber the choice; a small "Select all Year X students" secondary action is always available (and marks the selection touched). Edit mode loads the unit's real students and treats the selection as already chosen so a code edit can't silently replace it. **Teaching team**: `lecturer_id` replaced by `lecturer_ids: string[]` via a checkbox list with a live "N selected" count; at least one lecturer is required to save; selected lecturers render in a new "Teaching team" unit-table column (and a "Year" column was added). **Sessions**: inline boxes keep add/delete; type selector is Lecture/Tutorial only; each session has a lecturer `Select` restricted to the unit's selected team; `makeSession` defaults a new session to the sole team lecturer when exactly one exists, otherwise blank; if the team changes and a session's lecturer is no longer valid (or unset), the box shows an inline error and `sessionLecturerInvalid` blocks save until reassigned; duration is now a stepper (Minus/number/Plus, min 1, max 4, `hour`/`hours` label) persisting the same integer field. Create/edit mutations send `lecturer_ids` and per-session `lecturer_id` (`s.lecturer_id || null`), and on success invalidate `['units']`, `['lecturers']`, `['unit-sessions', id]`, `['schedulable-sessions']`, and `['assignments']`; delete invalidates the same set minus the per-unit sessions key. No new package dependencies, no `any`. Frontend **strict tsc green**, **production build green**, and **49 frontend tests green** (43 prior + 6 new).
- Unit 62 complete — **post-v1 frontend: API type alignment for the new scheduling model.** Frontend API clients and shared DTO types now compile against the post-v1 backend (Units 58–61) without redesigning any route (route redesign is Unit 63). Changes are confined to `frontend/src/lib/api/` + `features/timetable/assignment.ts`, with only the minimal route/component edits required to keep strict TypeScript passing. **`students.ts`**: introduced the centralized `YearLevel = 1 | 2 | 3` union (single source of truth, reused by unit/session DTOs) and a `UnitSummary` type mirroring the backend `EnrolledUnitSummary`; `Student.year_level` narrowed to `YearLevel`; `Student` gained `units: UnitSummary[]` + `unit_count: number`. No `unit_ids` input was added — the backend manages enrolment server-side (auto by matching year) and exposes no student-side enrolment request shape. **`units.ts`**: `Unit` dropped the single `lecturer`/`lecturer_id` in favour of `lecturers: LecturerSummary[]` and gained derived `year_level: YearLevel`; `UnitCreate`/`UnitUpdate` replaced `lecturer_id` with `lecturer_ids: string[]` (create requires ≥1 server-side; `year_level` is never a client input); `StudentSummary.year_level` narrowed to `YearLevel`; invalid unit-code/year-level parser failures continue to surface through the existing 422-detail path. **`sessions.ts`**: `SessionType` reduced to `lecture | tutorial`; `Session` gained nullable `lecturer_id` + `lecturer: LecturerSummary | null`; `SessionCreate`/`SessionUpdate` accept optional `lecturer_id`; `SchedulableSession` gained `allocated_student_ids: string[]` (internal validation payload, never displayed) and optional `unit_year_level?: YearLevel` (optional because the backend `SchedulableSessionResponse` does not yet emit it — flagged for the later validation/backend unit). **`assignments.ts`**: `AssignmentResponse` gained `allocated_student_ids: string[]`; session-level lecturer is already carried via `lecturer_display_name` (backend does not return `lecturer_id` on assignments); the save request shape is unchanged. **`lecturers.ts`**: unchanged — the backend `LecturerResponse` does not return taught-unit summaries, and Unit 61 introduced no frontend/DTO change. **`features/timetable/assignment.ts`**: `TimetableAssignment` gained optional `lecturer_id?` and `allocated_student_ids?` (rendering behavior unchanged until Unit 67). Minimal compile-keeping edits: `units.tsx` retains its single-lecturer selector but adapts at the API boundary (`lecturer_ids: [id]`, reads `unit.lecturers[0]`) and drops Lab/Workshop from its session-type dropdown; the timetable session-type label maps (`ScheduledSessionCard`, `UnscheduledSessionCard`) and sort order (`UnscheduledPool`) drop `lab`/`workshop`; test fixtures add `allocated_student_ids: []`. No `any` introduced, no new packages. Frontend **production build green (strict tsc)** and **43 frontend tests green**.
- Unit 61 complete — **post-v1 backend: lecturer availability replace-all save hardening.** `services/lecturer.set_availability` no longer mutates the `unavailable_slots` ORM relationship (the `.clear()`/`.append()` path was leaving stale rows and tripping the `(lecturer_id, day, slot)` unique constraint on repeated edits). It now performs a transactional full-set replace: validate the lecturer exists (structured 404); deduplicate identical day/slot pairs in the request **silently** (duplicates are dropped, not rejected — this replaces the old `duplicate_availability_entry` 422); then, in one transaction on the request session, issue a direct `delete(LecturerAvailability).where(lecturer_id == …)`, `flush()` the delete, `add()` the new rows, and `commit()`. Day/slot values remain enum-validated by Pydantic at the request boundary (invalid values → structured 422 before the service runs). Unexpected persistence failures `rollback()` and surface the existing structured error shape (`AppError("availability_save_failed", …, 500)`). After commit the relationship is reloaded via `db.refresh(lecturer, attribute_names=["unavailable_slots"])` so the response reflects the final saved state. The `lecturer_availability` table, its unique constraint, and the availability API/UI are unchanged (no migration, no schema change, no frontend change per spec scope). Repeated saves with different/overlapping sets work, an empty list clears all blocked slots, and other lecturers' rows are untouched. Tests: new `tests/test_lecturer_availability.py` (9 tests — create from empty, replace with a different set, overlapping replace with no unique conflict, clear via empty list, clear-when-already-empty, save same set twice idempotent, duplicate request entries dedupe, missing-lecturer 404, other lecturers untouched). Backend **258 tests green** (9 new). No new package dependencies (per spec scope).
- Unit 60 complete — **post-v1 backend: session types and hidden session-student allocations.** Session types are reduced to only `lecture` and `tutorial`, and a hidden, system-owned `session_student_allocations` table now records which students attend which session (lectures include every enrolled unit student; tutorials evenly divide enrolled students, each student in exactly one tutorial). There is no API route to view or edit allocations — they are internal validation/solver data only and the UI must never display tutorial membership. New model `SessionStudentAllocation` (`models/session_allocation.py`; FK→`sessions.id` and `students.id` both cascade-delete, unique `(session_id, student_id)`, indexes on each FK); `Session` gains an `allocations` relationship (`cascade="all, delete-orphan"`, `passive_deletes`) and its `SessionType` enum drops `lab`/`workshop`. New service `services/session_allocation.py` — `rebalance_unit_session_allocations(db, unit_id)` is the sole writer: it reconciles lecture allocations (every enrolled student) and tutorial allocations (deterministic, stable, minimal-movement even split — dedup existing valid placements, place unassigned into the smallest group, then move only the surplus needed to reach base/base+1 group sizes; never truly random) by diffing a computed target membership against existing rows; plus read helpers `allocation_counts` / `allocated_student_ids`. The function flushes within the caller's transaction and does **not** commit (atomicity: data mutation + allocation refresh succeed or fail together). Trigger points wired: `create_unit` (always) and `update_unit` (when `student_ids` changes); `create_student` (rebalances every auto-enrolled matching-year unit) and `delete_student` (captures affected unit ids, then rebalances after cascade removes the student's rows); `create_session` (always), `update_session` (when `session_type` changes), `delete_session` (rebalances the unit so a removed tutorial's students redistribute). Response shapes: `SchedulableSessionResponse` and `AssignmentResponse` now derive `student_count` from allocation rows and expose `allocated_student_ids: list[str]` (internal validation payload only); `list_schedulable_sessions`, `list_assignments`, and `save_assignments` use the new count helpers. Defensive capacity checks in `save_assignments` now use the allocated per-session count (lecture = enrolled unit count, tutorial = allocated group size) instead of total unit enrolment, so a tutorial fits a room sized to its group and zero-student sessions stay valid/schedulable. Migration `0011_session_allocations_and_session_types` (head now `0011`): creates the allocations table (+ unique constraint + both FK indexes), remaps any existing `lab`/`workshop` sessions to `tutorial`, then rebuilds the `sessiontype` Postgres enum down to `lecture`/`tutorial` (rename-aside → recreate → recast-via-text → drop-old). Solver modeling is intentionally unchanged (Unit 68 consumes the allocations later) per spec scope. Tests: new `tests/test_session_allocations.py` (17 tests — enum reduction, lecture allocation incl. zero-student, tutorial exactly-once + even balance + no-tutorials, idempotent/stable rebalance, add-student/add-tutorial minimal movement, remove-tutorial reassignment, student create/delete triggers, session-type-change trigger, schedulable + assignment DTO counts, zero-student schedulability, capacity uses group size). Backend **249 tests green** (17 new). No frontend or new package changes (per spec scope).
- Unit 59 complete — **post-v1 backend: unit teaching team and session-level lecturer.** Lecturer ownership moved from one lecturer per unit to a `unit_lecturers` teaching team plus a per-session `Session.lecturer_id`; all schedulability, display, and conflict sourcing now use the session lecturer. Migration `0010_unit_lecturers_team_and_session_lecturer` (head now `0010`) creates the `unit_lecturers` join table (composite PK ⇒ unique `(unit_id, lecturer_id)`, both FKs cascade), populates it from each `units.lecturer_id`, adds nullable `sessions.lecturer_id` (FK → `lecturers.id`), backfills it from each session's parent-unit lecturer, then drops `units.lecturer_id`. `sessions.lecturer_id` is intentionally left **nullable**: a session may exist without a lecturer (then it is simply not schedulable) — required so the optional-lecturer schema and schedulable-exclusion logic remain meaningful. Models: `Unit` drops `lecturer_id`/`lecturer`, gains a `lecturers` m2m via `unit_lecturers`; `Session` gains `lecturer_id` + `lecturer` relationship (lazy selectin). Schemas: `UnitCreate`/`UnitUpdate` replace `lecturer_id` with `lecturer_ids` (create requires ≥1; update may replace the team, ≥1 if supplied); `UnitResponse` now exposes `lecturers: LecturerSummary[]` (no more single `lecturer`/`lecturer_id`); `SessionCreate`/`SessionUpdate` accept optional `lecturer_id`; `SessionResponse` adds `lecturer_id` + `lecturer: LecturerSummary | None`. Services: `create_unit`/`update_unit` validate every lecturer id and persist the team, and `update_unit` rejects removing a lecturer still assigned to one or more of the unit's sessions with a structured `422` (`lecturer_still_assigned`) instead of silently unsetting; `create_session` resolves the lecturer (explicit ⇒ must be in team; omitted + exactly one team lecturer ⇒ auto-assign; omitted + multiple ⇒ reject `lecturer_required`) and rejects out-of-team lecturers (`lecturer_not_in_team`); `update_session` validates a new lecturer belongs to the team; `list_schedulable_sessions` now filters on `Session.lecturer_id IS NOT NULL` and uses `session.lecturer` for display. Assignment responses (`services/assignment.py`) now source `lecturer_display_name` from `session.lecturer`. The solver snapshot **DB loader** (`solver/snapshot.py`) was mechanically updated to read `session.lecturer_id` per session and skip lecturer-less sessions — the CP-SAT model and the pure `build_snapshot_from_data` builder are unchanged (full session-lecturer solver integration remains scheduled for Unit 68 per its spec). Tests: new `tests/test_unit_team_and_session_lecturer.py` (17 tests — team create/update validation, the removal-guard, session defaulting/membership for create & update, schedulable exclusion + session-lecturer display, assignment display sourcing); existing solver/apply/job/year-level fixtures updated to the new team + session-lecturer shape. Backend **232 tests green** (17 new). No frontend, CP-SAT model, or new package changes (per spec scope).
- Unit 58 complete — **post-v1 backend foundation: derived unit year levels and enrolment sync.** Units now derive a stored `year_level` from the first digit of their code (must be 1/2/3), students are restricted to years 1–3, and new-student/new-unit enrolment is synced through the existing `unit_students` join table (no second enrolment model). New: pure domain helper `backend/services/year_level.py` (`parse_unit_year_level`, `InvalidUnitCodeError`). Model changes: `Unit.year_level` (NOT NULL) + `ck_unit_year_level` check, `Student` gains a `ck_student_year_level` check and a `units` back-relationship over `unit_students`. Schemas: `UnitCreate`/`UnitUpdate` validate the code through the parser and never accept `year_level` from the client (`student_ids` default changed to `None` to distinguish "omitted" from explicit `[]`); `UnitResponse` adds `year_level`; `StudentResponse` adds `units` (new `EnrolledUnitSummary`) and a computed `unit_count`; student year validation tightened from 1–5 to 1–3. Services: `create_unit` derives/stores the year and, when `student_ids` is omitted, defaults to all students in that year (explicit list respected verbatim); `update_unit` recomputes the year on code change without replacing the student list; `create_student` auto-enrols into all matching-year units in the same transaction; `update_student` preserves enrolments (year change never silently drops memberships). Migration `0009_unit_year_level_and_year_constraints` adds the column, backfills by parsing each existing code (fails loudly on an invalid first digit), enforces NOT NULL, and adds both check constraints (head is now `0009`). Query-invalidation expectations for later frontend units documented in `api/units.py`. Tests: new `tests/test_year_level_and_enrolment.py` (parser success/failure, schema validation, unit-create year derivation + default/explicit enrolment, student-create sync, update preservation, DB + schema year constraints, response shapes); existing solver test fixtures updated to set `year_level`. Backend **215 tests green** (30 new). No frontend, solver, or new package changes (per spec scope).
- Unit 57 complete — final v1 scope guard and hardening pass (release-candidate verification). Full-app scope/architecture/UX/safety review only; **no v1 defects were found, so no corrective code changes were required** — the only change in this unit is this tracker update. The Scope Guard Checklist and the Verification Checklist both fully pass, and backend **185 tests**, frontend **43 tests**, and the frontend production build are all green. Result: **hardened v1 release candidate**. (Full breakdown in the Completed section below.)
- Unit 56 in progress — Trigger.dev production wiring for the async solver job. Resolved the production execution gap: a deployed Trigger.dev worker is a Node-only container with no Python/backend/DB, so the local `python -m solver.job_cli` spawn bridge cannot run in production. Added an HTTP execution bridge — the `solver-job` task now calls the deployed backend's new internal endpoint `POST /solver/internal/execute` (authorized by the shared `SOLVER_INTERNAL_TOKEN` as a Bearer token, NOT a Supabase admin JWT; fails closed with 503 when unset, constant-time compare) when `SOLVER_EXECUTE_URL` is set, and falls back to the local Python spawn bridge for dev when it is not. The endpoint runs `run_solver_job`, so all solver business logic and result application stay in the backend; status flows back to the frontend through the existing `SolverRun` row + `GET /solver/status/{id}` polling. Config: `solver_internal_token` in `backend/config.py`; `require_internal_token` dependency in `backend/auth/deps.py`; `SolverExecuteRequest` schema; `execute_solver_run` service. Worker: `jobs/src/trigger/solverJob.ts` refactored into `runViaBackendHttp` / `runViaLocalBridge` / `logFinalResult` with a mode-tagged start log. Docs: new `docs/trigger-dev-deployment.md` (execution model, secrets, manual steps, smoke test, failure-safety, logs, checklist); `docs/railway-deployment.md`, `jobs/README.md`, `jobs/trigger.config.ts`, and both `.env.example` files updated. Verified: backend import smoke, jobs `tsc --noEmit`, and 185 backend tests (5 new for the token guard + execute delegation) all green. Remaining items are manual (create the Trigger.dev production env, generate the shared secret, set `SOLVER_INTERNAL_TOKEN`/`TRIGGER_SECRET_KEY` on Railway and `SOLVER_EXECUTE_URL`/`SOLVER_EXECUTE_TOKEN` on Trigger.dev, `trigger.dev deploy`, run the production smoke + failure-safety checks) and cannot be performed from the repo
- Unit 55 in progress — backend Railway deployment config and documentation prepared in-repo (`backend/railway.json` with Nixpacks build + uvicorn `$PORT` start command + `/health` healthcheck; `docs/railway-deployment.md` covering env vars, the deliberate Alembic migration process, CORS rules, security, manual steps, and the verification checklist); pinned `python-dotenv` in `backend/requirements.txt` because `alembic/env.py` imports it (otherwise production migrations fail in a clean env); backend app import + route smoke (`/health`, `/auth/verify`) and Alembic head (`0008`) re-verified locally. Remaining items are manual (create the Railway service, set Root Directory to `backend`, configure server env vars, deploy, run `alembic upgrade head` deliberately, record the deployed URL, point Vercel `VITE_API_BASE_URL` at it, run health/auth/CORS checks) and cannot be performed from the repo
- Unit 54 in progress — frontend Vercel deployment config and documentation prepared in-repo (`frontend/vercel.json` with Vite build + SPA rewrites; `docs/vercel-deployment.md` covering env vars, security rules, and the verification checklist); production build re-verified green locally. Remaining items are manual (create the Vercel project, set Root Directory to `frontend`, configure public env vars, deploy, record the deployed URL, run the redirect/auth checks) and cannot be performed from the repo
- Unit 53 complete — full v1 acceptance pass executed and documented (`docs/v1-acceptance-flow.md`); all 23 acceptance steps PASS via live pipeline + automated coverage; backend 180 tests, frontend 43 tests, production build, and backend app-import smoke all green; no v1-blocking defects; follow-ups recorded (fresh interactive click-through, production Trigger.dev deploy, bundle code-splitting, pre-existing lint warnings)
- Unit 52 complete — frontend timetable validation and interaction test suite (Vitest + React Testing Library; pure validation-helper tests, presentational component tests, and a TimetablePage integration suite covering draft state, manual scheduling, save behavior, solver gating, and automatic unscheduling; 43 frontend tests)
- Unit 51 complete — backend constraint and solver test suite (dedicated fixture-driven suite covering the constraint mirror, conflict graph, snapshot builder, CP-SAT solver, result application, and failure safety; 36 new tests, 180 backend tests total)
- Unit 50 complete — frontend error handling and observability (Sentry + app error boundary + standardized timetable/solver error states)
- Unit 49 complete — backend observability (Sentry + correlation IDs + structured logs)
- Unit 48 complete — frontend async solver integration
- Unit 47 complete — frontend solver API client
- Unit 46 complete - backend solver start and status API
- Unit 45 complete — async solver job
- Unit 44 complete — jobs boundary and Trigger.dev setup
- Unit 43 complete — backend solver result application service
- Unit 42 complete — backend solver CP-SAT module
- Unit 41 complete — backend solver input snapshot builder
- Unit 40 complete — backend solver constraint mirror
- Unit 39 complete — frontend drag-and-drop save integration
- Unit 38 complete — frontend drag-and-drop scheduling shell
- Unit 37 complete — frontend validation display and solver gate shell
- Unit 36 complete — frontend warning validation engine
- Unit 35 complete — frontend blocking validation engine
- Unit 34 complete — frontend manual scheduling controls
- Unit 32 complete — frontend assignment API client
- Unit 31 complete — backend saved timetable assignment persistence and protected save API
- Unit 30 complete — frontend scheduled session rendering shell
- Unit 29 complete — frontend unscheduled pool integration (real schedulable-session data)
- Unit 28 complete — frontend unscheduled pool shell
- Unit 27 complete — frontend session management integration
- Unit 26 complete — frontend session API client
- Unit 25 complete — backend session persistence and protected API
- Unit 24 complete — frontend unit page integration
- Unit 23 complete — frontend unit API client
- Unit 22 complete — backend unit persistence and protected API
- Unit 21 complete — frontend unit and session management shell
- Codebase consistency fixes applied (slots, architecture invariants, unit field naming)

## Current Goal

- Unit 88 (timetable blocks regression/docs/acceptance pass) is now complete, closing the **84–88 "timetable blocks"** batch. Backend **358**, frontend **259**, and the frontend production build are all green; Alembic head `0013`; scope guard passes; `docs/timetable-blocks-acceptance-flow.md` records the 18-step manual checklist + automated evidence.
- The descriptive context files (`project-overview.md`, `architecture-context.md`, `ui-context.md`, `code-standards.md`) are aligned to the full **84–88 "timetable blocks"** batch decisions (room-specific block groups/cells, unnamed grey lock rendering, named gold/light-blue/light-pink rendering, `timetable_slot_blocked` frontend blocking rule, solver snapshot blocked cells, backend defensive save rejection). No further timetable-blocks units are queued.
- No new implementation batch is currently specced. Standing operational items remain: run the unchecked browser checklists in `docs/timetable-blocks-acceptance-flow.md` and `docs/more-features-acceptance-flow.md` against a deployed or local full stack, deliberately apply `alembic upgrade head` (now `0013`) on the deployed database, complete the manual Vercel/Railway/Trigger.dev steps, and address the frontend bundle code-splitting warning when prioritized.

## Completed

- **Unit 106: Timetable Action Bar — Save State, Download Lock, Message Clearing** (frontend-only)
  - **Save settles after success**: `saveMutation.onSuccess` now marks the draft clean directly (`setIsDirty(false)` + clears `saveError`) instead of waiting on the assignments refetch, so the Save button drops out of its busy state to the idle "Saved" label immediately and is reusable on the next edit. The busy/disabled state stays driven purely by the mutation lifecycle (`isPending`), so it can never stick spinning after a settled save.
  - **Download locked while dirty**: confirmed and preserved the Unit 94 gate — `downloadDisabledReason` returns `"Save timetable changes before downloading."` whenever `isDirty`, in addition to save-in-flight / solver-running / saved-data-loading-or-failed / export-running reasons.
  - **Message clearing (replace transient, keep clashes)**: consolidated the previously independent one-time notices (block edit/delete/create + download) into a single `notice: TransientNotice | null` route state, so a newer transient notice replaces the previous one instead of stacking behind it. A new save or solver run also clears any lingering notice. Blocking/warning validation summaries were split out of the single-message cascade into a **persistent `clashMessage`** line in `TimetableActionBar`, rendered beneath the transient line so clash/conflict warnings stay visible alongside a transient notice and clear only when validation stops reporting them. Action bar min-height (Unit 77) unchanged.
  - Files: `frontend/src/features/timetable/TimetableActionBar.tsx` (new `TransientNotice`/`TransientNoticeTone` exports, `noticeAppearance` helper, `getPrimaryMessage` + separate `clashMessage`, two-line message layout), `frontend/src/routes/timetable.tsx` (save mark-clean, consolidated `notice` state + setters, prop wiring).
  - Tests: 4 new cases in `frontend/src/routes/timetable.test.tsx` (save settles + reusable; download locks while dirty and re-enables after save; newer transient replaces older; clash warning persists beside a transient notice and clears only when validation stops reporting it). Full frontend suite **322 green**; `tsc -b` + `vite build` pass; no new lint errors (the two `set-state-in-effect` warnings are pre-existing baseline issues in untouched effects).

- **Unit 79: Frontend Local Timetable Draft Persistence**
  - New `frontend/src/features/timetable/draftStorage.ts`: schema-versioned `StoredTimetableDraft` (`schemaVersion: 1`, `savedAssignmentFingerprint`, `updatedAt`, `assignments`) under `localStorage` key `tts3.timetable.draft.v1`, with safe JSON parse, schema-version guard, required-field validation, `clearStoredDraft`, `saveStoredDraft` (best-effort), `computeSavedAssignmentFingerprint` (deterministic, order-independent), and `loadStoredDraft` returning `none` / `restored` / `discarded{invalid|schema-mismatch|fingerprint-mismatch}`.
  - Fingerprint guards against stale drafts overriding changed saved state: stored alongside the draft and compared to the live saved assignments on restore; mismatch discards the stored draft and surfaces a concise message.
  - `routes/timetable.tsx`: one-time hydration gate restores at most once after saved assignments first load (dirty + `restored` notice), discards safely (`discarded` notice), and never resurrects a stored draft on later refetches; a hydration-gated effect persists the dirty draft and clears storage after a successful save; empty dirty drafts (Clear All) persist until saved; restored assignments still flow through the existing blocking auto-unschedule and warning logic.
  - `TimetableActionBar.tsx`: dismissible `Unsaved draft restored.` / `Old unsaved draft was discarded because saved timetable data changed.` notices (priority 2.5), no storage internals shown; per-message `onDismiss` override.
  - Tests: `draftStorage.test.ts` (15) + `routes/timetable.test.tsx` draft-persistence suite (8); existing route tests clear `localStorage` per test. Draft + timetable tests green; production build green (strict tsc). No backend, API, or package dependency changes.

- **Unit 78: Frontend Drag Preview and Hover Highlighting Upgrade**
  - **Grid measurement layer**: `TimetableGrid` now attaches a `ResizeObserver` to its container and reports `{ cellWidth, rowHeight }` via an `onMetricsChange` callback whenever the layout changes. The page stores `gridMetrics` state and passes a stable `handleMetricsChange` callback (via `useCallback`) to avoid re-firing the observer.
  - **Scheduled-card drag preview**: Replaced `UnscheduledSessionCardPreview` in the `DragOverlay` with a new `DragPreviewCard` component (`features/timetable/DragPreviewCard.tsx`). The preview matches the scheduled card's visual shape — same subject colour tokens, compact `unit_code + Lec/Tut` header, lecturer name, and `student_count` for multi-hour sessions. Width = `metrics.cellWidth` (fallback 200px); height = `metrics.rowHeight × duration` (fallback 56px per slot).
  - **Pointer alignment modifier**: A `createPointerAlignModifier(metrics)` factory produces a dnd-kit `modifiers` function for `DragOverlay`. It reads `activatorEvent.clientX/Y` and `draggingNodeRect` to center the overlay horizontally on the cursor and align the cursor at the vertical center of the first slot — correct for both single- and multi-hour sessions. Falls back gracefully when metrics are unavailable.
  - **Occupied-cell hover highlighting**: `timetable.tsx` adds `onDragOver` to the `DndContext` which tracks the currently hovered droppable ID in `activeHoverKey`. A `useMemo` computes `hoverHighlightKeys: Set<string>` via the new pure helper `computeHoverHighlightKeys` — which runs `checkProposedPlacement` on the proposed slot range and returns the full set of `"day:roomId:slotId"` keys only when the proposal is blocking-valid. Invalid proposals (capacity exceeded, double-booking, crossing lunch, off timetable) return an empty set — no highlight, no reason shown during hover.
  - **GridCell updated**: Added `isHoverHighlighted?: boolean` prop; `isOver` from `useDroppable` is no longer used for visual highlight (it remains registered for drop detection). Highlight logic: `showDropHighlight = isHoverHighlighted || (isClickDropTarget && hovered)`. Added `data-grid-cell="true"` to every cell for the measurement query.
  - **TimetableGrid updated**: Accepts `hoverHighlightKeys?: Set<string>` and `onMetricsChange?` props; threads `isHoverHighlighted` to every `GridCell`; implements the ResizeObserver measurement loop via a stable callback ref.
  - **New pure module** `features/timetable/hoverHighlight.ts`: exports `TimetableGridMetrics`, `GRID_ROW_HEIGHT_PX`, `computePreviewHeight`, and `computeHoverHighlightKeys` — all side-effect-free and directly testable.
  - **Interaction compatibility preserved**: click-based scheduling, scheduled-card drag, unschedule button, solver editing lock, and warning recalculation are all unchanged. `handleDragStart/End/Cancel` each reset `activeHoverKey` to clear stale highlights. Failed drops still surface a blocking reason in the action bar.
  - **Tests**: New `hoverHighlight.test.ts` (15 tests — `computePreviewHeight`, `computeHoverHighlightKeys` for null/invalid/valid single/multi-slot/all-four-invalid-proposal-types/move-self). New `DragPreviewCard.test.tsx` (11 tests — compact labels "Lec"/"Tut" not "Lecture"/"Tutorial", fallback/measured dimensions, height × duration, student count rules, no unscheduled-card content). New route tests (4). Fixed pre-existing `UnscheduledPool.test.tsx` type error; added `ResizeObserver` class stub to `src/test/setup.ts`. Frontend **187 tests green** (30 new), **production build green**. No backend, API, validation-rule, or package dependency changes.

- **Unit 77: Frontend Sticky Timetable Action Bar and Validation Message Consolidation**
  - All timetable feedback consolidated into a single sticky `TimetableActionBar`: save state, solver lifecycle, blocking placement errors, blocking/warning validation summaries, and assignment-load errors are all shown in one bar — no separate panel shifts the page.
  - `SolverStatusPanel` removed from `timetable.tsx` (banner no longer rendered as a layout sibling). Solver state is driven by new props on `TimetableActionBar`: `solverRunStatus`, `isSolverStarting`, `solverStartError`, `solverStatusError`, `onDismissSolver`. The `SolverStatusPanel` component and its isolated tests are preserved unchanged.
  - `assignmentsIsError` `ErrorState` removed from `timetable.tsx`; assignment-load errors now show inline inside the bar with a "Try again" retry button.
  - Details overlay: the inline expanding details section was replaced with an `position: absolute; top: 100%` overlay anchored to the bar. Opening details adds no layout height — the overlay floats over the timetable canvas. Dismissal by trigger button re-click or Escape key.
  - Priority order enforced in the bar: (1) assignment-load / save system error → (2) solver running / status error → (3) blocking placement failure → (4) blocking violations summary → (5) warning summary → (6) editing disabled / pending placement → (7) unsaved changes → (8) ready. Solver startError / failed / succeeded states are dismissible via `X` button.
  - Slot label messages: `warning.ts` `lecturer_unavailable` message now uses `slotLabel(slot)` from `lib/slot-label.ts` — raw slot IDs (`s1`) are replaced with human time ranges (`9:00-9:50`) in all user-facing validation text.
  - Tests: 5 new tests in `timetable.test.tsx` — solver running state inside bar (not sibling), assignment error inside bar, details overlay is a bar descendant, details overlay shows time label not raw slot ID, solver and save buttons inside single bar surface. Also added `vi.mock('@/lib/api/units')` and `mockListUnits.mockResolvedValue([])` to test setup. Frontend **157 tests green** (5 new), **production build green**. No backend, API, validation-rule, or package dependency changes.

- **Unit 76: Frontend Unscheduled Pool Sizing and Search Refinement**
  - Unit boxes now use a CSS grid container (`repeat(auto-fill, minmax(17rem, 20rem))`) and `w-full` on each box so all boxes have equal width and wrap across available page width.
  - Session-type text (`lecture`/`tutorial`) removed from the search index — type is no longer searchable.
  - Search now matches unit code, unit name, and the full unit teaching-team lecturer names (all team members, not just the session lecturer); derived from the loaded units list via a `useMemo` map; falls back to `session.lecturer_display_name` when units data is not yet available.
  - `timetable.tsx` adds a `useQuery(['units'], listUnits)` (shared cache with the Units page) and passes `units` to `UnscheduledPool`.
  - Search placeholder updated from "Search unit, type, or lecturer" to "Search unit or lecturer".
  - Year filter, all empty states, drag/drop scheduling, and selection callbacks are all preserved unchanged.
  - Tests: 5 new tests — unit boxes are `w-full` (equal-width), empty unit boxes not rendered, teaching-team search via `units` prop, type not searchable (rendered), type not searchable (pure function); existing `'tutorial'` session-type match case removed from `it.each`. Frontend **152 tests green**, **production build green**. No backend, API, or package dependency changes.

- **Unit 75: Frontend Management Title Cleanup and Unit Filters**
  - Student title confirmed absent from all student UI (create form, edit form, table, display names) — completed in Unit 73; confirmed and test-covered here.
  - Lecturer title selector uses the final product list (`Mr`/`Ms`/`Mrs`/`Dr`/`Fr`/`A/Prof.`/`Prof.`); unsupported titles returned from the API display as text without crashing.
  - Added subject filter to `/students` (derived from enrolled units via `parseUnitCode`; invalid codes produce no option).
  - Added subject and year filters to `/lecturers` (subject derived from taught units; year derived from parsed unit codes; uses existing `taughtUnitsByLecturer` map).
  - Added subject filter to `/units` (derived from loaded unit codes; existing year and lecturer filters unchanged).
  - Updated `StudentFilters`, `LecturerFilters`, `UnitFilters` types, empty values, active predicates, and pure filter functions.
  - Tests: 19 new tests across filter and route suites; **147 frontend tests green**, **production build green**. No backend, API, or package dependency changes.

- **Unit 73: Frontend API Types and UI Token Alignment**
  - Aligned frontend DTOs to the Unit 72 backend contract: removed `title` from `Student`/`StudentCreate`/`StudentUpdate` and the unit `StudentSummary`, and dropped the `StudentTitle` type/re-export. Restricted the `LecturerTitle` union to `Mr`/`Ms`/`Mrs`/`Dr`/`Fr`/`A/Prof.`/`Prof.` (punctuation only on the professor titles).
  - Forced compile/UI fixes from the type changes: dropped the student title `Select`, table column, payloads, and delete-name in `students.tsx` (table now 5 columns); updated the lecturer title options in `lecturers.tsx`; removed `title` from the student label/search in `units.tsx`; updated student/lecturer search haystacks and all affected fixtures/tests.
  - Added subject colour design tokens (History/Philosophy/Theology/Literature/Latin/Greek/Science) to `index.css` and documented them in `ui-context.md` — declared once as tokens, never inlined.
  - Added the frontend-only `lib/unit-code-parser.ts`: pure `parseUnitCode`/`isValidUnitCode`, supported subject metadata with `var(--subject-*)` token references, structured invalid reasons (`empty`/`structure`/`unknown_subject`/`invalid_year`) with partial info, and subject/year filter-option helpers.
  - Added `lib/slot-label.ts`: centralizes slot-ID → human time-range conversion (`s4` → `1:30-2:20`) sourced from `TIME_SLOTS`, with `slotLabels`/`formatAffectedSlots` helpers for validation messages (validation modules are rewired to consume it in Unit 77).
  - Added focused tests (`unit-code-parser.test.ts`, `slot-label.test.ts`); no route UI behaviour redesigned. Verified frontend **typecheck, production build, and 116 tests green**.

- **Unit 72: Backend Title and Unit-Code Contract Cleanup**
  - Removed student `title` completely: dropped the `StudentTitle` enum, the `students.title` column, and `title` from all student schemas, the embedded `StudentSummary`, and the create service.
  - Migrated lecturer titles to the final list (`Mr`, `Ms`, `Mrs`, `Dr`, `Fr`, `A/Prof.`, `Prof.`); punctuation retained only for the two professor titles.
  - Added defensive unit-code structural validation (`^[A-Z]{3}\d{3}$`) with trim + uppercase normalization before persistence and a clear error message; uniqueness now enforced on the normalized code.
  - Added migration `0012` (drops the title column + `studenttitle` type; rebuilds the `lecturertitle` enum with the approved value mapping). Head is now `0012`.
  - Added `tests/test_72_title_and_unit_code_contract.py` (43 tests) and updated existing fixtures; verified backend **314 tests green**, app import smoke, and Alembic `0012 (head)`.
  - Breaking API change; frontend alignment deferred to Unit 73.

- **Unit 71: Post-V1 Regression, Docs, and Acceptance Pass**
  - Fixed student update year validation (1-3) and preserved session lecturer IDs in saved assignment DTOs.
  - Added missing backend and Units-page regression coverage.
  - Updated post-v1 architecture, product, standards, build-plan, progress, and acceptance documentation.
  - Verified backend 271, frontend 100, production build, backend import, and Alembic `0011 (head)`.
  - Kept the 22-step interactive acceptance checklist explicit and unchecked pending a real browser run.

- **Unit 1: Repository, Frontend Bootstrap, and UI Foundation**
  - Scaffolded `frontend/` with Vite + React + TypeScript
  - Installed TailwindCSS v4 with `@tailwindcss/vite` plugin
  - Configured `vite.config.ts` with Tailwind plugin and `@` path alias
  - Configured `tsconfig.app.json` with strict mode and `@/*` path alias
  - Wrote `src/index.css` with all v1 design tokens from `ui-context.md`:
    - page backgrounds, surface colors, text colors
    - accent colors (maroon primary, gold secondary)
    - border colors, focus ring, state colors
    - timetable grid tokens, unit card tokens, solver accent tokens
  - Loaded Inter, Cormorant Garamond, and JetBrains Mono via Google Fonts
  - Initialized shadcn/ui (`base-nova` style, Tailwind v4, CSS variables)
  - Added shadcn/ui base components: Button, Input, Card, Table, Dialog, Alert, Badge, Select, Form, Label
  - Installed `@base-ui/react` (required by `base-nova` style components)
  - Removed default Vite demo UI; `App.tsx` renders only the styled foundation screen
  - Foundation page shows: app title (serif), description, status badges, feature checklist card
  - No mock data, no routing, no auth, no backend code
  - Root `README.md` with project description and frontend run instructions
  - Root `.gitignore`
  - `frontend/.env.example`
  - `docs/` placeholder

- **Unit 2: Frontend Route Shell, App Layout, and Login Shell**
  - Installed `react-router-dom`
  - Created `BrowserRouter` + `Routes` in `App.tsx` with all v1 routes
  - Root `/` redirects to `/timetable`
  - Created shared layout components in `src/components/layout/`:
    - `AppFrame` — top-level page wrapper with `TopNav` and `<main>` content area
    - `TopNav` — horizontal header with brand name, nav links, sign-out placeholder (disabled)
    - `PageHeader` — page title, description, optional right-side action slot
    - `EmptyState` — centered empty state with icon, title, description, optional action
  - Created placeholder pages in `src/routes/`:
    - `/timetable` — TimetablePage with empty state
    - `/rooms` — RoomsPage with empty state
    - `/lecturers` — LecturersPage with empty state
    - `/students` — StudentsPage with empty state
    - `/units` — UnitsPage with empty state
    - `/login` — LoginPage with complete form shell (email, password, submit, loading state, error state)
  - Login form prevents default submission and shows a 1-second loading state; no Supabase calls
  - Created `/signup` page shell — same design as login: email, password, confirm password, loading state, error state
  - TopNav refactored: title is larger (`text-xl font-bold`, serif, maroon) and anchored left; sign-out button anchored right; nav links (`Timetable, Units, Lecturers, Students, Rooms`) absolutely centered on the page
  - AppFrame `main` fills full viewport width — `max-w-7xl` constraint removed
  - All pages use Unit 1 design tokens; no hardcoded hex values
  - Active nav link styled with maroon soft background
  - Build succeeds with zero TypeScript errors

- **Unit 3: Backend Bootstrap, Database, Migrations, and Error Foundation**
  - Created `backend/` structure: `api/`, `db/`, `models/`, `schemas/`, `services/`, `auth/`, `log/`, `alembic/`
  - `config.py` — Pydantic `Settings` with `DATABASE_URL` and `CORS_ORIGINS` loaded from `.env`
  - `main.py` — FastAPI app with CORS middleware (allows local frontend at `http://localhost:5173`), `AppError` exception handler, and `api_router` mounted
  - `api/health.py` — `GET /health` returns `{"status": "ok"}`
  - `api/errors.py` — `ErrorDetail`, `ErrorResponse` Pydantic models; `error_response()` helper; `AppError` exception; `app_error_handler` FastAPI exception handler
  - `api/router.py` — top-level `api_router` that includes the health router
  - `db/session.py` — SQLAlchemy 2.0 `engine`, `SessionLocal`, and `Base` (DeclarativeBase)
  - `db/deps.py` — `get_db()` FastAPI dependency yielding a `Session`
  - `log/setup.py` — `configure_logging()` using structlog with JSON output and ISO timestamps (named `log/` not `logging/` to avoid shadowing the Python stdlib)
  - `alembic.ini` — Alembic config pointing to `alembic/` script location; URL overridden by env var in `env.py`
  - `alembic/env.py` — reads `DATABASE_URL` from environment, imports `Base.metadata` as `target_metadata`
  - `alembic/script.py.mako` — standard Alembic migration template
  - `alembic/versions/0001_baseline.py` — empty baseline migration (no domain tables)
  - `requirements.txt` — FastAPI, Uvicorn, pydantic-settings, SQLAlchemy 2.0, Alembic, psycopg2-binary, structlog
  - `backend/.env.example` — documents required environment variables
  - No domain models, no auth, no CRUD routes, no solver code added

- **Unit 4: Backend Supabase Auth Boundary**
  - Added `PyJWT[crypto]>=2.8.0` to `requirements.txt`
  - Added `supabase_url` field to `config.py` `Settings`
  - Created `backend/auth/jwt.py` — `decode_supabase_token()` verifies RS256/ES256 Supabase JWTs via `PyJWKClient` fetching keys from `{SUPABASE_URL}/auth/v1/.well-known/jwks.json`; JWKS client caches keys across requests
  - Created `backend/auth/deps.py` — `CurrentAdmin` model and `get_current_admin` FastAPI dependency; extracts Bearer token, verifies JWT, raises `AppError` 401 on missing or invalid tokens
  - Created `backend/api/protected.py` — `GET /auth/verify` test endpoint requiring `get_current_admin`; returns `{"authenticated": true, "user_id": "..."}` for valid tokens
  - Registered protected router in `api/router.py`
  - Updated `.env.example` with `SUPABASE_URL`
  - No product CRUD routes, no frontend auth code, no RBAC added

- **Unit 5: Frontend Supabase Auth and Protected Routes**
  - Installed `@supabase/supabase-js`
  - Created `frontend/src/lib/supabase.ts` — Supabase browser client using `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY`
  - Created `frontend/src/lib/auth/context.tsx` — `AuthProvider` initialises session via `getSession()`, subscribes to `onAuthStateChange`, exposes `session`, `loading`, and `signOut` via `useAuth()`
  - Created `frontend/src/components/auth/ProtectedRoute.tsx` — shows blank loading screen during session init, redirects unauthenticated users to `/login`, renders children when authenticated
  - Updated `frontend/src/routes/login.tsx` — real Supabase email/password sign-in; controlled email/password inputs; loading and error states; redirects to `/timetable` on success; redirects already-authenticated users to `/timetable`
  - Updated `frontend/src/components/layout/TopNav.tsx` — sign-out button calls `useAuth().signOut()`; ProtectedRoute handles post-signout redirect
  - Updated `frontend/src/App.tsx` — `AuthProvider` wraps all routes; five app routes wrapped in `ProtectedRoute`
  - Updated `frontend/.env.example` — added `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY`
  - Build succeeds with zero TypeScript errors
  - No authenticated API client, no TanStack Query, no Zustand, no mock data added

- **Unit 5/2: Signup Functionality**
  - Wired controlled inputs (email, password, confirmPassword) in `frontend/src/routes/signup.tsx`
  - Added client-side validation: required fields, password/confirm match
  - Calls `supabase.auth.signUp()` on submit; loading state disables form during request
  - Friendly error messages mapped from Supabase error strings
  - Post-signup: redirects to `/timetable` if session returned; shows "check your email" success screen if email confirmation is required
  - Redirects already-authenticated users to `/timetable`
  - Added `Sign in` link on signup page; added `Create one` link on login page
  - Build succeeds with zero errors

- **Unit 6: Frontend Authenticated API Base Client**
  - Renamed `VITE_API_URL` → `VITE_API_BASE_URL` in `frontend/.env.example`
  - Created `frontend/src/lib/api/client.ts` — `apiRequest<T>` generic helper: reads `VITE_API_BASE_URL`, retrieves Supabase access token via `getSession()`, attaches `Authorization: Bearer`, sends/parses JSON, handles empty responses, normalizes backend errors into `ApiRequestError`, handles `401` consistently
  - Created `frontend/src/lib/api/auth.ts` — `verifyAuth()` calls `GET /auth/verify` (Unit 4 protected endpoint); exports `VerifyResponse` type
  - Updated `frontend/src/routes/timetable.tsx` — dev-only `useEffect` calls `verifyAuth()` on mount; renders inline status line (idle / loading / ✓ authenticated / ✗ error) via `import.meta.env.DEV` guard; stripped in production build
  - No TanStack Query, Zustand, CRUD, mock data, or product API clients added
  - Build succeeds with zero TypeScript errors

- **Unit 7: Frontend Rooms Page Shell**
  - Replaced placeholder `RoomsPage` with full management-page layout
  - Page header with title, description, and `Create room` primary action
  - Bordered table panel with column headers: Name, Capacity, Type, Actions
  - Inline empty state (icon + text) inside `TableBody` when no rooms exist — no fake rows
  - `RoomForm` component with Name (`Input`), Capacity (`Input[type=number]`), Room type (`Select`) fields
  - Create room `Dialog` — opens via header button, form shell, disabled Create CTA
  - Edit room `Dialog` — state-controlled, row-level trigger wired when real data exists
  - Delete confirmation `Dialog` — state-controlled, destructive CTA, explains session impact
  - All styling via design tokens, no hardcoded hex values
  - No backend calls, API clients, TanStack Query, or CRUD behavior
  - Build succeeds with zero TypeScript errors

- **Unit 8: Frontend Timetable No-Room State and Grid Shell**
  - Updated `/timetable` route to full workspace layout
  - Page header with revised description; reserved action/status bar (`TimetableActionBar`)
  - No-room empty state rendered when `ROOMS` array is empty (default — no room API yet)
  - Created `frontend/src/features/timetable/slots.ts` — centralized `DAYS`, `TIME_SLOTS`, `AM_SLOTS`, `PM_SLOTS`, `LUNCH_LABEL`; Monday–Friday; AM slots 9:00–11:00, PM slots 13:00–16:00
  - Created `frontend/src/features/timetable/GridCell.tsx` — blank cell (h-14, w-32), hover treatment via `--grid-cell-hover`, day-boundary border via `--grid-line-strong`, all tokens from `ui-context.md`
  - Created `frontend/src/features/timetable/TimetableGrid.tsx` — accepts `RoomColumn[]`; renders day headers, room sub-headers, AM rows, lunch divider (`--grid-lunch-bg`), PM rows; returns null when rooms empty
  - Created `frontend/src/features/timetable/TimetableActionBar.tsx` — reserved shell for future validation and solver controls
  - No room API, fake rooms, fake sessions, fake assignments, drag-and-drop, or solver behavior added
  - Build succeeds with zero TypeScript errors

- **Unit 9: Backend Room Persistence and Protected API**
  - Created `backend/models/room.py` — `RoomType` enum (`lecture`, `tutorial`) and `Room` SQLAlchemy model with `id`, `name` (unique), `capacity`, `room_type`, `created_at`, `updated_at`
  - Created `backend/schemas/room.py` — `RoomCreate`, `RoomUpdate`, and `RoomResponse` Pydantic schemas; validators reject blank names and non-positive capacity values
  - Created `backend/services/room.py` — `list_rooms`, `get_room`, `create_room`, `update_room`, `delete_room`; raises `AppError` 404 on not-found, 409 on name uniqueness conflict
  - Created `backend/api/rooms.py` — `GET /rooms`, `POST /rooms`, `PUT /rooms/{room_id}`, `DELETE /rooms/{room_id}`; all routes require `get_current_admin`; returns `RoomResponse` schemas
  - Updated `backend/api/router.py` — registered rooms router
  - Created `backend/alembic/versions/0002_create_rooms.py` — creates `rooms` table and `roomtype` Postgres enum; revises `0001`
  - Updated `backend/models/__init__.py` — imports `Room` so `Base.metadata` includes the table
  - Updated `backend/alembic/env.py` — imports `models` package so all models are registered for future `--autogenerate`
  - No frontend API client, no UI integration, no assignment or solver behavior added

- **Unit 10: Frontend Room API Client**
  - Created `frontend/src/lib/api/rooms.ts` — `Room` DTO type, `RoomCreate` and `RoomUpdate` input types, `RoomType` union, and four API functions: `listRooms`, `createRoom`, `updateRoom`, `deleteRoom`
  - All functions use `apiRequest` from the Unit 6 authenticated base client; auth token is attached consistently through that shared helper
  - `parseRoomError` helper adds room-specific messages for 409 (duplicate name) and 422 (validation) errors before re-throwing
  - No TanStack Query, no rooms page data wiring, no mock data added
  - Build succeeds with zero errors

- **Unit 11: Frontend Rooms Page Integration**
  - Installed `@tanstack/react-query`
  - Added `QueryClientProvider` wrapping the app root in `src/App.tsx`
  - Rewrote `src/routes/rooms.tsx` with full backend integration:
    - `useQuery(['rooms'], listRooms)` — loading, error, and empty states driven by real backend data
    - `useMutation` for create, edit, delete — each invalidates `['rooms']` on success
    - Controlled form state (`name`, `capacity`, `room_type`) for create and edit dialogs
    - Required field validation gates submit buttons; buttons show loading text while mutations run
    - Per-dialog error messages surface mutation errors to the user
    - Delete requires confirmation; delete dialog shows room name and error state
    - Room type values corrected to match backend enum (`lecture`, `tutorial`)
    - Row-level Edit and Delete action buttons wired to real room data
    - No mock data, no Zustand, no timetable integration, no optimistic updates
  - Build succeeds with zero TypeScript errors

- **Unit 12: Frontend Timetable Room Integration**
  - Replaced static `ROOMS: RoomColumn[]` placeholder in `src/routes/timetable.tsx` with `useQuery(['rooms'], listRooms)` from the room API client
  - Loading state: centered "Loading rooms…" panel shown while rooms fetch
  - Error state: backend error message shown in error panel
  - No-room state: empty state with `CalendarDays` icon and link to `/rooms` for navigation
  - Grid state: `TimetableGrid` rendered with real `Room[]` records; `Room` satisfies `RoomColumn` structurally (`id` + `name`)
  - `queryKey: ['rooms']` matches the rooms page cache — creating a room on `/rooms` invalidates and refetches, causing the timetable to render the grid automatically
  - Removed dev-only auth verify code from Unit 6 (no longer needed)
  - No new packages, no Zustand, no localStorage, no fake data
  - Build succeeds with zero TypeScript errors

- **Unit 12/2: Timetable Table UI Adjustments**
  - Updated `slots.ts`: all 8 time slot labels now show start–end times in `H:MM-H:MM` format; added 4th AM slot `s4` (`12:00-12:50`); PM slots renumbered s5–s8 with labels `1:30-2:20` through `4:30-5:20`
  - `TimetableGrid.tsx`: removed `overflow-x-auto` wrapper and `min-w-max`; grid is now `w-full`; day headers use `flex: rooms.length` to span proportionally; room sub-headers and `GridCell` use `flex-1` so columns distribute across available width without horizontal scroll
  - Time label column widened from `4rem` to `6rem` to accommodate longer `HH:MM-HH:MM` labels
  - All time labels, day headers, room sub-headers, and lunch row use `userSelect: 'none'` and `onContextMenu` prevention; global app text selection and right-click are unaffected
  - `GridCell.tsx`: replaced `shrink-0 w-32` with `flex-1`; cells resize proportionally
  - Build succeeds with zero TypeScript errors

- **Units 13/17: Frontend Lecturer and Student Page Shells**
  - Created `frontend/src/features/lecturers/AvailabilityEditor.tsx` — self-contained availability grid; Mon–Fri columns, AM slots (s1–s4), lunch divider, PM slots (s5–s8); click to toggle unavailable (maroon-soft highlight); legend and instructions; blank initial state
  - Rewrote `frontend/src/routes/lecturers.tsx` — full management shell: page header with "Add lecturer" action; table with Title/First name/Last name/Actions columns; inline empty state; Create/Edit/Delete/Availability dialogs; `LecturerFormFields` (title, first name, last name); all submit buttons disabled pending API integration; availability dialog uses `AvailabilityEditor` with disabled "Save availability" CTA
  - Rewrote `frontend/src/routes/students.tsx` — full management shell: page header with "Add student" action; table with Title/First name/Last name/Year level/Actions columns; inline empty state; Create/Edit/Delete dialogs; `StudentFormFields` (title, first name, last name, year level); all submit buttons disabled pending API integration; no lecturer availability controls
  - No API calls, no mock data, no TanStack Query, no persistence; build succeeds with zero TypeScript errors

- **Unit 14/18: Backend Lecturer and Student Persistence and Protected API**
  - Created `backend/models/lecturer.py` — `LecturerTitle` enum (`Dr.`, `Prof.`, `A/Prof.`, `Mr.`, `Ms.`), `AvailabilityDay` enum (Monday–Friday), `AvailabilitySlot` enum (`s1`–`s8`, matching timetable slots), `Lecturer` SQLAlchemy model, `LecturerAvailability` model with unique constraint on `(lecturer_id, day, slot)` and `delete-orphan` cascade
  - Created `backend/models/student.py` — `StudentTitle` enum (`Mr.`, `Ms.`, `Mx.`), `Student` SQLAlchemy model with `year_level` integer field
  - Created `backend/schemas/lecturer.py` — `AvailabilityEntry`, `LecturerCreate`, `LecturerUpdate`, `LecturerAvailabilitySet`, `LecturerResponse` (includes `unavailable_slots`); validators enforce non-blank names
  - Created `backend/schemas/student.py` — `StudentCreate`, `StudentUpdate`, `StudentResponse`; validators enforce non-blank names and year level 1–5
  - Created `backend/services/lecturer.py` — `list_lecturers`, `get_lecturer`, `create_lecturer`, `update_lecturer`, `delete_lecturer`, `set_availability` (replaces all unavailability records via ORM relationship)
  - Created `backend/services/student.py` — `list_students`, `get_student`, `create_student`, `update_student`, `delete_student`
  - Created `backend/api/lecturers.py` — `GET /lecturers`, `POST /lecturers`, `PUT /lecturers/{id}`, `DELETE /lecturers/{id}`, `PUT /lecturers/{id}/availability`; all protected by `get_current_admin`
  - Created `backend/api/students.py` — `GET /students`, `POST /students`, `PUT /students/{id}`, `DELETE /students/{id}`; all protected by `get_current_admin`
  - Created `backend/alembic/versions/0003_create_lecturers_and_students.py` — creates `lecturertitle`, `availabilityday`, `availabilityslot`, `studenttitle` Postgres enums; creates `lecturers`, `lecturer_availability`, `students` tables
  - Updated `backend/models/__init__.py` — registers `Lecturer`, `LecturerAvailability`, `Student`
  - Updated `backend/api/router.py` — registered lecturer and student routers
  - No frontend API clients, unit/session relationships, constraint evaluation, or solver behavior added

- **Unit 15/19: Frontend Lecturer and Student API Clients**
  - Created `frontend/src/lib/api/lecturers.ts` — `LecturerTitle`, `AvailabilityDay`, `AvailabilitySlot`, `AvailabilityEntry`, `Lecturer`, `LecturerCreate`, `LecturerUpdate`, `LecturerAvailabilitySet` types; `listLecturers`, `createLecturer`, `updateLecturer`, `deleteLecturer`, `setLecturerAvailability` functions; `parseLecturerError` helper for 409/422
  - Created `frontend/src/lib/api/students.ts` — `StudentTitle`, `Student`, `StudentCreate`, `StudentUpdate` types; `listStudents`, `createStudent`, `updateStudent`, `deleteStudent` functions; `parseStudentError` helper for 422
  - All functions use the Unit 6 `apiRequest` authenticated base client; token attachment and 401 handling delegated to base client
  - DTO types match backend `LecturerResponse` and `StudentResponse` schemas exactly; `AvailabilitySlot` union matches backend enum (s1–s3, s5–s8, no s4)
  - `/lecturers` and `/students` pages remain unconnected to real data
  - Build succeeds with zero TypeScript errors

- **Unit 16/20: Frontend Lecturer and Student Page Integrations**
  - Updated `AvailabilityEditor` to controlled component — accepts `value: AvailabilityEntry[]` and `onChange` props; no internal state
  - Rewrote `frontend/src/routes/lecturers.tsx` with full TanStack Query integration:
    - `useQuery({ queryKey: ['lecturers'], queryFn: listLecturers })` — loading, error, empty states driven by real backend data
    - `useMutation` for create, edit, delete, availability save — each invalidates `['lecturers']` on success
    - Availability dialog initialises from lecturer's `unavailable_slots`; saves via `setLecturerAvailability`
    - Per-dialog error messages surface mutation failures; loading text on buttons while mutations run
    - Removed local `Lecturer`, `LecturerTitle` type definitions; imported from `@/lib/api/lecturers`
    - No mock data, no Zustand, no student API calls
  - Rewrote `frontend/src/routes/students.tsx` with full TanStack Query integration:
    - `useQuery({ queryKey: ['students'], queryFn: listStudents })` — loading, error, empty states
    - `useMutation` for create, edit, delete — each invalidates `['students']` on success
    - Per-dialog error messages; loading text on buttons while mutations run
    - Removed local `Student`, `StudentTitle` type definitions; imported from `@/lib/api/students`
    - No mock data, no Zustand, no lecturer API calls, no availability controls
  - Query keys `['lecturers']` and `['students']` are separate and never cross-invalidate
  - Build succeeds with zero TypeScript errors

- **Unit 21: Frontend Unit and Session Management Shell**
  - Rewrote `frontend/src/routes/units.tsx` — full management shell: page header with "Create unit" action; table with Code/Name/Sessions/Actions columns; inline empty state (BookOpen icon + text)
  - Create unit `Dialog` — opens via header button; fields for unit code (e.g. HIS101), unit name (e.g. Ancient History), disabled lecturer selector (placeholder), disabled student selector (placeholder); session section with add button, inline session boxes, empty session state; Create CTA disabled until code and name are filled
  - Edit unit `Dialog` — same form structure as create; Save changes CTA disabled until required fields filled
  - Delete unit `Dialog` — confirms that the unit and all its sessions will be removed; destructive CTA
  - `SessionBox` component — compact card with session type `Select` (Lecture/Tutorial/Lab/Workshop) and duration `Select` (1–4 slots); delete button removes the box from local shell state
  - Session state is local to the form only (add/delete interaction only); no persistence, no backend calls, no TanStack Query wiring
  - Lecturer and student selectors are disabled with placeholder text; no real data connected
  - No fake unit, session, lecturer, or student records displayed
  - Fixed `@base-ui/react` Select `onValueChange` type (`string | null`) with `?? ''` nullish coalescing
  - Build succeeds with zero TypeScript errors

- **Unit 22: Backend Unit Persistence and Protected API**
  - Created `backend/models/unit.py` — `Unit` SQLAlchemy model (`id`, `code` unique, `name`, `lecturer_id` FK); `unit_students` many-to-many join table (unit→student, both cascade on delete); `lazy="selectin"` on `lecturer` and `students` relationships to avoid N+1 on list
  - Created `backend/schemas/unit.py` — `LecturerSummary`, `StudentSummary` (lightweight nested types); `UnitCreate` (code, name, lecturer_id, student_ids); `UnitUpdate` (all optional); `UnitResponse` (includes nested lecturer and students); validators enforce non-blank code and name
  - Created `backend/services/unit.py` — `list_units`, `get_unit`, `create_unit`, `update_unit`, `delete_unit`; validates lecturer exists (422), all student IDs exist (422), unique unit code (409 on conflict); excludes self on code uniqueness check during update
  - Created `backend/api/units.py` — `GET /units`, `POST /units`, `PUT /units/{unit_id}`, `DELETE /units/{unit_id}`; all routes require `get_current_admin`; returns `UnitResponse` schemas
  - Created `backend/alembic/versions/0005_create_units.py` — creates `units` table (with FK to `lecturers`, unique constraint on `code`) and `unit_students` join table; revises `0004`
  - Updated `backend/models/__init__.py` — registers `Unit` and `unit_students`
  - Updated `backend/api/router.py` — registered units router
  - No frontend code, no session model, no session routes added

- **Unit 23: Frontend Unit API Client**
  - Created `frontend/src/lib/api/units.ts` — `LecturerSummary`, `StudentSummary` nested types; `Unit` DTO; `UnitCreate`, `UnitUpdate` request types; `listUnits`, `createUnit`, `updateUnit`, `deleteUnit` API functions; `parseUnitError` helper for 409 (duplicate code) and 422 (validation) errors
  - All functions use the Unit 6 `apiRequest` authenticated base client; token attachment and 401 handling delegated to base client
  - DTO types match backend `UnitResponse` schema exactly; `LecturerSummary` and `StudentSummary` match backend nested types; `LecturerTitle` imported from existing `lecturers.ts` to avoid duplication
  - No server-owned unit data in Zustand; `/units` page not connected to real data; no session API client added
  - Build succeeds with zero TypeScript errors

- **Unit 24: Frontend Unit Page Integration**
  - Rewrote `frontend/src/routes/units.tsx` with full TanStack Query integration:
    - `useQuery({ queryKey: ['units'], queryFn: listUnits })` — loading, error, empty states driven by real backend data
    - `useQuery({ queryKey: ['lecturers'], queryFn: listLecturers })` — powers lecturer selector
    - `useQuery({ queryKey: ['students'], queryFn: listStudents })` — powers student selector
    - `useMutation` for create, edit, delete — each invalidates `['units']` on success; per-dialog error messages; loading text on CTA buttons
  - Lecturer selector: real `Select` populated from lecturer records; shows `Title First Last`; required for form validity
  - Student selector: scrollable checkbox list populated from student records; tracks `student_ids[]`; shows selected count
  - Create, edit, delete dialogs fully wired to backend mutations; delete dialog shows unit code and name for confirmation
  - Edit dialog initialises from real unit data (`code`, `name`, `lecturer_id`, `student_ids` extracted from `unit.students`)
  - Session section kept as non-persistent shell: add/remove session boxes work locally; no backend calls; placeholder text notes next phase
  - `isFormValid` requires `code`, `name`, and `lecturer_id`; sessions not required
  - `/lecturers` and `/students` routes not muddled; units page only reads from those query caches
  - No mock data, no Zustand, no session API calls
  - Build succeeds with zero TypeScript errors

- **Unit 25: Backend Session Persistence and Protected API**
  - Created `backend/models/session.py` — `SessionType` enum (`lecture`, `tutorial`, `lab`, `workshop`); `Session` SQLAlchemy model (`id`, `unit_id` FK with CASCADE, `session_type`, `duration`, `created_at`, `updated_at`); back-populates `unit.sessions`
  - Updated `backend/models/unit.py` — added `sessions` relationship with `cascade="all, delete-orphan"` so deleting a unit removes its child sessions
  - Created `backend/schemas/session.py` — `SessionCreate` (session_type, duration 1–4 validated); `SessionUpdate` (both optional, same duration guard); `SessionResponse`; `SchedulableSessionResponse` (includes derived lecturer display name, student count)
  - Created `backend/services/session.py` — `list_sessions_for_unit`, `create_session`, `update_session`, `delete_session`, `list_schedulable_sessions`; schedulable filter requires unit to have a lecturer; student count derived from parent unit; no assignment behavior
  - Created `backend/api/sessions.py` — two routers: `unit_sessions_router` (`GET/POST /units/{unit_id}/sessions`) and `sessions_router` (`GET /sessions/schedulable`, `PUT /sessions/{session_id}`, `DELETE /sessions/{session_id}`); all routes require `get_current_admin`
  - Created `backend/alembic/versions/0006_create_sessions.py` — creates `sessiontype` Postgres enum and `sessions` table; revises `0005`
  - Updated `backend/models/__init__.py` — registers `Session`
  - Updated `backend/api/router.py` — registered both session routers

- **Unit 26: Frontend Session API Client**
  - Created `frontend/src/lib/api/sessions.ts` — `SessionType` union (`lecture | tutorial | lab | workshop`); `Session` DTO; `SessionCreate`, `SessionUpdate` request types; `SchedulableSession` DTO (includes derived lecturer display name, student count); `listUnitSessions`, `createUnitSession`, `updateSession`, `deleteSession`, `listSchedulableSessions` API functions; `parseSessionError` helper for 404/422
  - All functions use the Unit 6 `apiRequest` authenticated base client; token attachment and 401 handling delegated to base client
  - Paths match Unit 25 backend routes exactly: `/units/{unitId}/sessions` for unit-scoped routes, `/sessions/{sessionId}` for top-level, `/sessions/schedulable` for schedulable listing
  - No TanStack Query hooks, no Zustand, no `/units` page wiring, no mock data
  - Build succeeds with zero TypeScript errors

- **Unit 27: Frontend Session Management Integration**
  - Rewrote `frontend/src/routes/units.tsx` with full session persistence:
    - `UnitTableRow` component — per-unit `useQuery(['unit-sessions', unit.id], listUnitSessions)` shows live session count in Sessions column; counts update automatically after mutations
    - `UnitSessionsPanel` component — rendered inside the edit unit dialog; fetches sessions with `['unit-sessions', unitId]`; "Add session" button calls `createUnitSession` with default `{session_type: 'lecture', duration: 1}`; invalidates query on success; loading and error states on both query and mutation
    - `SessionBoxLive` component — inline session box with per-session `updateMutation` (fires on session type or duration select change) and `deleteMutation` (requires inline delete/cancel confirmation before firing); `Loader2` spinner shown while update is in progress; both errors displayed inline below the box
    - `UnitFormFields` — sessions section removed (sessions require an existing unit); create dialog updated description to note sessions are added after creation
    - `SessionType` values use backend enum strings (`lecture`/`tutorial`/`lab`/`workshop`); `DURATION_OPTIONS` values are integers 1–4
    - All session mutations invalidate `['unit-sessions', unitId]`; unit mutations still invalidate `['units']`
    - No Zustand, no localStorage, no mock sessions
  - Build succeeds with zero TypeScript errors

- **Unit 28: Frontend Unscheduled Pool Shell**
  - Created `frontend/src/features/timetable/unitColors.ts` — `getUnitColor(identifier)` deterministic helper; hashes identifier string into one of 6 color variant names (maroon, gold, blue, green, purple, stone); no hex values
  - Created `frontend/src/features/timetable/UnscheduledSessionCard.tsx` — compact card prepared for `SchedulableSession` DTO; displays session type, unit code, unit name, duration, lecturer display name, student count; left-border accent driven by unit color variant tokens; `minWidth: 180px`, `maxWidth: 240px`; no drag-and-drop
  - Created `frontend/src/features/timetable/UnitGroup.tsx` — groups session cards under a unit label row (code, name, session count); accepts `UnitColorVariant` and `SchedulableSession[]`
  - Created `frontend/src/features/timetable/UnscheduledPool.tsx` — pool panel with heading, helper text, empty state (`CalendarPlus` icon, "No schedulable sessions yet", link to `/units`), and unit-bucket rendering when sessions provided; `buildUnitBuckets` groups sessions by `unit_id`; no backend calls
  - Updated `frontend/src/routes/timetable.tsx` — `UnscheduledPool` imported and rendered below `TimetableGrid` only in the rooms-exist grid state; all other states (loading, error, no-room) unchanged
  - No drag-and-drop, no TanStack Query wiring for sessions, no mock data, no backend calls
  - Build succeeds with zero TypeScript errors

- **Unit 29: Frontend Unscheduled Pool Integration**
  - Added `useQuery({ queryKey: ['schedulable-sessions'], queryFn: listSchedulableSessions })` in `timetable.tsx`; sessions query runs independently from the rooms query so pool and grid states are decoupled
  - `UnscheduledPool` updated to accept `isLoading`, `isError`, `error` props; renders spinner loading state, error message, empty state, or grouped session cards depending on query state
  - `buildUnitBuckets` now sorts buckets by unit code (alphabetical) and sorts sessions within each bucket by session type order (lecture → tutorial → lab → workshop) then by duration
  - `['schedulable-sessions']` query key is invalidated on `createMutation`, `editMutation`, and `deleteMutation` success in `units.tsx`, so the pool refreshes automatically after unit/session changes
  - No mock data, no Zustand, no scheduling/assignment/drag-drop behavior added
  - Build succeeds with zero TypeScript errors

- **Unit 30: Frontend Scheduled Session Rendering Shell**
  - Created `frontend/src/features/timetable/assignment.ts` — `SlotId` union (`s1`–`s7`, no `s8`); `TimetableAssignment` frontend rendering model with all fields needed to place and display a session (assignment_id optional, session_id, unit_id, unit_code, unit_name, session_type, duration, lecturer_display_name, student_count, day, start_slot, room_id); aligns with future Unit 32 backend DTO
  - Created `frontend/src/features/timetable/ScheduledSessionCard.tsx` — `position: absolute inset-x-0 top-0`, height `calc(N * 3.5rem)` where N = duration; left-border 4px accent (vs 3px on unscheduled cards); shows unit code, abbreviated session type, lecturer name (truncated), student count (duration > 1 only); uses `getUnitColor` + same BG/accent token maps as `UnscheduledSessionCard`; `z-index: 10` to overlay subsequent slot rows; `overflow: hidden`; distinct from unscheduled cards by layout, width behavior, and content
  - Updated `frontend/src/features/timetable/GridCell.tsx` — added `position: relative` (`className="relative h-14 …"`); accepts `assignment?: TimetableAssignment`; renders `ScheduledSessionCard` when assignment is present; suppresses hover state when a card is rendered
  - Updated `frontend/src/features/timetable/TimetableGrid.tsx` — accepts `assignments?: TimetableAssignment[]` (defaults to `[]`); `buildAssignmentMap` indexes assignments by `"${day}:${roomId}:${slotId}"`; each `GridCell` receives `assignment={assignmentMap.get(key)}` — undefined when no assignment starts at that cell; grid renders blank as before when `assignments=[]`
  - Updated `frontend/src/routes/timetable.tsx` — passes `assignments={[]}` to `TimetableGrid`; no backend calls, no fake data
  - No drag-and-drop, no assignment API client, no backend calls, no mock assignments in production UI
  - Build succeeds with zero TypeScript errors

- **Unit 31: Backend Saved Timetable Assignment Persistence and Protected Save API**
  - Created `backend/models/assignment.py` — `TimetableAssignment` SQLAlchemy model; `session_id` FK with CASCADE on session delete; `room_id` FK with CASCADE on room delete (satisfies room-deletion unschedule requirement); reuses existing Postgres `availabilityday`/`availabilityslot` enum types via `create_type=False`; unique constraint on `session_id` (one assignment per session); unique constraint on `(day, start_slot, room_id)` for same-start-slot double-booking guard at DB level
  - Created `backend/alembic/versions/0007_create_assignments.py` — creates `timetable_assignments` table; references existing Postgres enum types without recreation
  - Created `backend/schemas/assignment.py` — `AssignmentItem` (session_id, day, start_slot, room_id); `AssignmentSaveRequest` (list of AssignmentItem); `AssignmentResponse` (all joined display fields per spec: assignment_id, session_id, unit_id, unit_code, unit_name, session_type, duration, lecturer_display_name, student_count, day, start_slot, room_id, timestamps)
  - Created `backend/services/assignment.py` — `list_assignments` (eager-loads session→unit→lecturer/students via selectinload); `save_assignments` (replace-all transaction with full defensive validation); `clear_assignments` (optional clear); defensive checks reject: duplicate session in request, session/room not found, room capacity < student count, session crossing lunch, session running off timetable, room double-booking (multi-slot overlap detection, not just same-start-slot); warning-level conflicts (lecturer/student/availability) are not checked and are allowed through
  - Created `backend/api/assignments.py` — `GET /assignments` (list), `POST /assignments` (save/replace), `DELETE /assignments` (clear); all routes require `get_current_admin`
  - Updated `backend/models/__init__.py` — registered `TimetableAssignment`
  - Updated `backend/api/router.py` — registered assignments router
  - Room deletion cascade: handled by `ondelete="CASCADE"` FK constraint in migration; no service-layer changes needed; Postgres removes orphaned assignments automatically when a room is deleted

- **Unit 32: Frontend Assignment API Client**
  - Created `frontend/src/lib/api/assignments.ts` — `AssignmentResponse` DTO (all display fields matching backend `AssignmentResponse`); `AssignmentItem` (session_id, day, start_slot, room_id — save request per-assignment); `AssignmentSaveRequest` (wraps list of `AssignmentItem`); `listAssignments()` (`GET /assignments`); `saveAssignments(input)` (`POST /assignments`); `clearAssignments()` (`DELETE /assignments`)
  - `AvailabilityDay` and `AvailabilitySlot` imported from `@/lib/api/lecturers` (canonical source, avoids duplication); `SessionType` imported from `@/lib/api/sessions`
  - `parseAssignmentSaveError` handles backend defensive rejections (404, 409, 422) as save errors; not normal validation UX
  - All functions use `apiRequest` from authenticated base client; no draft state, no drag-drop, no validation logic added
  - Build succeeds with zero TypeScript errors

- **Unit 33: Frontend Timetable Draft Assignment State**
  - Added `useQuery(['assignments'], listAssignments)` in `timetable.tsx` to load saved assignments from the backend
  - Added `useState<TimetableAssignment[]>` draft initialized from saved assignments via `useEffect`; draft resets clean whenever saved assignments refetch
  - `isDirty: boolean` and `saveError: string | null` state tracks unsaved changes and save failure independently
  - `useMutation(saveAssignments)` sends the complete draft as `AssignmentSaveRequest` on save; on success invalidates `['assignments']` (triggering refetch and draft reset); on error preserves draft and surfaces `saveError`
  - `TimetableGrid` now receives `assignments={draft}` instead of `[]` — scheduled cards render from the draft set
  - `TimetableActionBar` rewritten with props (`isDirty`, `isSaving`, `saveError`, `onSave`): Save Timetable button (enabled when dirty, spinner while saving); unsaved-changes label visible when dirty; save error shown inline; placeholder text shown when clean and no error
  - `toTimetableAssignment` helper converts `AssignmentResponse` → `TimetableAssignment` (field-aligned, no casting needed)
  - Build succeeds with zero TypeScript errors

- **Unit 34: Frontend Manual Scheduling Controls**
  - `pendingSessionId: string | null` state in `timetable.tsx` — shared selection for both fresh placement (from pool) and move (from grid)
  - `handleSelectSession` toggles pending: clicking same session again cancels selection; clicking a different session switches it
  - `handleCellClick(day, slotId, roomId)` places the pending session — removes any existing draft entry for that session_id first (handles move), builds a new `TimetableAssignment` from the schedulable sessions list, appends to draft, sets `isDirty`, clears pending
  - `handleUnschedule(sessionId)` removes the session from draft, sets `isDirty`, also clears pending if the removed session was pending
  - `unscheduledSessions` derived in `timetable.tsx` — `schedulableSessions` filtered to exclude any session_id already present in `draft`; pool receives this filtered list
  - `ScheduledSessionCard` — added `×` unschedule button (top-right, stopsPropagation); card body click calls `onMoveSelect`; `isPending` shows outline ring and reduced opacity
  - `GridCell` — `isDropTarget = !!pendingSessionId && !assignment`; drop-target cells show hover highlight and pointer cursor; click fires `onCellClick`; passes `isPending`, `onUnschedule`, `onMoveSelect` to `ScheduledSessionCard`
  - `TimetableGrid` — added `pendingSessionId`, `onCellClick`, `onUnschedule`, `onMoveSelect` props; each GridCell closure captures its `day`, `slot.id`, `room.id`
  - `UnscheduledSessionCard` — added `isSelected` (outline ring) and `onClick` props; `cursor-pointer`
  - `UnitGroup` — passes `pendingSessionId` and `onSelectSession` through to each card
  - `UnscheduledPool` — passes `pendingSessionId` and `onSelectSession` through to each `UnitGroup`
  - `TimetableActionBar` — added `isPendingPlacement` prop; shows placement hint in accent color when a session is selected; hint message overrides normal status text (not the save error)
  - `pendingSessionId` also cleared when `savedAssignments` refetch resets the draft
  - Build succeeds with zero TypeScript errors

- **Unit 35: Frontend Blocking Validation Engine**
  - Created `frontend/src/lib/validation/blocking.ts` — pure validation helpers; `BlockingIssueType` union (`room_double_booking`, `room_capacity_too_small`, `session_crossing_lunch`, `session_off_timetable`); `BlockingIssue` interface (type, severity: `'blocking'`, affected_session_ids, optional affected_room_id / affected_day / affected_slot, human-readable message)
  - `checkProposedPlacement(proposed, existingDraft, rooms)` — checks a single proposed assignment against the current draft and room data before it enters the draft; excludes the pending session's own old position so moves don't self-conflict; returns all blocking issues found
  - `checkDraftForBlockingViolations(draft, rooms)` — checks all current draft assignments for off-timetable, lunch-crossing, room-capacity, and room-double-booking violations
  - `getBlockingViolatorIds(draft, rooms)` — returns a `Set<string>` of session IDs that violate any blocking rule; used for automatic unscheduling
  - `isOffTimetable`: `SLOT_INDEX[startSlot] + duration > 7` (7 total slots); `crossesLunch`: occupied indices span both AM (indices 0–2) and PM (indices 3–6); `rangesOverlap`: standard interval overlap test
  - `timetable.tsx`: added `blockingError: string | null` state; added `draftRef` (synced via effect) so data-change effects can read the latest draft without dependency array issues; `handleCellClick` now calls `checkProposedPlacement` before placing — sets `blockingError` and returns early on any blocking issue, clears it and places on success; `handleSelectSession` and `handleUnschedule` clear `blockingError`
  - Auto-unschedule effect runs when `rooms` or `schedulableSessions` query data changes: builds a validation copy of the draft with current session student counts, finds violators via `getBlockingViolatorIds`, removes them from draft, sets `isDirty = true`; no-op when draft is empty
  - `TimetableActionBar`: added `blockingError?: string | null` prop; shown in error style with "Cannot place session: …" prefix; priority below `saveError`, above pending placement hint
  - Build succeeds with zero TypeScript errors

- **Unit 36: Frontend Warning Validation Engine**
  - Created `frontend/src/lib/validation/warning.ts` — pure warning validation helpers; `WarningIssueType` union (`lecturer_overlap`, `unit_session_overlap`, `lecturer_unavailable`); `WarningIssue` interface (type, severity: `'warning'`, affected_session_ids, optional affected_lecturer_id / affected_student_ids / affected_day / affected_slot, human-readable message)
  - `checkDraftForWarnings(draft, lecturers?)` — checks all draft assignments for three warning rules: (1) lecturer overlap: two assignments with the same `lecturer_display_name` on the same day with overlapping slots; (2) unit/session overlap: two assignments from the same `unit_id` on the same day with overlapping slots (detects student conflicts via unit enrollment); (3) lecturer unavailable: assignment occupies a slot marked unavailable by the lecturer; returns all warning issues found
  - `getWarningSessionIds(draft, lecturers?)` — returns `Set<string>` of session IDs with any warning, used for grid visual flagging
  - `ScheduledSessionCard`: added `hasWarning?: boolean` prop; when true, changes border color to `--state-warning` and shows `AlertTriangle` icon (h-3 w-3) alongside the unschedule button for accessible non-color-only indicator
  - `GridCell`: added `hasWarning?: boolean` prop; passes through to `ScheduledSessionCard`
  - `TimetableGrid`: added `warningSessionIds?: Set<string>` prop; computes `hasWarning` per cell using `warningSessionIds.has(assignment.session_id)` and passes to `GridCell`
  - `TimetableActionBar`: added `warningCount?: number` and `canRunSolver?: boolean` props; shows amber warning summary with `AlertTriangle` icon when warnings exist ("N scheduling warnings — solver blocked until resolved"); updated placeholder text
  - `timetable.tsx`: added `useQuery(['lecturers'], listLecturers)` for availability data; computes `warningIssues` and `warningSessionIds` inline from draft + lecturers; computes `canRunSolver = !hasBlockingViolations && warningIssues.length === 0`; passes `warningSessionIds` to `TimetableGrid`, `warningCount` and `canRunSolver` to `TimetableActionBar`
  - Warning placements remain scheduled and saveable; solver gate (`canRunSolver`) is false whenever any blocking or warning issue exists
  - Build succeeds with zero TypeScript errors

- **Unit 37: Frontend Validation Display and Solver Gate Shell**
  - `TimetableActionBar`: replaced `warningCount` prop with `violationMessages: string[]` and `warningMessages: string[]`; added `onRunSolver?: () => void` prop
  - Added **Run Solver** button shell (using `--solver-accent` blue); enabled only when `canRunSolver` is true; HTML `title` attribute carries the full blocked reason for browser tooltip
  - Solver blocked reason message in status area: "N blocking violations and N scheduling warnings must be resolved before running the solver" — uses error color when violations present, warning color when warnings only
  - Added expandable **violation detail panel** (toggled via "View details (N)" / "Hide details" chevron button); renders below the main action bar row with `--bg-muted` background; lists blocking violations with `XCircle` icon and warning issues with `AlertTriangle` icon, grouped by severity
  - `timetable.tsx`: changed from computing just `hasBlockingViolations: boolean` to `blockingViolations: BlockingIssue[]` (full array); passes `.map(v => v.message)` and `warningIssues.map(i => i.message)` to the action bar
  - Verification checklist: blocking rejection visible ✓; warning cards marked ✓; warning detail messages accessible via "View details" ✓; solver disabled when any issue exists ✓; solver disabled state explains why ✓; no backend validation API added ✓
  - Build succeeds with zero TypeScript errors

- **Unit 38: Frontend Drag-and-Drop Scheduling Shell**
  - Installed `@dnd-kit/core` and `@dnd-kit/utilities` (already present in package.json)
  - `GridCell`: added `useDroppable` with id `${day}:${roomId}:${slotId}`; `disabled: !!assignment` so occupied start-slot cells don't accept drops; `isOver` drives drop-target highlight; highlight logic combines DnD `isOver` and existing click-mode `isClickDropTarget && hovered`
  - `UnscheduledSessionCard`: added `useDraggable` with `id={session.session_id}`; spreads `{...listeners}` and `{...attributes}` on card div; `isDragging` reduces opacity to 0.3; CSS transform intentionally not applied (DragOverlay handles visual movement)
  - `ScheduledSessionCard`: added `useDraggable` with `id={assignment.session_id}`; spreads `{...listeners}` and `{...attributes}` on card div; `isDragging` reduces opacity to 0.3; X (unschedule) button gets `onPointerDown={stopPropagation}` to prevent drag starting from the unschedule button
  - `timetable.tsx`: `DndContext` wraps the full timetable canvas (action bar + grid + pool); `PointerSensor` with `activationConstraint: { distance: 5 }` prevents accidental drags; `KeyboardSensor` for accessibility; `activeSessionId` state tracks dragged session for overlay; `handleDragStart` sets `activeSessionId`, clears `pendingSessionId` and `blockingError`; `handleDragEnd` parses droppable id (`day:roomId:slotId`), calls `checkProposedPlacement`, rejects with `blockingError` on blocking violations, updates draft on success; `handleDragCancel` clears `activeSessionId`; `DragOverlay` with `dropAnimation={null}` renders `DragPreviewCard` while dragging
  - `DragPreviewCard`: private component in `timetable.tsx`; renders session info (unit code, type, unit name, duration, lecturer) matching unscheduled card size; uses same design token maps
  - Warning recalculation is automatic — warning state is derived from draft on every render; no extra wiring needed
  - Manual click-based scheduling controls remain fully functional alongside drag-and-drop
  - Build succeeds with zero TypeScript errors

- **Unit 39: Frontend Drag-and-Drop Save Integration**
  - No new code required — all Unit 39 scope was already delivered by Units 33 and 38 combined
  - Drag/drop changes mark draft dirty: `handleDragEnd` sets `setIsDirty(true)` after every successful placement
  - Save sends the complete draft: `saveMutation` maps `draft` to `AssignmentItem[]` and calls `saveAssignments`
  - Save loading state: `isSaving={saveMutation.isPending}` passed to `TimetableActionBar`; spinner shown while pending
  - Successful save refetches: `onSuccess` calls `queryClient.invalidateQueries({ queryKey: ['assignments'] })`
  - Dirty-state reset: `useEffect` on `savedAssignments` calls `setIsDirty(false)` and `setSaveError(null)` when the refetch lands
  - Failed save preserves draft: `onError` only sets `saveError`; draft state is untouched
  - Backend defensive rejections visible as save errors: `parseAssignmentSaveError` parses 404/409/422 responses into readable messages; `saveError` displayed in `TimetableActionBar`
  - Build succeeds with zero TypeScript errors

- **Unit 40: Backend Solver Constraint Mirror**
  - Created `backend/constraints/types.py` — `ConstraintType` enum (8 values: room_double_booking, room_capacity, lunch_crossing, off_timetable, lecturer_overlap, student_overlap, unit_session_overlap, lecturer_availability); `ConstraintSeverity` enum (blocking, warning); `CONSTRAINT_SEVERITY` dict mapping each type to its severity (mirrors frontend blocking/warning split); `SessionInput`, `RoomInput`, `LecturerInput`, `AssignedSession` frozen dataclasses for ORM-detached solver input; `SolverConstraint` dataclass for violation output
  - Created `backend/constraints/graph.py` — slot order constants (s1–s7, AM/PM boundary at index 3); `ConflictEdge` and `ConflictGraph` dataclasses; `ConflictGraph.neighbors()` and `conflicts_between()` methods; `derive_lecturer_overlap_conflicts`, `derive_student_overlap_conflicts`, `derive_unit_session_overlap_conflicts` structural derivation functions; `build_conflict_graph` aggregator; assignment-based violation checks: `check_room_double_booking`, `check_room_capacity`, `check_lunch_crossing`, `check_off_timetable`, `check_lecturer_overlap`, `check_student_overlap`, `check_unit_session_overlap`, `check_lecturer_availability`; `compile_assignment_violations` aggregator
  - Created `backend/constraints/__init__.py` — re-exports all public symbols
  - Created `backend/tests/__init__.py` and `backend/tests/test_constraints.py` — 38 pytest fixture-based tests covering all constraint types, conflict graph construction, positive and negative cases
  - Added `pytest>=8.0.0` to `backend/requirements.txt`
  - No user-facing validation API added; no solver CP-SAT model; module is pure Python with no SQLAlchemy dependencies
  - All 38 tests pass

- **Unit 41: Backend Solver Input Snapshot Builder**
  - Created `backend/solver/types.py` — `RoomSnapshot`, `SessionSnapshot`, `AvailabilitySnapshot`, `LockedAssignment`, `TimetableConstants`, `SolverInputSnapshot` frozen dataclasses; `ORDERED_DAYS` (Monday–Friday), `ORDERED_SLOTS` (s1–s7), `AM_SLOTS`, `PM_SLOTS`, `AM_PM_BOUNDARY_INDEX` (3), `SESSION_TYPE_ORDER`; `TIMETABLE_CONSTANTS` singleton with all schedule grid constants
  - Created `backend/solver/snapshot.py` — `SnapshotIntegrityError` for defensive rejections; `build_snapshot_from_data(rooms, sessions, availability, saved_assignments)` pure builder (no DB access); `build_solver_input_snapshot(db)` DB loader using SQLAlchemy selectinload; `_validate_saved_assignments` defensive checks (missing session, missing room, invalid slot, lunch crossing, off timetable, room capacity, room double-booking); `_extract_conflict_pairs` per-category deduplication from Unit 40 conflict graph; all output collections sorted deterministically (rooms by name, sessions by unit_code/type_order/id, availability by lecturer_id, assignments by day/room/slot/session_id, conflict pairs sorted)
  - Created `backend/solver/__init__.py` — re-exports all public symbols
  - Created `backend/tests/test_snapshot.py` — 41 fixture-based tests covering: DTO fields, timetable constants (Monday–Friday, s1–s7, AM/PM blocks, lunch gap), snapshot population from all input types, locked assignment extraction, unscheduled session derivation, all three conflict graph types, deterministic ordering for all collections, all 7 defensive integrity checks (missing session, missing room, invalid slot, lunch crossing, off timetable, room capacity, room double-booking including multi-slot overlap), valid edge cases (empty snapshot, no students, AM-only, PM-only)
  - No CP-SAT model, no solver API, no new packages; builder does not query frontend draft state or create/modify assignments
  - All 41 tests pass

- **Unit 42: Backend Solver CP-SAT Module**
  - Added `ortools>=9.15.6755` to `backend/requirements.txt` (installed in backend venv; only this unit uses it)
  - Extended `backend/solver/types.py` with output DTOs — `SolverStatus` enum (`optimal`, `feasible`, `infeasible`, `unknown`); `GeneratedAssignment` frozen dataclass (session_id, day, start_slot, room_id, duration); `SolverRunResult` (status, generated_assignments, locked_assignments preserved from input, unscheduled_session_ids, scheduled_count, unscheduled_count, timed_out, concise message)
  - Created `backend/solver/model.py` — `solve_timetable(snapshot, time_limit_seconds=30.0)` entry function; accepts only a `SolverInputSnapshot`, never touches the DB/API/ORM/frontend draft
  - Modeling: one optional BoolVar per feasible (day, start slot, room) candidate per unscheduled session plus a `scheduled` BoolVar with `sum(candidates) == scheduled` (optional placement → partial results; a session with zero candidates is forced unscheduled, never dropped); candidates violating static hard constraints are never created — room capacity, lunch crossing (must fit AM or PM block), off-timetable, lecturer unavailability (hard for solver even though warning-only in frontend), overlap with locked room occupancy, and overlap with a locked conflict partner's time cells
  - Locked saved assignments are fixed occupied intervals: no variables, excluded at candidate level, returned unchanged; locked-vs-locked warning conflicts (e.g. saved placement during lecturer unavailability) are tolerated and cannot make the model infeasible
  - Constraints: per-(day, room, slot) `AddAtMostOne` for room no-overlap; lecturer/student/unit-session conflict pairs (merged + deduped from the snapshot's three pair lists) enforced as per-time-cell `sum(occ_a) + sum(occ_b) <= 1` between unscheduled pairs; duration contiguity inherent in candidate construction
  - Objective: maximize count of scheduled previously-unscheduled sessions; no soft preferences
  - Determinism: `num_search_workers = 1`, `random_seed = 0`, deterministic model-build order from sorted snapshot collections; explicit timeout via `max_time_in_seconds` with `timed_out` flag (true when search stopped without optimality proof)
  - Updated `backend/solver/__init__.py` — re-exports `solve_timetable`, `DEFAULT_TIME_LIMIT_SECONDS`, `SolverStatus`, `GeneratedAssignment`, `SolverRunResult`
  - Created `backend/tests/test_solver.py` — 24 deterministic fixture tests: result shape, empty snapshot, single placement, locked preservation, locked room occupancy blocking, locked conflict partner blocking, saved warning-state tolerance, room capacity (direct + failure), room no-overlap, lecturer no-overlap (partial + spread), student no-overlap, unit/session overlap in isolation, availability forced placement, fully unavailable lecturer, duration-4 PM-only start, duration exceeding available block, lunch-crossing prevention, objective maximization, explicit partial result, determinism (two runs equal), input snapshot not mutated; shared `assert_valid_solution` helper re-checks every hard constraint on solver output
  - No DB result application, no snapshot builder changes, no Trigger.dev job, no solver API, no frontend changes
  - All 103 backend tests pass

- **Unit 43: Backend Solver Result Application Service**
  - Created `backend/solver/apply.py` — `apply_solver_result(db, result)` service that applies a `SolverRunResult` to the saved timetable assignment state; lives inside `backend/solver/` with a clean solver-facing boundary and is **not** wired to any request handler or job
  - Structured internal result object `SolverResultApplication` (status, scheduled_count, unscheduled_count, is_partial, newly_scheduled_session_ids, remaining_unscheduled_session_ids, preserved_locked_count, concise message) for future solver job/API status surfacing; `ApplicationStatus` enum (`applied`, `partial`, `failed`); `SolverResultApplicationError` (code + message) raised after rollback
  - Locked preservation: the existing rows in `timetable_assignments` are treated as the authoritative locked solver inputs — they are never deleted or mutated; generated placements are inserted alongside them. `preserved_locked_count` reflects the actual saved rows
  - Persistence: generated assignments for previously unscheduled sessions are inserted in a single transaction (`db.commit()`); no `duration` column on the assignment row (duration derives from the session, per Unit 31)
  - Failure safety: a result whose status is not `OPTIMAL`/`FEASIBLE` (or a missing/invalid result object) triggers `db.rollback()` and raises `SolverResultApplicationError` (`solver_failed` / `invalid_result`) without mutating saved state; persistence errors roll back and raise `persistence_failed`
  - Defensive validation (backend safety, runs before commit, rolls back on first violation): duplicate generated session, unknown session id, unknown room id, invalid slot id (must be `s1`-`s7`), invalid day, lunch crossing, off-timetable, room capacity < student count, generated overwriting a locked saved assignment (`would_overwrite_locked`), and room double-booking — generated-vs-locked and generated-vs-generated — all surfaced as `blocking_integrity_violation`
  - Partial-success metadata: `is_partial = unscheduled_count > 0`; status `partial` when sessions remain unscheduled, `applied` otherwise
  - Diagnostic logging via `structlog.get_logger` (`solver_result_applied`, `solver_result_apply_rejected`, `solver_result_apply_failed`) bound with solver status and counts
  - Updated `backend/solver/__init__.py` — re-exports `apply_solver_result`, `ApplicationStatus`, `SolverResultApplication`, `SolverResultApplicationError`
  - Created `backend/tests/conftest.py` — isolated in-memory SQLite `db` fixture (FK enforcement enabled) so DB-backed services can be tested without Postgres; no new package required
  - Created `backend/tests/test_apply.py` — 19 tests using real ORM models + real solver DTOs: happy-path persistence, metadata counts, locked preservation, partial result, failed-result no-mutation (infeasible + unknown), invalid result object, all defensive checks (duplicate, unknown session/room, invalid slot, lunch crossing, off-timetable, capacity, overwrite-locked, double-book vs locked + vs each other), transaction rollback leaves no partial state, empty-result no-op
  - No CP-SAT modeling, snapshot builder, Trigger.dev job, solver API route, or frontend changes added
  - All 122 backend tests pass

- **Unit 44: Jobs Boundary and Trigger.dev Setup**
  - Created top-level `jobs/` — a standalone Node/TypeScript Trigger.dev v3 project sitting beside `frontend/` and `backend/` (Trigger.dev is Node-based, so it cannot live inside the Python `backend/` package); this matches the `jobs/` boundary in `architecture-context.md`
  - `jobs/package.json` — declares `@trigger.dev/sdk ^3.3.17` (resolved + installed 3.3.17) and dev deps `trigger.dev` (CLI), `typescript`, `@types/node`; scripts `login`, `dev`, `deploy`, `typecheck`
  - `jobs/trigger.config.ts` — `defineConfig` with `runtime: "node"`, `logLevel: "info"`, `maxDuration: 60`, `dirs: ["./src/trigger"]`; `project` is a placeholder ref (`proj_REPLACE_WITH_YOUR_PROJECT_REF`) that must be replaced with the dashboard project ref before `npm run dev` connects
  - `jobs/src/trigger/testJob.ts` — minimal registered `test-job` task; typed `TestJobPayload` (message, correlationId, timestamp) + `TestJobResult`; emits structured `test_job_started` / `test_job_completed` logs via `logger.info` and returns a completion object; contains **no** solver logic, no timetable queries, no DB access, no result persistence — comments document the orchestration-only boundary
  - `jobs/tsconfig.json` — strict TypeScript, `noEmit`, ES2022/Bundler resolution; `npm run typecheck` passes clean
  - `jobs/.env.example` — documents `TRIGGER_ACCESS_TOKEN` (non-interactive auth) and `TRIGGER_SECRET_KEY` (only for backend-triggered tasks, future); notes the project ref lives in `trigger.config.ts` and that backend service URL/token for solver orchestration are intentionally omitted this unit
  - `jobs/README.md` — boundary rules, env table, local dev commands (`npm install` → `npm run login` → `npm run dev`), how the Trigger.dev dev server differs from running FastAPI, and an explicit "not yet implemented" list (async solver job, start/status API, job-driven persistence, deployment wiring)
  - `jobs/.gitignore` — ignores `node_modules/`, `.trigger/`, `dist/`, `.env`
  - Updated `context/code-standards.md` File Organization — reconciled `backend/jobs/` → top-level `jobs/` with rationale
  - No solver job wired, no solver start/status API, no frontend changes, no backend Python changes

- **Unit 45: Async Solver Job**
  - Created `backend/solver/job.py` — backend-side execution of the async solver job; `SolverJobStatus` enum (`completed`/`partial`/`failed`); frozen `SolverJobPayload` (solver_run_id, correlation_id, optional admin_workspace_id + snapshot_id; `from_dict` parser) — a stable *reference*, never frontend draft state; `SolverJobResult` dataclass (status, solver_run_id, correlation_id, solver_status, sessions_attempted/scheduled/unscheduled, is_partial, timed_out, duration_seconds, started_at, completed_at, message, failure_code, newly_scheduled/remaining_unscheduled ids; `to_dict` for JSON); `run_solver_job(db, payload, *, time_limit_seconds=30.0)` runs the full pipeline `build_solver_input_snapshot` (Unit 41) → `solve_timetable` (Unit 42) → `apply_solver_result` (Unit 43) from **saved** DB state only
  - Failure safety: every step wrapped; snapshot/solve/apply exceptions roll back defensively and return a `failed` `SolverJobResult` (never raises to the caller); the Unit 43 service already rolls back unapplicable results (`solver_failed`). Saved assignment state is guaranteed unchanged on failure. Failure codes: `snapshot_integrity`, `snapshot_error`, `solver_error`, `solver_failed`, `apply_error`
  - Structured logging via `structlog`: `solver_job_started` (bound solver_run_id + correlation_id, started_at), `solver_job_completed` (status, solver_status, duration_seconds, sessions attempted/scheduled/unscheduled, is_partial, timed_out), `solver_job_failed` (step, failure_code, detail)
  - Job contains **no** solver modeling logic — it orchestrates the three backend services; `sessions_attempted = len(snapshot.unscheduled_session_ids)`
  - Created `backend/solver/job_cli.py` — thin process boundary (`python -m solver.job_cli`) the Node Trigger.dev task invokes; self-bootstraps `sys.path` with the backend dir; routes structlog to **stderr** so stdout carries only the result; reads `SolverJobPayload` JSON (stdin or argv[1]), opens a real `SessionLocal`, runs the job, prints `SolverJobResult` JSON on stdout; **always** emits a structured JSON failure doc on stdout even for a malformed payload (`invalid_payload`) or backend setup/import/connection error (`bridge_setup_error`) so the caller can always parse an outcome; exit 0 on completed/partial, 1 on failed; no business logic
  - Updated `backend/solver/__init__.py` — re-exports `run_solver_job`, `SolverJobPayload`, `SolverJobResult`, `SolverJobStatus`
  - Created `jobs/src/trigger/solverJob.ts` — registered `solver-job` Trigger.dev task; typed `SolverJobPayload` (solverRunId, correlationId, optional adminWorkspaceId/snapshotId) + `SolverJobResult`; owns job lifecycle (`solver_job_started`/`solver_job_completed`/`solver_job_failed` logs, timing) and spawns the Python bridge via `child_process` using `python -m solver.job_cli` with `cwd=BACKEND_DIR` + `PYTHONPATH=BACKEND_DIR` (the `-m` form keeps cwd—not `solver/`—on sys.path so stdlib `types` isn't shadowed by `solver/types.py`); validates `BACKEND_DIR`/script exist up front and surfaces Python **stderr** + exit code in the failure result when stdout has no parseable JSON; scans stdout for the last JSON line (robust to log pollution); `BACKEND_DIR` must be an absolute path and `PYTHON_BIN` may be absolute / relative-to-BACKEND_DIR / a PATH command; maps snake_case result doc → camelCase TS result; passes only stable references across the boundary; `maxDuration: 120` (CP-SAT default 30s + headroom); contains no solver/DB logic
  - Verified end-to-end against the real database: the bridge solved 8 sessions optimally, emitted clean stdout JSON, and persisted the generated assignments (`status: completed`)
  - Created `backend/tests/test_solver_job.py` — 13 tests: payload parsing (minimal/full/missing-field), empty DB no-op, end-to-end single-session schedule + persistence, locked preservation + new placement, partial result (room too small → forced unscheduled), payload echo, timing metadata, JSON-serializable result, and three failure paths via monkeypatch (solver exception, unapplicable `UNKNOWN` status, snapshot exception) each asserting `failed` status + saved state preserved
  - Updated `jobs/.env.example` (documented `BACKEND_DIR` / `PYTHON_BIN` bridge vars; backend reads its own `DATABASE_URL`) and `jobs/README.md` (async solver job section, pipeline diagram, boundary properties)
  - No solver start/status API, no frontend solver client/polling/UI, no production Trigger.dev deployment, no soft constraints, no new packages
  - All 135 backend tests pass; `jobs/` `npm run typecheck` passes clean

- **Unit 46: Backend Solver Start and Status API**
  - Created `backend/models/solver_run.py` - persisted `solver_runs` model with explicit API statuses (`pending`, `running`, `succeeded`, `failed`), Trigger job id, correlation id, admin requester id, scheduled/unscheduled counts, partial-success flag, failure code/message, and timestamps
  - Created `backend/alembic/versions/0008_create_solver_runs.py` - adds the `solverrunstatus` enum and `solver_runs` table with status/created indexes
  - Created `backend/schemas/solver.py` - frontend-friendly `SolverRunStatusResponse` that exposes run id, status, optional job id, timestamps, counts, partial-success metadata, and sanitized failure message only
  - Created `backend/services/trigger_client.py` - stdlib-only Trigger.dev dispatch client using `TRIGGER_SECRET_KEY`, optional `TRIGGER_API_URL`, and `TRIGGER_SOLVER_TASK_ID` (`solver-job` default); posts a JSON payload packet to `/api/v1/tasks/{taskId}/trigger` and returns the run id; no new backend package added
  - Created `backend/services/solver_run.py` - start/status service: rejects active `pending`/`running` runs, builds the solver input snapshot from saved DB state for defensive integrity checks, handles no-work/all-scheduled cases as harmless `succeeded` runs, requires rooms when there is unscheduled work, queues only stable references (`solverRunId`, `correlationId`, `adminWorkspaceId`, `snapshotId`) and never frontend draft assignments, marks trigger failures as failed, and returns structured `AppError` responses
  - Created `backend/api/solver.py` and registered it in `backend/api/router.py` - protected `POST /solver/start` and `GET /solver/status/{solver_run_id}` endpoints using the existing `get_current_admin` auth dependency
  - Updated `backend/solver/job.py` - records async job lifecycle into `solver_runs`: `running` on start, `succeeded` on completed/partial job result (with `partial_success` metadata), `failed` on failed result; maps the Unit 45 job statuses to the Unit 46 API statuses without exposing raw Trigger.dev internals
  - Updated `backend/config.py` and `backend/.env.example` - optional Trigger settings (`trigger_api_url`, `trigger_secret_key`, `trigger_solver_task_id`) with safe defaults except the secret key, which is required only when starting a real job
  - Fixed `backend/log/setup.py` - uses `structlog.stdlib.LoggerFactory()` with `add_logger_name`, matching production logging processors and preventing solver-service logs from breaking after the FastAPI app configures logging
  - Updated `backend/tests/conftest.py` - SQLite test fixture now uses `StaticPool` so FastAPI's sync endpoint thread and the test thread share the same isolated in-memory database
  - Created `backend/tests/test_solver_api.py` - 9 route-level tests using a lightweight ASGI caller: start auth, status auth, saved-state-only start payload, Trigger dispatch, active-run rejection, no-work status, defensive integrity failure, trigger failure persistence, status response shape, and structured 404
  - No frontend solver client/polling/UI, no CP-SAT logic changes, no soft constraints, no deployment wiring, no new Python package
  - All 144 backend tests pass

- **Unit 47: Frontend Solver API Client**
  - Created `frontend/src/lib/api/solver.ts` — frontend API client layer for the protected backend solver endpoints (Unit 46); API-client layer only, no solver UI, no polling, no Zustand solver state
  - `SolverRunStatus` union (`pending` | `running` | `succeeded` | `failed`) mirrors the backend `SolverRunStatus` enum; `SolverRunId` alias type for run identifiers
  - `SolverRunStatusResponse` DTO matches the backend `SolverRunStatusResponse` JSON shape exactly: `solver_run_id`, `status`, `job_id` (nullable), `created_at`, `updated_at`, `scheduled_count` (nullable), `unscheduled_count` (nullable), `partial_success`, `failure_message` (nullable) — backend snake_case field names used consistently with existing API-client conventions
  - `startSolverRun()` — `POST /solver/start` with **no request body**; the backend solves the *saved* timetable state, so no frontend draft assignment payload is ever transmitted; the shared Unit 6 `apiRequest` base client attaches the Supabase auth token
  - `getSolverRunStatus(runId)` — `GET /solver/status/{runId}` (run id `encodeURIComponent`-escaped); also routed through the authenticated base client
  - `parseSolverError` + `readSolverError` — solver-specific structured-error parsing; reads the backend `{ error: { code, message } }` envelope (stored on `ApiRequestError.detail` by the base client) and maps the five backend codes (`solver_run_active`, `solver_integrity_failed`, `solver_no_rooms`, `solver_job_trigger_failed`, `solver_run_not_found`) onto specific user-facing messages covering solver-already-running, saved-state-not-usable / defensive-check-failed, job-trigger-failed, and run-not-found; unknown errors are re-thrown unchanged (nothing swallowed silently)
  - `SolverErrorCode` union documents the backend error-code contract for later integration; all types and functions exported build-safe for the future solver UI/polling unit
  - No new package required; no TanStack Query polling, no Run Solver button behavior, no editing-disabled state, no timetable refresh, no assignment-save changes, no backend changes, no mock solver data
  - Frontend build (`npm run build`) succeeds with zero TypeScript errors

- **Unit 48: Frontend Async Solver Integration**
  - Created `frontend/src/features/timetable/useSolverRun.ts` — solver run lifecycle hook owning the full async flow against **saved** timetable state (never sends draft); `useMutation(startSolverRun)` seeds the status cache with the start response (so the initial state shows before the first poll) and tracks `activeRunId`; `useQuery(['solver-status', activeRunId])` enabled only when a run is tracked, with a function-form `refetchInterval` that polls every 2000ms while `pending`/`running` and returns `false` on `succeeded`/`failed` (TanStack Query tears the interval down on unmount); a terminal-transition `useEffect` (guarded by `handledRunRef`) fires `onSucceeded`/`onFailed` exactly once per run via callback refs; exposes `runStatus`, `isActive`, `isStarting`, `startError`, `start`, `dismiss`
  - Created `frontend/src/features/timetable/SolverStatusPanel.tsx` — running / success / partial-success / failure / start-error banner using the solver tokens (`--solver-running-bg`, `--solver-success-bg`, `--solver-partial-bg`, `--solver-failure-bg`); shows real `scheduled_count`/`unscheduled_count` from the backend run status; partial result reached when `partial_success` or `unscheduled_count > 0`; non-running states are dismissible (`onDismiss`); not color-only (icons: Loader2/CheckCircle2/AlertTriangle/XCircle), `role="status"` `aria-live="polite"`
  - `timetable.tsx` — added `useSolverRun({ onSucceeded })`; `onSucceeded` invalidates `['assignments']` and `['schedulable-sessions']`, which (via the existing saved-assignments effect) refetches saved state and resets the draft from the latest saved data; a failed run refetches nothing so saved state stays stable; `editingDisabled = solver.isStarting || solver.isActive`
  - Solver start gating consolidated into a single `solverDisabledReason` (and `canRunSolver = reason === null`) covering, in priority order: run in progress → no rooms → required data still loading (`rooms`/`schedulableSessions`/`savedAssignments` undefined) → blocking/warning validation counts → unsaved draft changes (`isDirty`, since the solver runs from saved state). All editing handlers (`handleSelectSession`, `handleCellClick`, `handleUnschedule`, `handleSave`, `handleDragStart`) early-return when `editingDisabled`; added `handleRunSolver` guarded by `canRunSolver`
  - `TimetableActionBar` — `canRunSolver`/`solverDisabledReason` now required; added `editingDisabled` and wired `onRunSolver`; Run Solver button shows a `Loader2` + "Solving…" running indicator and is disabled (with the reason as its `title`) whenever a run is active; Save button additionally disabled while `editingDisabled`; status line shows "Editing is disabled while the solver runs." during a run, otherwise the disabled reason (error/warning/muted colored by severity); the "View details" violation/warning panel is unchanged
  - Editing lockout threaded through the grid/pool: `editingDisabled` passed to `TimetableGrid` → `GridCell` (droppable `disabled`, click-placement off) → `ScheduledSessionCard` (draggable `disabled`, move-select off, unschedule button hidden), and `UnscheduledPool` → `UnitGroup` → `UnscheduledSessionCard` (draggable `disabled`, click off, dimmed)
  - Polling runs only while active, stops on terminal status, and cleans up on unmount; partial-success warning shows real counts; failed runs keep timetable/draft stable and surface an actionable message; sessions the solver cannot place stay in the unscheduled pool (derived from draft, which excludes only solver-placed sessions); no backend validation API added
  - Frontend build (`npm run build`) succeeds with zero TypeScript errors; no new lint errors introduced (the 5 pre-existing lint problems — protected `components/ui/*` fast-refresh warnings and the Unit 33 saved-assignments set-state-in-effect — are unchanged)

- **Solver run pipeline fixes (post-Unit 48 debugging, 2026-06-12)**
  - Diagnosed the 502 from `POST /solver/start`: the backend's own `solver_job_trigger_failed` `AppError` (status 502), raised because `TRIGGER_SECRET_KEY` was only set in `jobs/.env` — the backend reads `backend/.env`, so `trigger_solver_job` failed with "secret key is not configured" on every start. Added `TRIGGER_SECRET_KEY` / `TRIGGER_API_URL` / `TRIGGER_SOLVER_TASK_ID` to `backend/.env` (and they remain documented in `backend/.env.example`)
  - Fixed payload double-encoding in `backend/services/trigger_client.py`: the trigger request sent `payload` as a pre-serialized JSON **string** (`json.dumps(payload)` + `options.payloadType`); Trigger.dev delivered it to the task verbatim as a string, so `payload.solverRunId` was `undefined`, the bridge payload lost all fields (`JSON.stringify` drops `undefined`), and `job_cli` failed with `invalid_payload` — leaving the `solver_runs` row stuck in `pending`. The client now sends `payload` as a plain JSON object
  - Hardened `jobs/src/trigger/solverJob.ts`: the task now parses a string payload defensively and returns a structured `invalid_payload` failure when `solverRunId`/`correlationId` are missing, instead of silently bridging an empty payload
  - Hardened `backend/services/solver_run.py`: `_reject_active_run` now expires active runs older than `STALE_RUN_CUTOFF` (10 min) as `failed` / `stale_run` instead of blocking all future solver starts forever when a job dies without reporting back (the task itself is capped at 120s)
  - Verified end-to-end against the live stack (uvicorn + `trigger.dev dev` + Supabase): `start_solver_run` → Trigger.dev run → Python bridge → CP-SAT → `apply_solver_result` → status write-back; run reached `succeeded` with 12/12 sessions scheduled and assignments persisted
  - Operational note: dev-environment solver runs execute only while `npx trigger.dev dev` is running in `jobs/`; if it is down, runs queue at Trigger.dev and the run stays `pending` until it reconnects (or is expired by the stale-run guard)

- **Unit 49: Backend Observability**
  - Created `backend/observability/` package (new boundary; documented in `code-standards.md` File Organization and `architecture-context.md` Stack). Layers exception capture + correlation context on top of the existing `backend/log/` structlog foundation rather than replacing it
  - `observability/sentry.py` — `configure_sentry()` initialises Sentry only when `SENTRY_DSN` is set; the SDK import is lazy and every path is defensive, so the backend runs normally when the DSN is absent **or** `sentry-sdk` is not installed (emits `sentry_disabled` / `sentry_unavailable` / `sentry_enabled` structured logs). `send_default_pii=False` and `traces_sample_rate=0.0`; `LoggingIntegration(level=INFO, event_level=None)` keeps log records as breadcrumbs but never auto-promotes them to events — so the many expected `logger.error`/`logger.warning` solver/validation failures are not mistaken for crashes. `capture_unexpected_exception(exc, **tags)` is a no-op when disabled, isolates tags via `new_scope` where available, and never raises
  - `observability/request_context.py` — `RequestContextMiddleware` (pure ASGI, never reads the request body) mints or reuses an `X-Request-ID` per HTTP request, binds it into structlog contextvars (so every log line carries `request_id`), stashes it on `scope["state"]` for exception handlers, and echoes it back as a response header; `get_request_id(request)` resolves it from `request.state`
  - `observability/safe_logging.py` — safe log payload conventions: `MAX_LOGGED_IDS` (20) and `id_sample()` for bounded id logging; module documents the rule that logs prefer counts/IDs and never include tokens, secrets, database URLs, or full student payloads
  - `log/setup.py` — added `structlog.contextvars.merge_contextvars` as the first processor so middleware-bound `request_id` flows into all logs without each call site passing it
  - `main.py` — calls `configure_sentry()` after `configure_logging()`; adds `RequestContextMiddleware` (outermost of our middleware); the catch-all `Exception` handler now logs `unhandled_exception` (with request_id, method, path, error_type) and calls `capture_unexpected_exception` before returning the generic 500 (internals never leak to the caller)
  - `api/errors.py` — `app_error_handler` now logs expected `AppError`s at **warning** level (`app_error` with code/status/path/method); these structured product errors are returned normally and never sent to Sentry
  - `services/assignment.py` — added `assignment_save_requested` (count), `assignment_save_succeeded` (saved_count), and `assignments_cleared` logs; all defensive blocked-placement rejections route through a `_reject_save()` helper that emits a warning-level `assignment_save_rejected` with the code plus IDs/counts (session_id, room_id, capacity, student_count, day, slot) — never full student lists or payloads
  - `services/solver_run.py` — added `solver_start_requested` (admin id), `solver_start_rejected` for the not-solver-ready paths (snapshot-integrity, no-rooms, run-already-active — each with the API error code), `solver_start_no_work` for the harmless succeeded cases, `solver_run_queued` (solver_run_id, correlation_id, job_id, sessions_attempted), `solver_start_trigger_failed`, `solver_run_expired_stale`, and `solver_status_lookup` (found/status) — correlation via solver_run_id + correlation_id
  - Async solver job lifecycle (`solver_job_started`/`completed`/`failed` with duration + counts, bound to solver_run_id/correlation_id) and solver result application logs (`solver_result_applied`/`apply_rejected`/`apply_failed`) already existed from Units 45/43 and satisfy the remaining logging-events scope
  - `config.py` — added `sentry_dsn: str | None = None` and `environment: str = "development"`; `.env.example` documents `SENTRY_DSN` and `ENVIRONMENT` with the no-secret-logging note
  - `requirements.txt` — added `sentry-sdk>=2.0.0` (installed `sentry-sdk 2.62.0` in the backend venv); no other new package
  - Out of scope (not implemented, per spec): frontend Sentry, frontend error boundaries, new API/solver/validation behavior, deployment configuration
  - Verified: all 144 backend tests pass; app imports cleanly with Sentry disabled (no DSN); middleware confirmed to bind `request_id` into logs, echo the `X-Request-ID` response header, and reuse an inbound id

- **Unit 50: Frontend Error Handling and Observability**
  - Installed `@sentry/react` (only new frontend dependency this unit)
  - Created `frontend/src/lib/observability/sentry.ts` — `initSentry()` initialises Sentry only when `VITE_SENTRY_DSN` is set; every path is defensive so the app runs normally when the DSN is absent and no setup/capture call ever throws; `tracesSampleRate: 0` + `sendDefaultPii: false` (crash reporting only, no PII/tracing in v1); `captureUnexpectedError()` no-op when disabled; `isSentryEnabled()` accessor
  - `frontend/src/main.tsx` — calls `initSentry()` before `createRoot` mounts the app
  - Created `frontend/src/components/error/ErrorBoundary.tsx` — app-level class error boundary; `getDerivedStateFromError` flips to fallback, `componentDidCatch` reports to Sentry via `captureUnexpectedError` (with component stack); reload handler calls `window.location.reload()`
  - Created `frontend/src/components/error/AppErrorFallback.tsx` — safe fallback screen for unexpected crashes; explains something unexpected happened, **no** stack traces, single Reload action, token-driven (serif title, `--state-error` accent, surface card) preserving the academic visual language
  - `frontend/src/App.tsx` — wrapped the full app shell with `<ErrorBoundary>` (outermost, around `QueryClientProvider`/`BrowserRouter`/`AuthProvider`/routes)
  - Created `frontend/src/lib/errors.ts` — `getErrorMessage(error, fallback)` shared helper that normalises `ApiRequestError`/`Error`/unknown into a user-facing string; centralises query/mutation error messaging
  - Created `frontend/src/components/error/ErrorState.tsx` — shared inline error panel for API/load failures; `--state-error` border + `--state-error-bg`, `AlertTriangle` icon (not color-only), `role="alert"`, optional title + `onRetry` "Try again" action; documented as system/API errors only (never validation feedback)
  - Timetable assignment-load failure: `timetable.tsx` now reads `isError`/`error`/`refetch` from the `['assignments']` query and renders an `ErrorState` banner ("Saved timetable could not be loaded") with a retry action above the canvas — previously this error was silent
  - Save failure: `saveMutation.onError` now uses `getErrorMessage` with an actionable fallback ("Your timetable changes were not saved. Please try again."); still surfaced via `TimetableActionBar` `saveError`
  - Solver start failure: already surfaced via `SolverStatusPanel` `startError` (Unit 48); unchanged
  - Solver status polling failure: `useSolverRun` now exposes `statusError` (set only while a run is tracked and the poll errors; TanStack Query keeps retrying on its interval); `SolverStatusPanel` renders a warning banner ("Solver status could not be refreshed") that keeps editing locked while the run continues on the backend
  - Solver completion-refresh failure: the `onSucceeded` `['assignments']` invalidation/refetch failure is covered by the same assignment-load `ErrorState` banner
  - Validation distinction preserved: blocking violations and warning issues remain product state in `TimetableActionBar` (error/warning colors, "View details" panel) and are never routed through Sentry, the error boundary, or `ErrorState`
  - Verification checklist (all met): Sentry configured from env vars ✓; app runs with DSN absent ✓; app-level error boundary exists ✓; crashes show safe fallback without stack traces ✓; assignment-load errors visible + actionable ✓; save errors visible + actionable ✓; solver start/status errors visible + actionable ✓; validation warnings not treated as crashes ✓; blocking validation stays product feedback ✓; all new UI token-driven with no hardcoded hex ✓; frontend build succeeds ✓
  - Updated `frontend/.env.example` — documented `VITE_SENTRY_DSN` (optional; app runs without it; public client key) and `VITE_ENVIRONMENT`
  - Out of scope (not implemented, per spec): backend observability, new validation/assignment/solver/drag-drop behavior, styling outside established tokens
  - `npm run build` succeeds with zero TypeScript errors; `npm run lint` shows only the 5 pre-existing problems (3 `components/ui`/auth fast-refresh warnings + the Unit 33 saved-assignments set-state-in-effect) — no new lint errors introduced

- **Unit 51: Backend Constraint and Solver Test Suite**
  - Created `backend/tests/test_constraint_and_solver_suite.py` — a single cohesive, fixture-driven suite (36 tests) that verifies backend solver correctness independently of frontend UX validation; fixtures use the same DTO/domain shapes the solver input builder and result-application service consume in production (no frontend draft state, no production mock state); solver runs use an explicit small time limit (`SOLVE_LIMIT = 10.0`) and the module's deterministic settings
  - **Constraint mirror** (Unit 40 `constraints/`): assignment-based checks for lecturer conflict, student conflict, lecturer unavailable slot, unit/session overlap, room double-booking (incl. multi-slot overlap), room capacity failure, lunch crossing, and off-timetable boundary — each with a positive (flagged) and negative (not-flagged) case asserting product outcomes (constraint type + severity + affected sessions)
  - **Conflict graph** (Unit 40 `build_conflict_graph`): determinism across repeated builds, per-category edge coverage (unit/student edges present, no spurious lecturer edge for distinct lecturers), and neighbor lookup — the one place structural output *is* the product
  - **Snapshot builder** (Unit 41 `build_snapshot_from_data`): compiles real-format `RoomSnapshot`/`SessionSnapshot`/`AvailabilitySnapshot`/`LockedAssignment` into a `SolverInputSnapshot`, extracting locked assignments, deriving the unscheduled set, and emitting lecturer/unit conflict pairs; plus a determinism check
  - **CP-SAT solver** (Unit 42 `solve_timetable`): unscheduled session successfully scheduled; locked saved assignment respected (returned unchanged, never re-solved); room capacity failure leaves session unscheduled; solver never emits a lunch-crossing placement; duration exceeding available block stays unscheduled; lecturer/student/unit-session conflicts force partial results; lecturer-unavailable forces exact placement; explicit partial result when not all sessions fit; determinism across two runs
  - **Result application** (Unit 43 `apply_solver_result`, DB-backed via the `db` SQLite fixture + real ORM models): successful assignments persisted as real `timetable_assignments` rows; partial result persists only the scheduled placements with `is_partial` metadata
  - **Failure safety**: failed (`INFEASIBLE`) run rejected with `solver_failed` and saved assignments byte-for-byte unchanged; `UNKNOWN`/timed-out result with a generated placement never persists it; defensive rejection (generated placement double-booking an existing saved row) rolls back with `blocking_integrity_violation` and corrupts nothing
  - All 12 required spec cases are covered and labelled inline; assertions verify product outcomes (scheduled/persisted/rejected), not solver internals
  - No new dependencies (pytest already present from Unit 40); no pytest config file needed — `python -m pytest` run from `backend/` discovers `tests/` and is the backend test command; no frontend/browser/deployment changes, no new solver features, constraints, soft constraints, or user-facing validation
  - Verified: full backend suite is green — **180 tests pass** (144 prior + 36 new)

- **Unit 52: Frontend Timetable Validation and Interaction Test Suite**
  - Added the frontend test toolchain (only new dev dependencies this unit): `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `jsdom`. Created `frontend/vitest.config.ts` (jsdom environment, globals, `src/test/setup.ts` setup file, `css: false`, Tailwind plugin intentionally omitted) and `frontend/src/test/setup.ts` (jest-dom matchers + RTL `cleanup` after each test). Added `test` (`vitest run`) and `test:watch` scripts to `frontend/package.json` — the frontend test command is `npm test`
  - Created `frontend/src/test/fixtures.ts` — factories producing objects shaped exactly like the real API DTOs (`Room`, `SchedulableSession`, `AssignmentResponse`, `Lecturer`, `SolverRunStatusResponse`) and the `TimetableAssignment` rendering model; kept under `src/test/`, outside production feature state, so no test data can leak into the app
  - **Pure validation-helper tests** (high-value, zero brittleness): `frontend/src/lib/validation/blocking.test.ts` asserts all four blocking rules reject before entering the draft (room double-booking incl. multi-slot overlap, room capacity too small, crossing lunch, running off the timetable), that a move excludes the session's own old position, and that `getBlockingViolatorIds` (the auto-unschedule source) flags capacity and double-booking violators; `frontend/src/lib/validation/warning.test.ts` asserts the warning rules allow placement but flag it (lecturer conflict, student/unit-session overlap, lecturer-availability conflict) and do not fire across different days
  - **Presentational component tests** (visible UI outcomes): `SolverStatusPanel.test.tsx` covers the idle/starting/running/success/partial/failure/start-error/status-error display states; `TimetableGrid.test.tsx` covers no-room (renders no grid), weekday/room/lunch rendering, a scheduled card rendered from assignment data, and the non-color-only warning indicator; `UnscheduledPool.test.tsx` covers empty/loading/error/grouped rendering
  - **TimetablePage integration suite** (`frontend/src/routes/timetable.test.tsx`): mocks the API client modules (`rooms`/`sessions`/`assignments`/`lecturers`/`solver`), `@/lib/supabase`, and the `AppFrame` shell; wraps the page in a real `QueryClientProvider` + `MemoryRouter`. Covers: no-room empty state vs grid render; saved assignments loading into draft state (on the grid, removed from the pool); manual click-based scheduling updating draft state only (Save enabled, nothing persisted); successful save persisting the exact draft payload through `saveAssignments` and resetting the dirty state on refetch; failed save leaving the draft visible and unsaved with the error surfaced; solver disabled while a validation warning exists (and not started); solver enabled with no issues and a run started on click (running banner shown); and automatic unscheduling — when a session's student count grows past the room capacity (driven via `queryClient.setQueryData`), the draft assignment is removed and the session returns to the unscheduled pool
  - Drag/drop outcome is exercised through the equivalent, reliable click-based scheduling path rather than brittle low-level pointer simulation, per the spec's guidance
  - Verified: `npm test` → **43 frontend tests pass** (6 files); `npm run build` succeeds with zero TypeScript errors (test files type-check under `tsc -b` and are excluded from the bundle); `npm run lint` shows only the 5 pre-existing problems (4 `components/ui`/auth fast-refresh exports + the Unit 33 saved-assignments set-state-in-effect) — no new lint errors from the test suite
  - Out of scope (per spec, not added): backend tests, new validation rules, new API routes, new solver behavior, new product features, or mocked production state

- **Unit 53: Full V1 Acceptance Flow**
  - Verification/documentation unit only — no new features, architecture, soft constraints, imports/exports, multi-admin behavior, or student/lecturer-facing views added (confirmed against spec Out of Scope)
  - Created `docs/v1-acceptance-flow.md` — the v1 acceptance checklist/report: environment, date (2026-06-13), test account/context (single Supabase admin, no sensitive credentials), per-step pass/fail for all 23 acceptance steps with evidence sources, spec verification checklist (all 10 items checked), defects, and follow-ups
  - Executed automated evidence for this pass: backend `python -m pytest` → **180 passed**; frontend `npm test` → **43 passed** (6 files); frontend `npm run build` → success, zero TS errors; backend `import main` smoke → `TTS3 API` loads cleanly with Sentry init
  - Each acceptance step mapped to verifying coverage: steps 1, 21, 22 confirmed against the prior **live** stack run (uvicorn + `trigger.dev dev` + Supabase, solver reached `succeeded` 12/12 — recorded post-Unit 48); steps 2–20 and 23 confirmed via the Unit 52 `timetable.test.tsx` integration suite + validation-helper tests (draft state, manual scheduling, save behavior, refresh-discards-draft, blocking rejection, warning allow+flag, solver gating, partial result, auto-unschedule) and backend service coverage
  - No v1-blocking defects found; both suites and the build are green
  - Follow-ups (out of Unit 53 scope, recorded not hidden): fresh fully-interactive human click-through against the running browser app; production Trigger.dev deployment wiring (dev runs require `trigger.dev dev` up, else queue as `pending`); frontend bundle code-splitting (~862 kB / 255 kB gzip single chunk); 5 pre-existing lint warnings unchanged

- **Unit 55: Backend Deployment to Railway** (config + docs done in-repo; deploy + migrations are manual)
  - Created `backend/railway.json` — pins `builder: NIXPACKS`; `deploy.startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT` (Railway injects `$PORT`; `main:app` with `backend/` as working dir per the established import convention); `healthcheckPath: /health`, `healthcheckTimeout: 100`, `restartPolicyType: ON_FAILURE`, `restartPolicyMaxRetries: 10`
  - Deliberate migrations honored: the start command does **not** run Alembic; `docs/railway-deployment.md` documents running `railway run alembic upgrade head` once after config and verifying with `railway run alembic current` (head `0008_create_solver_runs`); production schema must not be modified outside Alembic
  - Pinned `python-dotenv>=1.0.1` in `backend/requirements.txt` — `alembic/env.py` imports `dotenv`, which was only transitively present in the local `.venv`; without an explicit pin the production migration command fails in a clean Nixpacks build (real gap closed, in scope as the production migration process)
  - Created `docs/railway-deployment.md` — deployment guide: monorepo Root Directory must be `backend`; server-side env var table (`DATABASE_URL`, `SUPABASE_URL`, `CORS_ORIGINS`, `ENVIRONMENT`, optional `SENTRY_DSN`, optional Trigger.dev vars; `PORT` auto-injected); `CORS_ORIGINS` is a JSON list set to the exact Vercel origin (+ optional explicit local dev origin) with an explicit no-`["*"]`-in-production rule (app sends `allow_credentials=True`); deliberate migration process; security "must NOT expose to frontend" list (`DATABASE_URL`, service role key, Trigger secret, backend Sentry DSN, JWT secrets); manual setup steps; verification checklist mapped to the spec; deployed-URL placeholder
  - Verified in-repo: backend imports cleanly via the venv (`main.app` = `TTS3 API`); `/health` and `/auth/verify` routes present; `alembic heads` reports `0008 (head)` — matching the documented head
  - Out of scope (not done, per spec): Trigger.dev worker deployment, new backend features, solver/architecture changes, Redis/cache infra, multi-tenant behavior
  - Remaining (manual, cannot be done from repo): create the Railway service + connect repo, set Root Directory `backend`, add server env vars, deploy and confirm `/health`, run `alembic upgrade head` deliberately and check `alembic current`, record the deployed URL in `docs/railway-deployment.md`, set Vercel `VITE_API_BASE_URL` to the live backend URL and redeploy the frontend, confirm `CORS_ORIGINS` contains the Vercel origin, and run the health/protected-endpoint/CORS verification on the live URL

- **Unit 54: Frontend Deployment to Vercel** (config + docs done in-repo; deploy is manual)
  - Created `frontend/vercel.json` — pins `framework: vite`, `buildCommand: npm run build`, `outputDirectory: dist`, `installCommand: npm install`, and an SPA fallback rewrite (`/(.*)` → `/index.html`) so React Router deep links resolve; static assets are served before rewrites so hashed `/assets/*` are unaffected
  - Created `docs/vercel-deployment.md` — deployment guide: monorepo Root Directory must be `frontend`; documents the four public env vars (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_API_BASE_URL`, optional `VITE_SENTRY_DSN`/`VITE_ENVIRONMENT`); explicit security "must NOT configure" list (service role key, `DATABASE_URL`, Trigger.dev secrets, backend Sentry DSN, JWT secrets); manual setup steps; verification checklist; placeholder for the deployed URL
  - Noted the `VITE_API_BASE_URL` cross-dependency: dev uses `/api` proxy, Vercel needs an absolute backend URL; until Unit 55 deploys the backend, API-backed pages will fail (Supabase auth still works) — documented as a temporary limitation per spec
  - Re-verified production build locally: `npm run build` → success, single ~862 kB / 255 kB-gzip chunk (pre-existing code-split follow-up), zero TS errors
  - No backend deploy, DB migrations, Trigger.dev production wiring, new features, or auth-provider changes (spec Out of Scope honored)
  - Remaining (manual, cannot be done from repo): create Vercel project + connect repo, set Root Directory `frontend`, add env vars (Production scope), deploy, record deployed URL in `docs/vercel-deployment.md`, run signed-out redirect + login/signup + deep-link verification on the live URL

- **Unit 57: Final V1 Scope Guard and Hardening Pass** (verification + hardening only — no features added)
  - Verification/hardening unit per spec: a full scope, architecture, UX, and safety pass confirming the app matches the intended v1 product without accidental v2 complexity. **No v1 defects were found, so no corrective code changes were required** — the only change in this unit is this progress-tracker update (honoring the user's "only change things if you need to" and the spec's no-dumping-ground rule)
  - **Scope Guard Checklist — all confirmed NOT introduced** (verified via repo-wide greps + review of `frontend/src/App.tsx` routes, `backend/models/`, and `backend/api/router.py`):
    - blob/object storage — none (no `supabase.storage`, `@vercel/blob`, or S3 usage; only out-of-scope mentions in context docs)
    - Redis/caching infrastructure — none
    - soft constraints — none; `backend/solver/model.py` objective is `Maximize(sum(scheduled_vars))` with an explicit "No soft preferences" comment
    - student-facing views — none; routes are exactly `/login`, `/signup`, and admin-protected `/timetable`, `/rooms`, `/lecturers`, `/students`, `/units`
    - lecturer-facing views — none (the `/lecturers` route is admin CRUD, which is in scope)
    - multi-admin collaboration / multi-tenant organizations / RBAC — none
    - timetable version history — none (Alembic `revision`/`down_revision` IDs are migration-chain metadata, not a versioning feature)
    - user-defined constraint rules — none (constraints are hardcoded / developer-defined)
    - automatic student allocation — none
    - file imports/exports (CSV/Excel/upload/download) — none
  - **Verification Checklist — all pass**:
    1. Frontend-owned validation architecture preserved — `routes/timetable.tsx` holds the draft in local state initialized from saved backend assignments; validation lives in `lib/validation/`
    2. Blocked placements rejected immediately — `handleCellClick`/`handleDragEnd` call `checkProposedPlacement` and return early on any blocking issue, so blocked placements never enter the draft
    3. Warning placements remain visible and block solver — warnings are computed from the draft, flagged on cards (`warningSessionIds`), and folded into solver gating
    4. Any validation issue blocks solver — `solverDisabledReason` is non-null when any blocking violation, warning, or unsaved-dirty state exists (`canRunSolver` false)
    5. Edits persist only through explicit save — draft → DB only via `handleSave`/`saveMutation`; refetch resets the draft from saved state
    6. Backend defensive checks ≠ normal UX validation — `services/assignment.py` `_reject_save` is defensive ("does not change behavior"); warning-level conflicts are deliberately allowed through; `backend/api/` imports `constraints` nowhere and there is no validation/constraints router (invariant 13)
    7. Solver uses the backend constraint mirror — `solver/snapshot.py` calls `constraints.graph.build_conflict_graph`, extracts the lecturer/student/unit conflict pairs, and `solver/model.py` enforces them as no-overlap constraints
    8. No accidental v2 features present (see Scope Guard above)
    9. No blob storage / Redis / soft constraints / version history / role views introduced (see Scope Guard above)
    10. Destructive changes require confirmation — rooms/lecturers/students/units each have a wired delete confirmation `Dialog`; the room dialog communicates the cascade impact ("Any sessions scheduled in this room will need to be rescheduled")
    11. All styling uses tokens — **zero hardcoded hex** across `frontend/src/**/*.{ts,tsx}`; all colors resolve through `var(--token)`
    12. Progress tracker and docs match implementation — confirmed; deployment env docs present (`docs/railway-deployment.md`, `docs/vercel-deployment.md`, `docs/trigger-dev-deployment.md`, `docs/v1-acceptance-flow.md`; `.env.example` for `frontend/`, `backend/`, and `jobs/`)
    13. V1 acceptance flow passes — backend **185 passed** (`python -m pytest`), frontend **43 passed** (6 files, `npm test`), frontend production build **success** (zero TS errors); no v1-blocking defects
  - Additional hygiene confirmed: no `TODO`/`FIXME`/`HACK`/`@ts-ignore`/`@ts-expect-error` markers and no `any` types in source
  - Only non-blocking follow-up unchanged (not a v1 defect): the single ~862 kB / 255 kB-gzip JS chunk (frontend bundle code-splitting), plus the standing manual deploy steps (Units 54–56) and a fresh interactive human click-through
  - Conclusion: the app is a **hardened v1 release candidate**

- **Unit 58: Backend Year-Level Derivation and Enrolment Sync**
  - Added `backend/services/year_level.py` — pure helper `parse_unit_year_level(code)` (first digit, must be 1/2/3, whitespace-stripped) and structured `InvalidUnitCodeError(ValueError)`; reused by schemas, services, and the migration backfill
  - `backend/models/unit.py` — added NOT NULL `year_level` column, `ck_unit_year_level` check constraint (`year_level IN (1,2,3)`), and `back_populates="units"` on the `students` relationship
  - `backend/models/student.py` — added `ck_student_year_level` check constraint and a `units` relationship over the existing `unit_students` join table (`back_populates="students"`, referenced by table name to avoid a cross-module import)
  - `backend/schemas/unit.py` — `UnitCreate`/`UnitUpdate` validate `code` via the parser (422 on invalid first digit) and never accept `year_level`; `student_ids` default changed to `None` so the service can distinguish "omitted" from explicit `[]`; `UnitResponse` includes `year_level`
  - `backend/schemas/student.py` — added `EnrolledUnitSummary` (id/code/name/year_level); `StudentResponse` includes `units` and a computed `unit_count`; year-level validation tightened from 1–5 to 1–3 on create and update
  - `backend/services/unit.py` — `create_unit` derives and stores the year level and defaults enrolment to all students in that year when `student_ids` is omitted (explicit list, including empty, respected verbatim); `update_unit` recomputes `year_level` on code change without replacing the selected student list
  - `backend/services/student.py` — `create_student` auto-enrols the new student into all matching-year units in the same transaction; `update_student` preserves enrolments (a year change never silently drops memberships)
  - `backend/api/units.py` — documented later-frontend query-invalidation expectations (`['units']`, `['students']`, `['schedulable-sessions']`, indirectly `['assignments']`)
  - Migration `backend/alembic/versions/0009_unit_year_level_and_year_constraints.py` — adds the column nullable, backfills by parsing each existing code (fails loudly on an invalid first digit), enforces NOT NULL, then adds both year-level check constraints; head is now `0009`
  - Tests: new `backend/tests/test_year_level_and_enrolment.py` (parser success/failure, schema code validation, unit-create year derivation + default/explicit enrolment, student-create auto-enrolment, update preservation, DB + schema year constraints, response shapes); updated existing solver test fixtures to set `year_level`; backend **215 tests green** (30 new)
  - No frontend, solver, or new dependency changes (per spec scope)

- **Unit 59: Backend Unit Teaching Team and Session-Level Lecturer**
  - `backend/models/unit.py` — added the `unit_lecturers` join table (composite PK on `(unit_id, lecturer_id)`, both FKs `ondelete="CASCADE"`); removed `Unit.lecturer_id`/`lecturer`; added a `lecturers` many-to-many relationship (lazy selectin); `students`/`sessions` relationships unchanged
  - `backend/models/session.py` — added nullable `lecturer_id` FK (→ `lecturers.id`) and a `lecturer` relationship (lazy selectin); kept nullable so a session can exist without a lecturer (then it is not schedulable)
  - `backend/models/__init__.py` — registered `unit_lecturers`
  - `backend/schemas/unit.py` — `UnitCreate`/`UnitUpdate` replace `lecturer_id` with `lecturer_ids` (create requires ≥1, update requires ≥1 when supplied); `UnitResponse` now exposes `lecturers: LecturerSummary[]`
  - `backend/schemas/session.py` — `SessionCreate`/`SessionUpdate` accept optional `lecturer_id`; `SessionResponse` adds `lecturer_id` + `lecturer: LecturerSummary | None` (imports `LecturerSummary` from `schemas.unit`)
  - `backend/services/unit.py` — `_require_lecturers` validates+dedupes the team (422 on unknown); `create_unit` persists the team; `update_unit` may replace the team but rejects removing a lecturer still assigned to one or more of the unit's sessions with a structured `422` (`lecturer_still_assigned`), never silently unsetting
  - `backend/services/session.py` — `create_session` resolves the lecturer (explicit ⇒ must be in team `lecturer_not_in_team`; omitted + one team lecturer ⇒ auto-assign; omitted + multiple ⇒ reject `lecturer_required`); `update_session` validates a new lecturer belongs to the team; `list_schedulable_sessions` filters `Session.lecturer_id IS NOT NULL` and uses `session.lecturer` for display
  - `backend/services/assignment.py` — `_build_response` and eager-load queries source the lecturer from `session.lecturer` (not `unit.lecturer`)
  - `backend/solver/snapshot.py` — DB loader reads `session.lecturer_id` per session and skips lecturer-less sessions; CP-SAT model and pure `build_snapshot_from_data` builder unchanged (full solver integration deferred to Unit 68)
  - Migration `backend/alembic/versions/0010_unit_lecturers_team_and_session_lecturer.py` — creates `unit_lecturers`, backfills it from `units.lecturer_id`, adds nullable `sessions.lecturer_id`, backfills each session from its parent unit's lecturer, then drops `units.lecturer_id`; head is now `0010`
  - Tests: new `backend/tests/test_unit_team_and_session_lecturer.py` (17 tests); updated `test_apply.py`, `test_solver_api.py`, `test_solver_job.py`, `test_constraint_and_solver_suite.py`, and `test_year_level_and_enrolment.py` fixtures to the new team + session-lecturer shape; backend **232 tests green** (17 new)
  - No frontend, CP-SAT model, or new dependency changes (per spec scope)

## In Progress

- Unit 55 — in-repo deliverables complete (`backend/railway.json`, `docs/railway-deployment.md`, `python-dotenv` pinned in `backend/requirements.txt`); blocked on the manual Railway dashboard/CLI steps, the deliberate production migration run, and live-URL verification, which must be done by a human.
- Unit 54 — in-repo deliverables complete (`frontend/vercel.json`, `docs/vercel-deployment.md`); blocked on the manual Vercel dashboard/CLI steps and live-URL verification, which must be done by a human.

## Next Up

- Run the post-v1 interactive checklist against the full stack, then complete the outstanding manual deployment wiring for Vercel, Railway, and Trigger.dev. WebSocket solver progress and bundle code-splitting remain later, out-of-scope follow-ups.

## Open Questions

- None from Unit 3.

## Consistency Fixes

- **Stale constraint enum test assertion**: commit ed2cd7e renamed `ConstraintType.LECTURER_AVAILABILITY`'s value to `lecturer_unavailable` (matching the frontend `WarningIssueType` naming) but left `test_constraints.py` asserting the old `lecturer_availability` string; the test now asserts `lecturer_unavailable`.
- **Slot renaming (s5-s8 → s4-s7)**: PM slots are now s4-s7 (s1-s3 AM, s4-s7 PM). Updated `slots.ts`, `lecturers.ts` (frontend), `backend/models/lecturer.py`, and created migration `0004_rename_availability_slots.py` to rename the Postgres enum values in existing databases.
- **Post-v1 session membership model**: Replaced the stale "optional session students" wording with hidden allocation rows: lectures include all enrolled students, tutorials use balanced groups, and zero-allocation sessions have no student conflicts.
- **Unit field naming**: Standardised on `id` (DB primary key), `code` (course code e.g. HIS101), `name` (unit name). Updated `units.tsx` form state and specs 21/22/24.

## Architecture Decisions

- **Frontend-owned validation and explicit save workflow**: Manual scheduling now edits a frontend draft first. The admin explicitly saves the timetable to persist assignments. The frontend owns all user-facing validation: blocking rules reject impossible placements, warning rules allow placements but block solver execution. Backend assignment validation is defensive only until backend constraints are mirrored later for solver use.
- **Validation severity split**: `blocking` means the placement cannot enter/remain in the timetable draft. `warning` means the placement remains visible but invalid/warning-styled and blocks solver execution.
- **Blocking placement rules**: room double-booking, room capacity too small, crossing lunch, and running off the timetable. Data changes that create these states automatically unschedule affected sessions.
- **Warning placement rules**: session-level lecturer conflicts, allocated-student conflicts, lecturer availability conflicts, and other non-blocking conflicts represented by current data. Independent unit/session overlap is retired. Warning-invalid assignments may be saved.
- **Post-v1 scheduling identity**: Unit year comes from the first code integer (1-3); units own lecturer teams, sessions own lecturer identity, and hidden session allocations drive capacity, conflicts, and solver input.
- **Draft-only Clear All and frontend-only filters**: Clear All mutates only the current frontend draft until Save; management search/filter state stays local to the frontend.

- **`backend/log/` not `backend/logging/`**: The spec suggests `backend/logging/` for structlog helpers, but naming a local package `logging` shadows Python's stdlib `logging` module, breaking structlog's own imports. The directory is named `log/` instead.
- **Backend runs from `backend/` as working directory**: Import paths in `main.py`, routers, and `alembic/env.py` omit the `backend.` prefix (e.g., `from config import settings`). Commands: `uvicorn main:app --reload` and `alembic upgrade head` are both run from inside `backend/`.
- **shadcn/ui `base-nova` style with Tailwind v4**: shadcn 4.10.0 uses `base-nova` as the
  default style for new Vite projects with Tailwind v4. This style depends on `@base-ui/react`
  rather than `@radix-ui/react-*`. All future component additions should use `npx shadcn@latest add`.
- **`@tailwindcss/vite` plugin (Tailwind v4)**: No `tailwind.config.js` — configuration is
  CSS-based via `@theme` in `index.css`. Design tokens are CSS custom properties in `:root`
  and referenced via `var(--token-name)` in components per `ui-context.md`.
- **Path alias `@/*` → `src/*`**: Configured in both `vite.config.ts` and `tsconfig.app.json`.
  Root `tsconfig.json` also has paths for shadcn CLI compatibility.
- **BrowserRouter in App.tsx**: Router is declared at the App level. `main.tsx` remains a thin
  entry point. Route protection is deferred to the auth unit.
- **Layout components in `src/components/layout/`**: Reusable shell components are kept
  separate from route pages and feature modules. Route pages compose layout primitives.

## Session Notes

- shadcn 4.10.0 `init -d` fails at workspace config loading in monorepo layouts; workaround
  is to let it write `components.json` then add components individually via `npx shadcn@latest add`.
- The `form` component is not available via the `base-nova` registry; it was written manually
  using the standard shadcn form pattern with `react-hook-form` and `@radix-ui/react-label`.
